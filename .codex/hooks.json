{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'ROOT=\"$(git rev-parse --show-toplevel)\"; while [ \"$ROOT\" != \"/\" ] && [ ! -f \"$ROOT/.codex/scripts/sprint-status-gate.sh\" ]; do ROOT=\"$(dirname \"$ROOT\")\"; done; cd \"$ROOT\" && bash .codex/scripts/sprint-status-gate.sh'"
          }
        ]
      }
    ]
  }
}
