# auto-bmad per-story state — written by scripts/state_update.py (full stable schema;
# see references/state-and-resume.md). Prose belongs in reports/{key}.md, not here.
story_key: epic-9
epic_num: 9
story_num: null
branch: epic/9-live-app-hardening-clean-up
status: in-progress
updated_at: "2026-07-01T17:32:32Z"
started_at: "2026-06-30T14:18:23Z"
completed_at: null
active_seconds: 1207
timing_anchor: 1782922632
is_first_in_epic: false
is_last_in_epic: false
needs_project_context_bootstrap: false
git_mode: remote
base_branch: main
tea_risk: null
tea_selected: []
tea_rationale: null
epic_story_count: 11
stories_after_in_epic: null
completed_phases: [0, 1, 2]
code_review_iterations: 2
code_review_loop_done: false
hitl_halt: null
external_review_iterations: 0
convergence_unverified: false
story_trace: null
commits: [aa94d0e]
phase8_steps:
  trace_gate: done
  nfr: done
  test_review: done
  project_context: null
  reconcile: null
  archive: null
  retro: null
gate_decision: PASS
gate_iterations: 0
deferred_work_archived: 0
pr_url: null
ci_run_url: null
ci_status: unknown
pr_merged: false
merge_method: null
merge_commit: null
branch_deleted: false
open_questions:
  - "[RESOLVED in 9.0 create-story] CI e2e runs Playwright against `next dev` (development), so Story 9.0 prod-gate does NOT fire in CI; deterministic ?_time= sun specs stay green. Reopen only if the e2e webServer switches to next build/start."
deferred_work:
  - "[9-1] Maintainer: re-baseline venue-detail visual references (nextjs-app/docs/design/references/screens/{mobile,desktop}/venue-detail.png) to the de-bloated surface so the visual gate flips green. The gate script forbids the dev agent from editing references — deliberate hand-off, not a bypass."
  - "[9-3] Live cold/warm wall-clock latency timings deferred to a maintainer/preview run (live-Supabase ms-timing is non-reproducible locally and the epic forbids flaky timing asserts); the in-CI signal is the RPC-count + cache-hit tests + the manual 14->7 RPC evidence."
  - "[9-4] useVenueDetail intentionally left on the non-deferred planner (single, 9.3-server-cached venue, outside AC3's list-churn scope); residual: dragging the slider with a detail panel open still re-keys the cheap detail query per step. Hand-off candidate to Story 9.10's regression pass if observed; not a 9.4 deliverable."
  - "[9-5] Maintainer: capture/bless a reference PNG for the map-with-user-location-dot state (mobile+desktop) — none exists; dev captured candidates to scratch and is forbidden from self-blessing references. (onboarding mobile+desktop visual gate already PASS; map-primary FAILs are pre-existing stale-reference drift, not 9.5.)"
  - "[9-5 review] AC3 honest-distance label not applied to VenueQuickInfo -> owned by Story 9.9 (quick-info rework). Logged to deferred-work.md."
  - "[9-5 review] AC4 'denied'-vs-'fallback' geolocation-status granularity not distinguished (used 'fallback' as the honest signal) -> conditional/None. Logged to deferred-work.md."
  - "[9-6] Maintainer: rebaseline map-primary.png (mobile+desktop) — stale references still show the removed floating locate/settings buttons + category chips + nav chevrons. CAVEAT: the _state=map-primary forced route suppresses the mobile top-bar (isForcedVisualReference), so capture the REAL returning-user render, not the _state-forced one. Consolidated capture in the dev session scratchpad."
  - "[9-7] Mobile tag-chip UI deferred (Open Q1): the shared tag-filter BEHAVIOUR works on all surfaces but the chip UI is desktop-only (no design reference for a mobile chip row). A mobile chip surface needs a design reference -> maintainer decision."
  - "[9-7] The map-primary.png desktop reference rebaseline (already flagged under 9.6) now ALSO must reflect 9.7's active tag-chip pill state (list+pins filtered) — one combined maintainer rebaseline covering 9.5/9.6/9.7 chrome changes."
blockers: []
overrides: {}
constraints: []
epic_slug: live-app-hardening-clean-up
active_story: null
stories_landed:
  - 9-0-production-gate-the-dev-planner-forcing-url-leak
  - 9-1-clean-app-content-sweep
  - 9-2-design-system-cta-token-fix-copy-correction
  - 9-3-venue-sun-compute-performance-server-caching
  - 9-4-client-query-hygiene-time-change-debounce
  - 9-5-location-onboarding-reliability
  - 9-6-map-chrome-consolidation-dead-control-cleanup
  - 9-7-tag-filtering-real-data-working-chips
  - 9-8-venue-sharing-real
  - 9-9-mobile-venue-quick-info-card-rework
  - 9-10-mobile-device-verification-pass-regression-guards
auto_decisions:
  - "Share-target tiles render text glyphs instead of brand icons [Med] → defer: brand-mark fidelity is a maintainer visual-gate/asset call at rebaseline, logged to deferred-work.md (Tier A, 9-8-venue-sharing-real)"
  - "Shipped share targets differ from the reference's five tiles [Med] → dismiss: functional-only is the correct OQ4 default and Epic 9's anti-dead-control mandate; strict 5-tile parity would reintroduce dead tiles (Tier A, 9-8-venue-sharing-real)"
  - "Venue-detail route + feedback POST swept into read rate-limit bucket [Med] → fix: gate venueRateLimitMiddleware to GET only so mutations keep their own quota (E_review, epic-9)"
  - "DesktopNavBar active tag orphaned with no chip to clear [Med] → fix: prune activeTags to the new allTags union on venue-set change (E_review, epic-9)"
  - "Honest-distance annotation missing on Favoriter list + venue-detail Avstånd card [Med] → fix: thread distanceIsApproximate to FavouritesList + VenueDetailContent (E_review, epic-9)"
  - "ForcedVenueDetailInitialFrame dead shareText label thread [Low] → fix: remove the unused label (E_review, epic-9)"
uat_items:
  - "[9-0] Dev (npm run dev), open /?_time=21:00&_date=2026-07-01 -> planner shows forced 21:00 on 2026-07-01 and does NOT advance with the real clock (forcing honoured in dev). [AC#1 dev, AC#2]"
  - "[9-0] Dev, open / with no params -> planner shows live current time and ticks forward (~every minute). [AC#1 baseline]"
  - "[9-0] Production (npm run build && npm run start), open /?_time=21:00&_date=2026-07-01 -> forcing IGNORED: live current Stockholm time/date shown, clock advances. [AC#1 prod, core fix]"
  - "[9-0] Production, open /?_time=13:00 (time only) -> still ignored: live time shown, no pinned 13:00. [AC#1 single-param edge]"
  - "[9-0] Production, open plain / -> identical to forced-URL prod case (live clock). [AC#1 equivalence]"
  - "[9-0] Run deterministic-sun e2e vs dev (npx playwright test test/e2e/map-primary.spec.ts --project=desktop) -> ?_time=13:00 specs stay green (pre-existing 645:7 width-assertion failure unrelated). [AC#2]"
  - "[9-0] Run unit guard (npm run test -- AppContextProviders) -> prod ignores forced _time/_date and never calls useSearchParams; dev/test honour them. [AC#3]"
  - "[9-1] Venue detail (mobile+desktop, /?venue=test-venue-sunny&_state=venue-detail&_time=14:00): EXPONERING, BÄST KL., PLATSER UTE, the 'Vi räknar…' paragraph, the uncertainty-reason middot line, and 'Blir skuggigt om X min' are ALL absent. [AC#1]"
  - "[9-1] Venue detail: preserved signals still render — % SOL badge, single full-width AVSTÅND card (no empty 2nd cell), Säkerhet/confidence, Solprognos timeline, Öppettider/Adress/Visa Rutt. [AC#1]"
  - "[9-1] Venue detail: no orphaned middot/empty row/dangling label where removed tiles were (mobile+desktop). [AC#2]"
  - "[9-1] Quick-info card (/?_state=map-with-selected-venue&_time=14:00, mobile): no uncertainty paragraph/reason; still shows sun range, Säkerhet %, Avstånd, % SOL badge, Mer Info/Visa Rutt. Visa Rutt route overlay STILL shows 'Säkerhet · <level>' (KEPT route uncertainty util). [AC#1]"
  - "[9-1] Venue list cards (/?_state=map-panel-venues mobile + /?_time=16:30 desktop): no uncertainty text; distance + sun% + amber Säkerhet chip present; clean separators (no dangling trailing middot). [AC#1/#2]"
  - "[9-1] aria-label cleanliness (AC#3): card select-button accessible name reads essentials only; 'Säkerhet' announced ONCE (no 'Säkerhet: 60% Säkerhet 60%' dup); no embedded 'Vi räknar…' paragraph."
  - "[9-1] Behaviour/animation: shaded/low-sun cards still show confidence (no empty placeholder row); favourite toggle works post-aria-refactor; list fade/stagger + quick-info slide-up unbroken."
  - "[9-1] CAVEAT: venue-detail mobile+desktop automated VISUAL gate FAILS until maintainer re-baselines reference PNGs (stale = old design). Manual checks above are authoritative meanwhile; map-with-selected-venue + map-panel-venues gates already PASS."
  - "[9-2] SETUP: cd nextjs-app && npm run dev (http://localhost:3000). If gradients still look olive after the edit, wipe .next and restart (Turbopack caches stale CSS)."
  - "[9-2] VISA RUTT route-overlay button (/?venue=test-venue-sunny&_state=map-with-selected-venue&_time=14:00, mobile) -> gold->bright-amber gradient (gold top, bright amber bottom), NO olive/dark-green start; tap opens native maps. [AC#1]"
  - "[9-2] /about 'Tillbaka till kartan' CTA, mobile (~390px) AND desktop (~1280px) -> same gold->bright-amber ramp, no olive; navigates back to the map. [AC#1]"
  - "[9-2] 404 CTA on a bogus route (/__sunnyseat-invalid), mobile + desktop -> gold->bright-amber ramp, no olive; returns to map. [AC#1]"
  - "[9-2] CTA hover/active/focus -> dims (hover:opacity-90), focus ring on Tab, pill/padding/icon/shadow unchanged — only the gradient colour shifted. [Behaviour gate]"
  - "[9-2] Amber-gradient audit surfaces: venue-detail sun-% badge = clean gold flat fill; 'ÖPPET' status badge = clean bright-amber; neither reads olive. [AC#3]"
  - "[9-2] Swedish (default) nav rooftop filter chip reads 'Takterrass' (not the old 'Takt'); switch to English -> same chip reads 'Rooftop'. [AC#2]"
  - "[9-3] SETUP: cd nextjs-app && npm run dev. Real caching/edge behaviour only engages with SUNNYSEAT_SUN_ENGINE=real + Supabase service-role in .env.local (flag-off returns the seed, no engine). Rate limit = 120 req/60s per IP."
  - "[9-3] curl -sD - http://localhost:3000/api/venues -o /dev/null -> Cache-Control: public, max-age=30, s-maxage=30, must-revalidate + an ETag present. [AC3]"
  - "[9-3] Conditional 304: re-request with -H 'If-None-Match: <etag>' -> HTTP 304, same ETag + Cache-Control echoed, empty body. [AC3]"
  - "[9-3] Freshness headers: /api/venues AND /api/venues/<slug> both carry X-Sun-Data-Source + X-Weather-Updated-At; detail route also has Cache-Control s-maxage=30. [AC2 detail parity]"
  - "[9-3] Edge-cacheable: request /api/venues plain and with -H 'X-Forwarded-For: 9.9.9.9' -> identical ETag + body (GET no longer reads request IP). NOTE: whether the CDN actually SERVES the s-maxage copy is a Vercel-edge behaviour, NOT observable on next dev. [AC3]"
  - "[9-3] Relocated rate limit (now in Edge proxy.ts; runs in-process on next dev): for i in $(seq 1 130); do curl -s -o /dev/null -w '%{http_code}\n' -H 'X-Forwarded-For: 8.8.8.8' .../api/venues; done -> first ~120 = 200, rest = 429 within the minute; malformed XFF ('not-an-ip') -> 400."
  - "[9-3] Warm-vs-cold (real-engine path): warm the server, open the venue list, reload it, open 'Mer info' then reopen without changing the time -> 2nd list load + reopened detail feel materially snappier (buildings 24h + sun 15min caches). Subjective; exact ms deferred to a maintainer preview run."
  - "[9-3] Byte-identical sun outputs: for the same venue + same time-of-day, currentSunStatus / sun-window / peak-time (UI + /api/venues/<slug> JSON) read CORRECT and UNCHANGED from before the perf fix — caching only makes it faster, never alters the sun output. [AC1]"
  - "[9-4] First-load single fetch (AC2): hard-reload with DevTools Network filtered to 'venues' -> exactly ONE GET /api/venues fires, only AFTER location settles (no earlier fallback-coords request that re-fires at GPS = no double-fetch)."
  - "[9-4] Pre-settle gating (AC2): reload and watch the geolocation-pending window -> NO /api/venues request while location is still resolving (idle/pending); the first+only request appears once status = success/fallback."
  - "[9-4] Time-drag coalescing (AC3): drag the time slider rapidly across many 15-min steps in one motion and release -> Network shows at most ONE new /api/venues after release (not one per snapped step)."
  - "[9-4] Thumb/badge stay live during drag (AC3): the slider thumb + time badge update continuously/smoothly throughout the drag (decoupled from the deferred query) — never freeze on the network."
  - "[9-4] List freshness after settle (AC3): after the drag settles, the venue list/sun data updates to the new settled time (not left showing stale pre-drag data) once the single request resolves."
  - "[9-4] Live-now state (AC3): drag away from the current time then back onto it -> reads as an intentional 'live now' state (request, if any, carries no planner params), not a silent no-op."
  - "[9-4] Favoriter derives from cache (AC1): with Närmast loaded (>=1 favourite visible), clear Network and switch to Favoriter -> NO new /api/venues?ids= fires, favourites appear instantly; toggling Närmast<->Favoriter issues no further /api/venues."
  - "[9-5] SETUP: cd nextjs-app && npm run dev (http://localhost:3000). DevTools > Sensors to grant/deny/emulate geolocation; device-toolbar (mobile viewport) for the floating locate button (lg:hidden on desktop). Fresh Incognito / clear site data for each first-time-user run."
  - "[9-5] First-paint no map-flash (AC1): Incognito (empty localStorage), reload -> the amber welcome overlay covers the whole screen on the FIRST frame; the map must NOT briefly show through before it. PASS = zero map-behind-overlay flash."
  - "[9-5] First-click liveness / no dead-click (AC1): on the fresh first-paint overlay, click 'Use my location' IMMEDIATELY as the very first interaction -> the geolocation permission prompt fires every time (repeat a few reloads). PASS = never a dead first click."
  - "[9-5] Amber dot on GRANT (AC2): DevTools Sensors set a Gothenburg lat/lng, click Use my location -> Allow -> overlay fades, map recenters, ONE amber dot (18px, white ring, halo) at your coords; change coords + re-trigger -> same dot repositions without flicker."
  - "[9-5] NO dot on DENY/fallback (AC2): fresh Incognito; Block the prompt OR click skip -> map centres on Gothenburg centrum, NO amber user-location dot."
  - "[9-5] Honest distance labels (AC3): compare list distances between the deny/skip run and the grant run -> fallback reads '≈ från centrum' (sv)/'≈ from centre' (en); a real granted fix omits the qualifier."
  - "[9-5] Locate-button feedback — MOBILE viewport only (lg:hidden on desktop) (AC4): click the floating locate button -> aria-busy + pulsing icon (data-locate-state=pending); on deny/fallback it stays CLICKABLE (data-locate-state=fallback) and re-requests on re-click."
  - "[9-5] Returning user straight to map (AC1): after completing a grant/deny once in a normal window, reload (flag now set) -> the map appears directly with NO welcome overlay and no overlay-then-null flash."
  - "[9-5] SW-update reload = PRODUCTION ONLY (AC4c): not testable on npm run dev (SW disabled in dev). Verify on a deployed build where a new SW activates over an open tab -> exactly one auto-reload, no loop."
  - "[9-6] Mobile (<1024px, DevTools device-toolbar): over the map ONLY the vertical zoom +/- stack floats — the previously-floating locate + settings buttons are GONE. (The old map-primary reference PNG still shows the 4-button stack, so the automated visual gate FAILs by design -> maintainer rebaseline, not a defect.) [AC1]"
  - "[9-6] Mobile: in the top search row, click the settings gear -> it now OPENS the settings modal (previously greyed/cursor-not-allowed, did nothing). Confirm the modal opens and the gear is not greyed. [AC2]"
  - "[9-6] Mobile: click the top-bar locate button (Navigation icon, left of the gear) + watch DevTools Elements -> aria-busy=true + data-locate-state=pending + pulse while resolving; recentres on success; on deny/unavailable -> data-locate-state=fallback and STAYS clickable to re-request (no dead-click regression — 9.5's relocated wiring). [AC1]"
  - "[9-6] Desktop (>=1024px): top-nav 'My location' (desktop-nav-my-location, crosshair) -> map recentres/flies to your location on success (shared fly-to). Confirm both this + the mobile top-bar locate recentre on success."
  - "[9-6] Desktop: the filter-chip row has NO pager chevron arrows flanking it — the dead </> nav-pager chevrons are GONE (the 8 disabled filter chips remain; those are Story 9.7's). [AC3]"
  - "[9-6] Mobile: in the venue-list sort/controls row only the working sort buttons (Sol/sun + Nära/distance) remain — the disabled Café + Öppet-nu category buttons are GONE. [AC3]"
  - "[9-6] Either viewport: click the search box, type a query returning results, press Enter WITHOUT arrowing down -> the first result is selected and the map pans to it (cmdk auto-highlights the first item). Bare-Enter no longer does nothing. [AC4]"
  - "[9-7] SETUP: cd nextjs-app && npm run dev, DESKTOP viewport >=1024px (tag chips are desktop-only). Real DB tags require the real data path (SUNNYSEAT_VENUE_STORE=supabase, the live default); the fixture path uses the same 7 seeded arrays so all steps still exercise. No 'clear all' button — clear by toggling each active chip off."
  - "[9-7] Chips enabled + real labels (AC1/AC2): the top-nav filter-chip row is interactive (not greyed/cursor-not-allowed) and shows REAL DB tags (e.g. Innergård, Hund ok, Wifi, Kanal, Kväll — the first-seen union of the 7 venues), NOT the fabricated placeholders and NOT a truncated 'Takt'."
  - "[9-7] Chip filters list + pins TOGETHER (AC3): click one chip (e.g. Innergård) -> the venue LIST and the map PINS both narrow to only venues carrying that tag, simultaneously (shared TagFilterContext, not just one surface)."
  - "[9-7] Active on-pill (AC4): the clicked chip flips to the on state (dark #1b1b1e / text-primary bg, white label, aria-pressed=true); click again -> deactivates and the full set returns."
  - "[9-7] Second chip WIDENS = OR/union (AC4): with one chip active, click a DISJOINT second chip (e.g. Kanal) -> the result set GROWS to venues matching EITHER tag (union), it does NOT shrink to the intersection."
  - "[9-7] Graceful-empty + clear (AC4): a venue with no matching tag is EXCLUDED while a chip is active but reappears once all chips are cleared; a zero-match combination shows the 'Inga platser hittades' empty state (not a broken/blank list or a stuck skeleton)."
  - "[9-7] sv/en casing (AC5): switch language -> chip labels read with consistent FULL casing (sv Innergård/Kväll <-> en Courtyard/Evening; never a truncated 'Takt'); filtering matches on the canonical sv value so results are locale-independent."
  - "[9-7] Drag-with-chip fires no extra requests (9.4 hygiene): DevTools > Network (filter venues), activate a chip, drag the time slider back and forth -> NO burst of /api/venues per step (nav + map queries de-dupe byte-identically; the tag filter is pure client-side .filter()). Most meaningful on the real data path."
  - "[9-8] SETUP: cd nextjs-app && npm run dev, open http://localhost:3000 (desktop); use dev-tools device emulation for the mobile checks."
  - "[9-8] Desktop: open a venue detail (click a pin/card -> right-side detail panel) -> top-right cluster shows Heart, Share (arrow), Close; the Share button is ENABLED (not greyed), frosted-pill styled. [AC1]"
  - "[9-8] Desktop: click Share -> a centered modal opens ABOVE the detail panel with title 'Dela {venue}', subtitle, a row of 5 brand-colored target tiles, and a copy-link row showing the URL. [AC1]"
  - "[9-8] Desktop share modal: the copy-link URL reads http://localhost:3000/?venue=<slug> (sv default: no locale prefix) with NO _state/_time/_date/tags params, even if the address bar had them. [AC3/OQ1]"
  - "[9-8] Desktop: click 'Kopiera lank' -> flips to 'Kopierad' + check icon ~1.8s then reverts; Ctrl+V elsewhere pastes exactly the ?venue=<slug> URL shown. [AC1]"
  - "[9-8] Desktop modal: click WhatsApp/Facebook/X/Telegram tile -> new tab to that service's share-intent URL prefilled with the venue link/text; click 'E-post' -> OS mail composer opens with the venue URL in the body. [AC1]"
  - "[9-8] Desktop modal: press Escape -> modal closes, detail panel stays; re-open + click the dark scrim outside the card -> also closes; the modal's x button closes only the modal. [AC1/behaviour]"
  - "[9-8] Mobile viewport: open a venue detail (bottom sheet) -> top-right cluster now shows Heart, Share (arrow), Close; the Share button EXISTS + is enabled (mobile previously had none). [AC2]"
  - "[9-8] Mobile w/ navigator.share (mobile Safari/Chrome over HTTPS/localhost): tap Share -> OS native share sheet appears with the venue name as title + the ?venue=<slug> URL; cancel -> nothing else happens (no error, no modal). [AC1]"
  - "[9-8] Desktop browser WITHOUT navigator.share: click Share -> falls back to opening the ShareModal (copy-link + targets) — the button is never dead. [AC1 graceful degradation]"
  - "[9-8] Deep-link (AC3): open http://localhost:3000/?venue=<real-slug> in a fresh/incognito tab -> the correct venue's detail resolves (recipient lands directly on the shared venue)."
  - "[9-8] Deep-link locale: open http://localhost:3000/en/?venue=<slug> -> resolves the same venue with English UI (the /en locale prefix is preserved in shared links). [AC3]"
  - "[9-8] i18n: switch to English, open a venue, click Share -> modal title/subtitle/'Copy link'/'Copied'/target labels all render in English ('Share {name}'). [i18n parity]"
  - "[9-8] Reduced motion (edge): enable OS 'reduce motion', open the share modal -> appears/dismisses without the slide/scale spring (opacity-only); all buttons still function."
  - "[9-8] KNOWN caveats a tester should expect (documented/deferred): share-target tiles render text glyphs (Wa/f/X/Tg/@) not brand logos; clipboard silently no-ops only in an insecure context (off HTTPS/localhost); the automated visual-validate gate is blocked by the host /tmp bug."
  - "[9-9] SETUP: cd nextjs-app && npm run dev; use a MOBILE viewport 360-430 CSS px (DevTools device toolbar) for all checks (the card is lg:hidden)."
  - "[9-9] Open http://localhost:3000/?_state=map-with-selected-venue (390px) -> the compact quick-info card renders anchored above a map pin (~230px wide) with a downward triangle tail. [AC1]"
  - "[9-9] Card header strip: '% SOL' badge is a small pill top-LEFT of the 72px photo strip, favourite heart top-RIGHT; they don't overlap/crowd. [AC1]"
  - "[9-9] Close (X) button is a floating dark pill just ABOVE the card's top-right corner (not partway down the side); clicking it dismisses the card. [AC1]"
  - "[9-9] Card body shows ONLY: centered venue name, amber sun-window line, a confidence % figure, and the distance — NO EXPONERING block, NO uncertainty/Osäker reason, NO 'Blir skuggigt om X min', NO explanatory paragraph. [AC1 / 9.1 removals]"
  - "[9-9] Bottom row: a full-width-ish 'VISA RUTT' gold->amber gradient button (flex-1) left with 'MER INFO' outlined button right; both enabled, not greyed. [AC1]"
  - "[9-9] Tap 'VISA RUTT' -> route action fires (route overlay / maps affordance opens) — wired, not dead. [AC1 behaviour]"
  - "[9-9] Tap 'MER INFO' (or the centered venue name) -> the full venue detail view opens. [AC1 behaviour]"
  - "[9-9] Tap the favourite heart on the strip -> toggles filled/unfilled (wired handler). [AC1 behaviour]"
  - "[9-9] Full-sun: load .../?_state=map-with-selected-venue&_time=13:00 -> high '% SOL' badge + populated sun window; layout holds, no overflow/clipping at 360px. [AC2]"
  - "[9-9] Shaded/partial (e.g. _time=21:00): sun line shows the 'Soltid saknas' fallback (not a range) and the card still renders cleanly with no overflow at 360-430px. [AC2]"
  - "[9-9] Cross-width: repeat at 360px and 430px -> name, sun line, distance, both CTAs fit without horizontal overflow or truncation. [AC2]"
  - "[9-9] Planner-collision: with the 'Planera soltid' time-slider panel visible, select a pin near the TOP of the map -> the card is pushed down so its top edge/'% SOL' badge sits clearly BELOW the planner panel with a visible gap (no overlap). [AC3]"
  - "[9-9] Honest distance FALLBACK: deny/skip geolocation (DevTools Sensors 'unavailable' or dismiss prompt), reload, select a venue -> distance shows the real number annotated '≈ från centrum' (sv default). [AC3]"
  - "[9-9] Honest distance GRANTED: set a custom geolocation fix (DevTools Sensors), reload, select a venue -> distance shows the number WITHOUT the '≈ från centrum' qualifier. [AC3]"
  - "[9-9] Honest-distance parity: with fallback active, compare the same venue's distance in the list card vs the quick-info card -> both agree it's centrum-relative (both qualified). [AC3]"
  - "[9-9] Animation: select a venue then a different pin -> content crossfades to the new venue; dismissing slides/fades out; with OS 'reduce motion' on, transitions are opacity-only (no slide/scale). [Animation gate]"
  - "[9-10] SETUP: cd nextjs-app && npm run dev -> http://localhost:3000 (dev honours ?_time= / ?_state= forcing)."
  - "[9-10] Null-coord project() fix (Task 3): open DevTools Console un-filtered, load a ?venue=<null-coord-slug>&_state=map-with-selected-venue deep-link (a venue with NO coordinates), pan/zoom -> NO repeated MapLibre 'Expected value to be of type number, but found null' warning (pre-fix it fired ~3x + re-fired on every move/zoom). NOTE: reproducing by hand needs a real null-coord venue row (fixtures all have coords) -> otherwise automated-only via item 3."
  - "[9-10] Guard automated proof: npx vitest run test/components/MapView.test.tsx -> the Task-3 tests pass (wholly-null location + null lat/lng never call project; finite-coord still projects; guard bails before the move/zoom listener registers)."
  - "[9-10] Normal path unbroken: open /?venue=test-venue-sunny&_state=map-with-selected-venue&_time=13:00 -> the quick-info card anchors to the pin (a well-formed venue still projects/positions)."
  - "[9-10] Mobile onboarding gate (9.5): DevTools iPhone-14 emulation, clear localStorage, load / -> the welcome overlay gates the map, shell behind is inert/aria-hidden, NO map flash-through."
  - "[9-10] Mobile location dot (9.5): grant a Gothenburg geolocation override + reload with onboarded flag set -> one amber user-location-pin at your coords; absent on fallback. NOTE: DevTools emulation doesn't fire the onboarding-CTA getCurrentPosition faithfully — use the returning-user auto-acquire path; CTA prompt is a needs-human real-device check (logged pre-existing gap)."
  - "[9-10] Mobile chrome consolidation (9.6): on / (mobile) -> a SINGLE control set: top-bar locate + settings enabled, the gear OPENS the Settings modal (not a dead stub), duplicate floating locate/settings over the map are GONE, zoom +/- remain."
  - "[9-10] Mobile sharing (9.8): open /?venue=test-venue-sunny&_state=venue-detail (mobile) -> the venue-detail sheet shows an enabled 'Dela plats' share button (tapping invokes native share where supported)."
  - "[9-10] Mobile quick-info rework (9.9): open /?venue=test-venue-sunny&_state=map-with-selected-venue&_time=13:00 (mobile) -> the reworked card renders without overflow and sits CLEAR below the 'Planera soltid' planner panel (badge not jammed under the slider)."
  - "[9-10] Clean-app deletions + CTA token (9.1/9.2): mobile venue detail -> NO EXPONERING / uncertainty-reason / 'Blir skuggigt om X min' / fabricated 'BÄST KL.'/'Platser ute ~N' cards / orphaned separators; real %-Säkerhet + Avstånd cards remain; VISA RUTT CTA is the gold->bright-amber ramp with NO olive start."
  - "[9-10] Mobile regression net: npx playwright test --project=mobile -> epic-9-mobile-regression.spec.ts passes 5/5 (onboarding gate, location dot, share button, single-control-set/settings-modal, live-layout planner clearance). Expect ~4 PRE-EXISTING mobile failures unrelated to 9.10 (2 onboarding CTA-geolocation, 2 favourites 'Sol HH:MM' label) — logged maintainer follow-ups, not regressions."
  - "[9-10] Clean-URL date guard: npx vitest run test/unit/queries/clean-url-date-selection.test.tsx -> 2 tests pass (a future date on a clean/un-forced URL flows into the /api/venues query key + fires exactly one fresh fetch; reselecting today returns to the live planner-less key)."
  - "[9-10] Confirmed AC2 guards green: npx vitest run test/components/AppContextProviders.test.tsx test/unit/queries/deferred-planner-query.test.tsx test/components/MapView.test.tsx test/components/UserLocationLayer.atdd.test.tsx test/components/UserPin.test.tsx -> all pass (prod-gate ignores ?_time=, settled time change = one venues request, Favoriter<->Närmast no redundant fetch, dot on success/hidden on fallback)."
  - "[9-10] Full gate sanity: npx tsc --noEmit (0), npx eslint . --quiet (0), npx vitest run (all green). NEEDS-HUMAN: physical real-device native share + real-GPS onboarding-CTA prompt; the stale mobile reference-PNG rebaseline cascade is a maintainer follow-up (not gate-able here)."
