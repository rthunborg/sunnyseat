## Story epic-9
- [Phase 2 — epic test design] sun-engine.ts has a FALSE comment 'one buildings fetch reused internally' (lines 51/317) — code actually fires TWO get_buildings_near_point RPCs per venue. Stale comment masked the perf bug; Story 9.3 must correct it and reviewers must not trust it as evidence the fetch is shared.
- [Phase 2 — epic test design] Open Q (R-to-plan): if CI e2e builds run with NODE_ENV=production, Story 9.0's prod-gate disables ?_time=/?_date= forcing and breaks the deterministic sun e2e specs. Confirm CI build mode BEFORE merging 9.0.

## Story 9-0-production-gate-the-dev-planner-forcing-url-leak
- [Phase 3 — create-story] CI-e2e build-mode open Q RESOLVED: CI runs Playwright vs `next dev` (NODE_ENV=development) per playwright.config.ts webServer + build-and-test-nextjs.yml (no next build/start in the e2e path), so Story 9.0's prod-gate does not fire in CI and the deterministic ?_time=13:00 sun specs stay green. Reopen only if the Playwright webServer switches to a production build.
- [Phase 5 — dev-story] Pre-existing UNRELATED e2e failure observed: nextjs-app e2e map-primary.spec.ts:645 (desktop planner-bar viewport-width layout assertion, driven by ?_state=venue-detail) fails identically on baseline main — NOT introduced by 9.0. Likely to show red in epic CI; track separately (candidate for Story 9.10 regression pass).
- [Phase 5 — dev-story] 9.0 used a two-component split (DefaultTimeProviders for prod + DevSearchParamTimeProviders for dev/test) so the useSearchParams reads are DCE-eligible in prod — avoids any react-hooks/rules-of-hooks disable. Convention worth reusing for other NODE_ENV-gated provider reads.

## Story 9-1-clean-app-content-sweep
- [Phase 3 — create-story] SCOPING: RouteOverlay still consumes getPredictionUncertaintyDisplay + uncertainty.* i18n keys (MapView.routeConfidenceLabel -> RouteOverlay.tsx:132-137). 9.1's AC reads like a FULL deletion of the uncertainty util/keys but a literal full delete would BREAK the route overlay. 9.1 scopes it correctly (keep util + uncertainty.levels, delete only truly-unreferenced keys) — dev-story & review must NOT over-delete.
- [Phase 3 — create-story] bistro-bakgarden.shadow_warning_minutes=0 is LIVE Supabase data (app on real data path), not just a fixture. 9.1 handles it as an idempotent live SQL UPDATE (null it) + nulling the 8-2 contract SQL so a re-seed can't reintroduce it.
- [Phase 5 — dev-story] De-bloat removal map MISSED extra consumers: ForcedVenueDetailInitialFrame.tsx + forced-venue-detail.ts (the dev-forced _state=venue-detail SSR frame the visual gate renders) also read detail.facts.*/detail.shadowWarning and set shadowWarningMinutes:45 — had to trim them too for the deleted i18n keys to be truly unreferenced. Future de-bloat stories: grep label/i18n readers BEYOND the listed components, incl. forced-state frames.
- [Phase 5 — dev-story] REMOVAL stories invert the visual gate: the reference screenshot is the OLD design, so a correct de-bloat implementation FAILS the LLM visual gate until the reference is re-baselined. Expected; the gate routes it to maintainer sign-off (dev agent is forbidden from editing references). 9.1 left venue-detail mobile+desktop refs needing re-baseline.
- [Phase 5 — dev-story] BREAKING: i18n keys removed (detail.facts.exposure/bestAt/outdoorSeats, detail.shadowWarning); component props dropped predictionUncertainty + uncertainty labels; VenueDetailContentLabels.facts now {distance} only. uncertainty.* + prediction-uncertainty-display.ts KEPT (RouteOverlay consumer). Live data: bistro-bakgarden.shadow_warning_minutes nulled.

## Story 9-2-design-system-cta-token-fix-copy-correction
- [Phase 3 — create-story] DESIGN.md (design-token source of truth) documents the BUGGY --gradient-route-button (table row + 'gold-to-dark' recipe) — the canonical doc encodes the very defect 9.2 fixes. The fix is incomplete without a same-change DESIGN.md sync. Doc-vs-code drift worth flagging at retro.
- [Phase 3 — create-story] Open Qs (non-blocking): gradient angle 169deg (route button) vs 171deg (gradient-cta-amber) — keep 169deg, only stops shift (2deg negligible); and CTA reference PNG staleness — if a reference shows the OLD olive button the visual gate FAILs by design -> route to maintainer re-baseline, do not force a pass.
- [Phase 5 — dev-story] HOST TOOLING BUG (affects ALL frontend stories incl. 9.5/9.6/9.9): .claude/scripts/visual-validate.sh screenshots via mktemp /tmp/impl-XXXXXX.png — a path the Windows-native Playwright binary CANNOT write, so the AUTOMATED visual gate always errors 'Could not screenshot dev server' on this host. Workaround: documented manual path (VISUAL_VALIDATE_PROVIDER=none ALLOW_MANUAL_VISUAL_VALIDATION=1) with byte-identical reproduction. Gate script left unmodified; maintainer should make the temp path Windows-portable.
- [Phase 5 — dev-story] Stale Turbopack CSS cache trap: a running next dev kept serving the OLD gradient after the globals.css edit; a full .next wipe + restart was needed before the corrected CSS served. Verify the served chunk before any visual capture.
- [Phase 5 — dev-story] Optional maintainer nicety: not-found.{mobile,desktop}.png + map-with-selected-venue.png references still show the OLD olive CTA, but the lenient LLM gate PASSES the gold-shade shift as minor — re-baseline is cosmetic, not required.

## Story 9-3-venue-sun-compute-performance-server-caching
- [Phase 3 — create-story] R-013 refinement: both /api/venues routes DO set Cache-Control public,max-age=30,s-maxage=30 — what defeats edge caching is the x-forwarded-for read for rate-limiting (route.ts:216-222) forcing the route dynamic. AC3 reframed so dev fixes the REAL cause (relocate rate-limiting to Edge), not the (already-present) header.
- [Phase 3 — create-story] No nextjs-app/middleware.ts exists — the recommended AC3 Option A (Edge rate-limiting for edge-cacheability) entails CREATING one. Dev must not assume an existing middleware seam. Option B (precompute pipeline) flagged disproportionate -> needs-human only if it's the sole viable path.
- [Phase 3 — create-story] 9.3 byte-identical guard: the double->single get_buildings_near_point dedupe must produce deep-equal sun outputs — a diff is a FAIL, not a rebaseline. Staleness windows agreed: sun-compute 15 min / buildings 24h / CDN s-maxage 30s.
- [Phase 5 — dev-story] LANDMINE: Next 16 renamed the Edge middleware convention middleware.ts -> proxy.ts; the build HARD-ERRORS if both exist. 9.3 assumed creating middleware.ts but had to integrate rate-limiting into the existing proxy.ts. Any future 'add Edge middleware' story must use proxy.ts, not middleware.ts.
- [Phase 5 — dev-story] Edge runtime forbids node:net (and all native Node APIs): relocating the rate-limiter from the Node route handler into the Edge proxy required swapping node:net's isIP for a pure-JS IPv4/IPv6 regex (verified by next build). Re-audit any code moved Node-route -> Edge-proxy for Node built-ins.
- [Phase 5 — dev-story] 9.3 caching = process-scoped TTL Map (NOT unstable_cache) — sanctioned open-Q fallback, lossless + fake-timer-testable, rationale in-code. Sun-compute cache caches SUCCESS only (a building-RPC-failed degraded outcome is never pinned). Byte-identical AC1 snapshot = verbatim pre-refactor main baseline; passes -> no output drift.

## Story 9-4-client-query-hygiene-time-change-debounce
- [Phase 3 — create-story] 9.4/9.3 alignment: the client time-slider step (PLANNER_STEP_MINUTES=15) == 9.3's 15-min server cache bucket, so a settled (debounced) client fetch lands on a warm server-cache boundary. Client TanStack staleTime stays 5 min (finer than the 15-min bucket) — harmless given the 15-min step, but worth a glance if a future story revisits the client window.
- [Phase 5 — dev-story] useVenueSearch is consumed by BOTH MapView (now gated: enabled + coordsSettled) AND VenueSearchShell (typed-search, intentionally LEFT ungated — debounced/user-initiated, not part of the first-paint double-fetch). Future query-gating must distinguish the two call sites; the new lastMapViewSearchCall() test helper keys off the `enabled` prop the search shell omits.
- [Phase 5 — dev-story] 9.4 decisions: AC1 derive-favourites-from-list-cache (no separate fetch), AC2 gate on settled geolocation, AC3 useDeferredValue in MapView + keepPreviousData mask. useFavouriteVenues.ts + TimeContext.tsx left untouched; staleTime/bucket/DTO/query-keys unchanged.

## Story 9-5-location-onboarding-reliability
- [Phase 3 — create-story] CROSS-STORY: the locate-button surface is split between 9.5 (BEHAVIOUR — make 'Use my location' reliable post-hydration) and 9.6 (CHROME — consolidate top-bar locate+settings, remove the duplicate floating map buttons). 9.6's create-story/dev must coordinate via 9.5's Completion Notes so they don't clobber each other's locate work.
- [Phase 3 — create-story] architecture.md lists an OnboardingScreen.tsx sibling LocationPermission.tsx that does NOT exist in the tree (permission flow lives in OnboardingScreen.tsx + useGeolocation.tsx). Planning-time placeholder — do NOT create it. Stale architecture-doc reference for the epic retro/planning-drift.
- [Phase 3 — create-story] Visual-gate blind spot: the desktop onboarding.png reference is implementation-derived (Story 1.5), so the visual gate cannot authoritatively catch desktop-onboarding regressions for 9.5 — treat as non-authoritative; also a new map-with-user-location-dot reference PNG is a maintainer rebaseline decision (dev forbidden from self-blessing).
- [Phase 4 — ATDD] HOST GATE PATTERN (all future Epic 9 ATDD stories): the repo PostToolUse hook runs tsc --noEmit + vitest run + eslint on EVERY test-file write, so a .skip scaffold that STATICALLY imports a not-yet-existing module breaks the gate at import resolution (before skip runs). Survivor pattern: load not-yet-existing modules (e.g. UserLocationLayer, useServiceWorkerUpdate) via a RUNTIME dynamic specifier inside the skipped bodies, and cast not-yet-existing props through Record<string,unknown>.
- [Phase 5 — dev-story] LEDGER CLOSURE for E8b reconcile: AC4(c)'s SW controllerchange->single-reload (refreshing latch + first-install guard) genuinely mitigates the long-standing 7.3 skipWaiting/clientsClaim cross-deploy stale-tab ChunkLoadError window. The 7.3 deferred-work entry (Target: None) can be RETIRED — flag for the epic-end deferred-reconcile.
- [Phase 5 — dev-story] Synchronous-snapshot onboarding gate (useSyncExternalStore) has two non-obvious second-order effects a reviewer must sanity-check: (1) a same-tab grant/deny flag write flips the live snapshot and would skip the exit fade -> needed a SESSION LATCH; (2) createPortal SSR-errors under jsdom renderToString once the real screen (not a placeholder) is the server output -> needed PORTAL-AFTER-MOUNT (SSR renders inline first, portals to body on mount to escape the inert shell).
- [Phase 5 — dev-story] 9.5/9.6 locate split honoured: 9.5 wired feedback on the FLOATING MapControls locate button only (aria-busy/data-locate-state/pulse); did NOT touch the top-bar surface or remove the duplicate floating pair. 9.6 owns that chrome consolidation. Also: #d97706 UserPin colour has no DESIGN.md token (raw reference value used, no new token invented) — token candidate.
- [Phase 7 — Tier-A] SPEC-VERIFY LESSON: the 9.5 file-impact map falsely claimed 'there is NO [locale]/layout.tsx' — it EXISTS and nests the onboarding gate inside [data-app-shell], which is exactly what made the inert-before-portal dead-click window REAL not cosmetic. Spec file-impact maps must be verified against the tree, not trusted. Fixed: shouldBlockAppShell = shouldShow && mounted (inert the shell only once the overlay has portalled out).
- [Phase 7 — Tier-A] The MED-1 same-commit inline-inert dead-click window is a browser paint-FRAME concern NOT synchronously observable in jsdom (React flushes the inline render + setMounted/portal re-render together in act()). The added unit test locks the portal-OUT end-state; the clean-context e2e onboarding spec (9.10) is the only true behavioural guard for the dead-click.

## Story 9-6-map-chrome-consolidation-dead-control-cleanup
- [Phase 3 — create-story] CLOBBER TRAP (9.5<->9.6): naively deleting the floating MapControls locate button would SILENTLY DROP Story 9.5's post-hydration locate-reliability wiring (aria-busy / data-locate-state=pending/fallback / pulse). 9.6 must RELOCATE that wiring onto the surviving top-bar locate surface, and keep BOTH mobile top-bar and desktop-nav My-location recentring-on-success. Front-loaded into 9.6 Task 1.
- [Phase 3 — create-story] Spec drift (planning-drift candidate): the floating settings gear over the map was never in ux-design-specification.md (mobile map-controls = Zoom+/Zoom-/My-location only). The maintainer 'remove floating settings, enable top-bar gear' decision realigns code to the design authority — the code had drifted from spec, not a net-new design call.
- [Phase 5 — dev-story] Clobber guard HELD: 9.5's locate-reliability wiring (data-locate-state/aria-busy/pulse, clickable on fallback) was RELOCATED onto VenueSearchShell's mobile top-bar Navigation button, not deleted. Success fly-to kept in MapControls (still mounts for the zoom stack); mobile top-bar locate + desktop-nav-my-location share one useGeolocation context so both recenter on success.
- [Phase 5 — dev-story] map-primary rebaseline CAVEAT: the _state=map-primary forced-reference route SUPPRESSES the mobile VenueSearchShell top-bar (isForcedVisualReference), so the reference PNG never showed the surviving top-bar pair. The maintainer rebaseline must capture the REAL returning-user render, not the _state-forced one.
- [Phase 5 — dev-story] AC4 Enter-to-select was ALREADY satisfied by cmdk 1.1.1 (bare Enter selects the first item, verified empirically) — no production onKeyDown added, just a documenting regression test. Removing the pager chevrons orphaned common.json nav.previous/nav.next beyond the named venue.controls.* scope; removed both (parity-guarded).

## Story 9-7-tag-filtering-real-data-working-chips
- [Phase 3 — create-story] DATA PREREQ RESOLVED (no halt): live public.venues has NO tags column and holds only the 7 test/fixture venues (no production data, no data-loss). Real tag VALUES are sourced DETERMINISTICALLY from the per-slug arrays already in venue-visual-metadata.ts (the placeholder being REPLACED) + the Claude Design reference — migrating existing values into a real additive `tags text[] not null default {}` contract, not fabricating. Story forbids inventing tags beyond the 7.
- [Phase 3 — create-story] PLANNING-DRIFT (retro): ux-design-specification.md has only a generic 'filter' icon — NO dedicated tag-chip filter spec. The chip filter is design-REFERENCE-driven (Claude Design src-desktop/TopBar.jsx), not UX-spec-driven; and the reference's own App does NOT cross-filter its list/pins (chip state local to TopBar), so list+pin cross-filtering is NET-NEW wiring, not reference-proven.
- [Phase 3 — create-story] ARCHITECTURE: the chip UI (DesktopNavBar via ResponsiveLayout) and the venue surfaces (MapView as children) are in SEPARATE subtrees joined only at AppContextProviders — that is why AC3 requires a shared TagFilterContext; lifting state into MapView would NOT reach the nav.
- [Phase 4 — ATDD] MULTI-CHIP SEMANTICS = OR/UNION (venue matches ANY active tag, .some()), per story AC3 + the Claude Design reference TopBar.jsx — NOT AND/intersect. (An orchestrator ATDD prompt mistakenly said 'AND'; the ATDD agent correctly followed the authoritative story spec and wrote OR/union red tests.) Dev-story MUST implement OR/union; an active chip shows an 'on' pill (aria-pressed + bg-text-primary text-white).
- [Phase 5 — dev-story] INFRA: the Supabase MCP tools (apply_migration/execute_sql) were NOT exposed as callable functions in the auto-bmad delegate environment this session (the supabase MCP servers disconnected mid-run); the LIVE tags migration + 7-row seed went through the documented IPv4 session-pooler psql path instead (verified: tags column present, 7 venues intact, byte-matched seed, no data loss). If MCP is meant to be the standard live-DB path for delegates, its availability needs confirming.
- [Phase 5 — dev-story] BLAST-RADIUS: adding a REQUIRED tags: string[] to VenueDataDto forced tags onto ~15 unrelated test fixtures + 2 production synthetic-venue factories — wide-but-shallow. Anticipate this for any future required-DTO-field addition (a good compile-time guardrail but a broad edit surface).
- [Phase 5 — dev-story] 9.7 done: real tags migrated deterministically from the venue-visual-metadata.ts placeholder arrays into the additive live contract; OR/union chip filtering of BOTH list + pins via a shared TagFilterContext (chip UI + venue surfaces are separate subtrees); active chip = #1b1b1e/text-primary on-pill; graceful-empty; dead nav.filterChips.* keys removed.
- [Phase 7 — Tier-A] DE-DUPE INVARIANT (retro): the two useVenueSearch call sites (nav chips in DesktopNavBar + MapView list/pins) MUST have byte-identical query keys (same SEARCH_RADIUS_KM, coords/status, useDeferredValue(plannerQuery), coordsSettled gate) or TanStack won't collapse them into one request — 9.7 initially left the nav un-deferred+un-gated, a real fetch-hygiene regression vs 9.4. Fixed + locked with a de-dupe-invariant test. Future stories touching EITHER call site must assert key parity.

## Story 9-8-venue-sharing-real
- [Phase 3 — create-story] LEDGER RETARGET (E8b reconcile candidate): the deferred-work.md item 'Share action behaviour' still targets the CUT Story 6.4 (Epics 4/5/6 were dropped at the Epic-7 retro). Epic 9 reallocated share scope to 9.8 without updating that target. Its intent is fulfilled by 9.8 -> the epic-end deferred-reconcile should mark/retarget it 6.4->9.8 so the queue stays truthful.
- [Phase 3 — create-story] NO-DEAD-TILES PRINCIPLE (the epic's raison d'être): the reference ShareModal.jsx brand tiles Instagram/Snapchat have NO public web share-intent. Shipping them as silently-dead tiles would REPRODUCE the exact 'dead control' defect this epic exists to remove. 9.8 default: wire only real web-share-intent tiles (WhatsApp/Facebook/X/mailto) + copy-link; omit or copy-link the no-intent ones; NEVER a silently-dead tile.
- [Phase 3 — create-story] Share deep-link = the EXISTING ?venue=<slug> query param (built as ${origin}${pathname}?venue=<slug>, usePathname preserves the as-needed locale segment) — NOT a new /v/<slug> route (there is no page route, only app/api/venues/[slug]). AC3 is already satisfied by MapView's venueSlugParam/venueMatchesSlug/fallbackVenueFromSlug chain -> share is a regression test, not re-architecture.
- [Phase 5 — dev-story] next-intl usePathname() strips the locale prefix — build share URLs from raw window.location (origin+pathname+search) to preserve /en, not next-intl's pathname.
- [Phase 5 — dev-story] The routing-boundary scan test greps literal window.open( / maps.apple / google.com/maps / geo: even inside comments — scanned files must avoid those literals in prose.
- [Phase 5 — dev-story] Design system has no success/green token; the copy-confirmation fell back to surface-sand/amber-dark + Check icon. A --color-status-success token would let confirmations match the reference.

## Story 9-9-mobile-venue-quick-info-card-rework
- [Phase 3 — create-story] Ledger doc-path drift: the 9.5 deferred note points at components/custom/venue/VenueQuickInfo.tsx but the component actually lives at components/composed/venue/VenueQuickInfo.tsx — verify file-impact maps against the tree, not the doc.
- [Phase 3 — create-story] Story 9.9 is pure layout/positioning polish — AC1's 'content removals' were already done by Story 9.1 on this branch; the 9.10 verification pass + retro must NOT double-count removal work.
- [Phase 3 — create-story] Story 9.5's honest-distance-label defer (Target:9.9) is folded into 9.9 Task 3 — E8b reconcile/archive should mark it resolved once 9.9 lands (queue-not-archive: its ledger entry leaves on completion).
- [Phase 5 — dev-story] 9.9 mobile quick-info clearance is now DERIVED from planner-panel geometry (MOBILE_PLANNER_* constants in MapView, ~211px bottom) not a magic TOP_CLEARANCE — keep in sync if TimeSliderPanel's mobile offset/padding changes (comment added).
- [Phase 6 — automate] MapView component tests are constrained by the shared map mock (hardcoded 390x700 canvas collapses maxY->minY) + jsdom's missing matchMedia (always resolves the mobile branch) — desktop-branch/variable-canvas assertions need a shared-mock rewrite; out of scope for presentational coverage.

## Story 9-10-mobile-device-verification-pass-regression-guards
- [Phase 3 — create-story] Epic-9's mobile 'verification' surface is thinner than it reads — the `mobile` Playwright project already runs every non-axe spec at iPhone 14 and 4/5 of 9.10's AC2 guards already exist; real net-new is one clean-URL-date guard + a consolidated mobile e2e spec + the MapLibre null-coordinate warning hunt.
- [Phase 3 — create-story] EPIC CLOSE-OUT BLOCKER: the stale reference-PNG rebaseline cascade (map-with-selected-venue / venue-detail / map-primary + missing share-modal & location-dot refs) has compounded across 9.1/9.5/9.6/9.7/9.8/9.9 — ONE maintainer rebaseline action; Epic-9 cannot truthfully claim a green visual gate until it lands.
- [Phase 5 — dev-story] Task 3 surfaced a GENUINE latent bug in prior-story code: the ?venue=<slug> deep-link (MapView.tsx:604) selects a venue with no location check, and the QuickInfo project([lng,lat]) call lacked the finiteness guard its sibling easeTo has -> 3x MapLibre 'found null' warning. Now FIXED with hasValidVenueLocation, not just guarded-in-test.
- [Phase 5 — dev-story] Mobile-emulation gap: getCurrentPosition triggered by a CLICK (onboarding CTA) does not resolve under Playwright's iPhone-14 profile, while the silent auto-acquire path does — the dot e2e must use the returning-user auto-acquire path. Worth knowing for future mobile geolocation e2e.
- [Phase 5 — dev-story] Local Next dev-server lock is per-project-dir (a 2nd `next dev` defers to :3100 and exits); Playwright's webServer auto-start timed out, so the mobile pass needs ONE manually-managed server on :3000.
