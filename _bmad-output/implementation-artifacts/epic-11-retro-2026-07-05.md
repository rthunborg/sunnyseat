# Epic 11 Retrospective — "Feels Instant, Reads Clear"

**Date:** 2026-07-05
**Facilitator:** Bob (Scrum Master)
**Project Lead:** Rasmus (sole participant — solo party-mode; all roles below answered from the accumulated `_bmad-output/auto-bmad/retro-notes/epic-11.md` phase notes, the 8 story files' Dev Agent Records/Completion Notes/Review Findings, and `sprint-status.yaml`, not a live multi-user session)
**Epic status:** 8/8 stories at `review` (all code + tests landed on one epic branch, not yet merged) · **Retrospective:** done
**Previous retro:** Epic 10 ("Honest Sky", 2026-07-03) — merged to main via PR #16 (`3911539`); close-out caveats resolved same day (live CHECK migration applied, AC2 spot-check AGREE, PNGs rebaselined, FR12 cap decision recorded, `windowLabelTier` fix).

---

## 1. Epic Summary & Metrics

| Metric | Value |
|---|---|
| Stories planned at epic start | 8 (11.1–11.8), authored 2026-07-04 from a maintainer live-field-test workshop at sunnyseat.vercel.app (post-Epic-10) that traced 5+ concrete UI/perf defects to code root causes |
| Stories delivered | 8/8 at `review` (100% of planned scope; zero insertions, zero cuts) — all landed on a single epic branch (`epic/11-feels-instant-reads-clear`) in one unattended auto-bmad epic run |
| Root causes targeted | Decorative `TimeSlider` thumb eating pointer events; time-in-the-query-key per-step commits (the Epic 9/10 "dampened not removed" stall); the list route discarding the engine's day walk; `MapContainer`'s heavy overlay dimming; no mobile tag-filter UI; the venue-detail fallback-first malformed render; three-epics-deferred build/config hygiene debt (`vercel.json`, `.gitattributes`) |
| Sequencing | 11.1 (day-series foundation, unblocks zero-fetch scrub) → 11.2 (slider fix rides the 11.1 commit seam) → 11.3/11.4/11.5/11.6 (surface reworks; 11.4 needed 11.1's DTO touch-point) → 11.7 (hygiene + consolidated maintainer PNG rebaseline, deliberately AFTER all visual changes land) → 11.8 (live + real-device verification, last) |
| Final test state | 143 files / 1361 vitest tests, 0 skipped, monotonic growth from an 1120/30-skipped baseline at 11.1 start (all 6 epic-provided red-phase ATDD scaffolds un-skipped + green across the epic, none dropped) |
| New e2e specs | `epic-11-scrub-zero-fetch` (11.1), `epic-11-slider-touch-drag` (11.2, real-touch `touch` project), `epic-11-sheet-touch-gestures` + `epic-11-chip-filter-parity` (11.3), plus a new Chromium `touch` (Pixel-5, CDP raw-touch) Playwright project wired into CI |
| Trace-gate verdict | PASS — P0 100% |
| NFR-gate verdict | PASS |
| Test-review grade | 92/100 (Grade A) |
| Tier-B integration review | 2 iterations; 48 lens delegates + 7 follow-up delegates (~350 raw findings) triaged down to 14 survivors + 2 additional in the second pass; 6 genuine cross-story issues fixed in-run (not deferred) |
| Code-review outcome | Every story cleared its own Tier-A review (mostly Approve-with-Lows; 11.7 had one High auto-decision — a byte-identical reference-PNG pair — resolved in-run by re-capture, see below) |
| PR shipping state | **DRAFT** — the 11.7 High auto-decision (re-capture the accidentally-identical `map-primary`/`map-panel-venues` mobile PNG pair) requires the maintainer's rebaseline blessing before the PR can go ready-for-review; this is a structural gate (dev agents are forbidden from self-blessing reference PNGs), not an unresolved defect |

**Delivered capability:** the core "when is it sunny where" loop now feels instant and mobile-first-class. Time-scrubbing derives every per-time UI value (marker %, pin state, quick-info figures, list ordering, obscured presentation) client-side from a cached per-venue day-series and issues **zero** network requests; a date change fires exactly **one** request behind a dim+spinner overlay with markers staying mounted (11.1). The time-slider thumb is now reliably grabbable on touch (a `pointer-events` bug was silently eating drags), decoupled from the app-level commit so a drag tracks the finger 1:1 and commits once on release, and the planner is properly range-constrained (today→today+3, today-minimum = live wall clock) (11.2). Mobile gained first-class tag filtering (a chip row sharing the same context/data source as desktop) and a fourth handle-only collapsed sheet snap with a retuned, jank-free `@use-gesture` drag (a real gesture-physics bug — the release direction reading 0 at touch-up — was the actual root cause of "the sheet feels janky") (11.3). The venue quick-info card was reworked to match the reference: confidence clutter and a redundant sun-window line removed, real opening hours rendered in their place, the route CTA simplified to "VISA RUTT" (11.4). The map's oppressive amber wash was cut to roughly a quarter strength, the location dot became larger with a pulsing halo, and recenter is now viewport-aware (accounting for the bottom sheet/side panels) (11.5). The venue detail view no longer flashes a malformed fallback frame, the redundant "Soltider idag" timeline section was removed (the planner is the one way to explore times), and the reviews section is centered with a single empty-state message (11.6). Three epics of deferred build/config debt were finally closed: the Vercel install command no longer silently swallows a broken `lightningcss` install, `.gitattributes` now enforces LF on source files (ending the EOL-churn tax that cost Epic 10 a review round), and the orphaned `toSunStatusToken` mapper was deleted (11.7). Finally, the interaction and perf wins were promoted from story-local "seam-provable" specs to the epic's standing CI gate — the request-count invariant (scrub=0, date-change=1), real-touch gesture coverage under a dedicated `touch` Playwright project, and a consolidated maintainer post-merge protocol (live p95, physical-device checklist, PNG-blessing) rather than three separately-re-deferred handoffs (11.8). **Not yet merged to main** — the epic PR ships as a **draft** pending the maintainer's rebaseline blessing (structural, per the dev-forbidden-to-self-bless rule), plus the standard post-merge live p95 + device-sweep protocol.

---

## 2. What Went Well

- **The epic thesis — "REMOVE the fetch, don't dampen it" — was stated once (11.1) and then genuinely held for eight stories.** Epics 9 and 10 had each landed a real caching/debounce win (the sun-engine cache, `useDeferredValue`) yet the user-visible stall survived because time stayed in the query key. This epic named that pattern explicitly as its own root cause and built the fix at the actual seam (decouple time from the query key entirely, derive everything client-side from a cached day-series) rather than adding another layer of damping. Every later story (11.2's slider commit, 11.3's chip filter, 11.4's per-time quick-info fields) explicitly checked "does this reintroduce time into the key?" and the answer stayed no all the way through 11.8's final confirmation.
- **A real gesture-physics bug was found and root-caused, not papered over, in two independent places.** 11.2 discovered `@use-gesture`'s `direction` reads 0 at pointer/touch-up, so a release-direction decision based on instantaneous direction silently failed; 11.3 hit the *same* bug independently in the sheet's four-snap logic and applied the same fix (derive release direction from accumulated movement sign). Both diagnoses happened via real-touch e2e (CDP `Input.dispatchTouchEvent`), which is exactly the class of defect an emulated mouse-drag would have missed — the epic's own thesis about visual/interaction gates justified itself in-run.
- **The "no-fabrication" discipline held across every content and data change.** 11.4's opening-hours field renders nothing when absent rather than a fabricated fallback; 11.6 replaced a `?? '22:00'` stand-in badge with an honest omit-when-absent badge (closing the exact class of bug the epic was hunting); 11.1's day-series never invents a value outside the parity guardrail. This is the same principle that shaped Epic 10's "unknown ≠ 0" discipline, now applied to UI content rather than weather signals.
- **Three epics of genuinely-scheduled-but-never-executed hygiene debt were finally closed in one story (11.7) without becoming a scope-creep magnet.** The `vercel.json` lightningcss swallow and the `.gitattributes` EOL-normalization gap had been carried from Epic 8 through Epic 10 (three retro action items, re-deferred each time); 11.7 fixed both with static proof (an injected `ETARGET` failure demonstrating the old chain swallowed it and the new one doesn't) rather than requiring a live deploy, and scoped the EOL fix narrowly (source extensions only, explicit `.log`/binary exclusions, renormalization kept in its own commit) rather than a destructive blanket sweep.
- **The consolidated reference-PNG rebaseline (deferred across Epics 9, 10, and every Epic-11 story) was finally executed as a single, well-organized pass (11.7) instead of continuing to compound.** 12 screen pairs were captured, verified in-DOM before staging, and documented in one `REBASELINE-LOG.md` entry — and when the Tier-A review caught a genuine defect in that capture (two screens staged byte-identical), it was root-caused and fixed in the same story rather than shipped as "close enough."
- **The Tier-B integration review caught and fixed real cross-story issues that no single story's own review could have found**, including a stale CI-invariant test (a confidence-copy regex in an *Epic 10* e2e spec that 11.4's own review missed because it only ran vitest, not that particular e2e file) and a flaky real-touch tap that was silently masked by `retries:2` in CI. Both were root-caused and hardened in-run by 11.8 rather than left as "known flaky."
- **The epic consistently distinguished "dev-blessable" from "maintainer-blessable" evidence and never blurred the line**, even under pressure to look "more done": every new visual state across 11.1–11.6 was explicitly handed to 11.7's consolidated rebaseline rather than self-blessed piecemeal, and 11.8's three remaining live/device/PNG-blessing items were consolidated into one clearly-labeled post-merge protocol instead of three separate vague TODOs.

## 3. What Was Hard

- **A three-way (dev-notes vs. actual code vs. reference-design) mismatch surfaced in 11.6's "keep the peak/best-window helpers" guidance.** The story instructions assumed the peak-time subtitle lived in the detail header and should survive the "Soltider idag" section removal; the actual code had it structurally inside the removed section, and the reference design has no such subtitle at all. The dev agent correctly treated the verbatim AC text + reference design as authoritative over the story's own narrative guidance and pruned the dead helpers — a defensible call, but it is the second epic in a row (after Epic 10's code-location misreads) where a story's prose diverged from the code it described at the line-number level, which is a signal that dev-notes line-number references need a freshness check closer to drafting time.
- **A stale cross-epic test was found only by a full-suite sweep, not by any per-story review.** 11.4 removed the visible "Säkerhet: NN%" confidence chip from the quick-info card, which broke an *Epic 10* e2e assertion (`epic-10-weather-matrix.spec.ts`'s `CONFIDENCE_BADGE_COPY` regex) that 11.4's own code review never ran (it validated with vitest only). This is exactly the "shipped-but-insufficient" pattern the epic exists to kill, just at the meta-level of test suites instead of product code — a regex tied to a specific rendered string silently went stale the moment the string moved to sr-only-only, and nothing caught it until 11.8's mandated full e2e sweep.
- **The AC3 "centered per the reference" instruction for the Omdömen section directly contradicted the literal reference JSX** (which is left-aligned), and the dev correctly prioritized the maintainer's explicit prose over the stale reference file — but this means the reference design asset itself is now known-stale for that one section, a discrepancy that only gets reconciled at the 11.7 rebaseline (i.e., a design-asset drift that traveled two stories before anyone with rebaseline authority looked at it).
- **A genuinely serious defect (a byte-identical reference-PNG pair) was carried forward silently across the *previous* commit before this epic's own review caught it.** 11.7's Tier-A review found `mobile/map-primary.png` and `mobile/map-panel-venues.png` staged as literally the same file (same git blob, same md5) — and confirmed the *prior committed* pair was also identical, meaning this specific defect had been sitting in the reference set undetected since before Epic 11 touched it. The root cause (both routes default to the sheet's `mid` snap state, so a naive capture without a state assertion produces the same pixels) is now fixed with a distinctness check, but the fact it survived at least one prior rebaseline pass is a sobering data point about how easy it is for a capture recipe's "individually-in-state" check to miss "collectively-indistinguishable."
- **The epic's touch-gesture testing infrastructure had to be built essentially from scratch under real time pressure**, and even after landing, needed a second hardening pass: 11.2 added the `touch` Playwright project and wired it into CI only after a Tier-A review caught "added a project, forgot the CI wiring" (a pattern that had already bitten in Epic 10's context per the retro-notes); then 11.8's own mandated sweep found a *second* instance of exactly the same failure mode two stories later — a genuinely flaky real-touch tap that only stayed "green" in CI because of a blanket `retries:2`, which is precisely the kind of fragility the epic's anti-"shipped-but-insufficient" thesis was built to surface, and it took the epic's own final verification story to catch it in its own infrastructure.

## 4. Key Insights

1. **Naming the anti-pattern explicitly at epic kickoff ("REMOVE the fetch, don't dampen it") and repeating it in every subsequent story's Dev Notes was the single highest-leverage move of the epic.** Unlike Epic 10's two-signal invariant (which needed re-derivation each story), this epic's thesis was restated verbatim story-to-story from the retro-notes, which meant every dev pass could self-check against a fixed, unambiguous bar ("does a settled scrub still fetch? yes = FAIL") instead of a vaguer "make it faster" goal that a caching layer could satisfy without fixing the root cause.
2. **A full-suite / cross-epic sweep is necessary, not optional, before calling a UI-removal story done — and it must include e2e, not just vitest.** Two separate epic-11 stories (11.4 removing the visible confidence line; 11.5 clashing with 11.4's already-removed ETA label) produced stale assertions in *other* epics' or *other* stories' e2e specs that no per-story review surfaced, because per-story review ran only the fast unit/component suite. The lesson from Epic 10's retro ("run the full e2e suite, not just the one you touched") was explicitly re-learned here at a slightly different failure mode (removed a rendered string → stale regex in an unrelated epic's spec) and this time was caught only because 11.8 mandated a full sweep as its own deliverable, not as an incidental side effect.
3. **A capture/verification recipe that checks "is this screen correctly in-state" is not the same guarantee as "are these two screens distinguishable from each other" — both checks are needed for any reference-PNG rebaseline.** The byte-identical `map-primary`/`map-panel-venues` defect passed an individual-state check (each route rendered its own correct DOM state) but failed a pairwise-distinctness check nobody was running, and it had silently survived at least one prior rebaseline. Future rebaseline passes (in this project or any project with a reference-PNG gate) should assert pairwise distinctness across the full staged set, not just per-screen correctness.
4. **Real-device/real-touch testing infrastructure needs its own "is this actually wired in and not flaky" audit as a standing practice, because it fails silently in two different ways.** It can fail by omission (a new Playwright project never gets invoked by CI, so a hard gate is dormant-green — caught twice, once in 11.2 by its own review, once again structurally in 11.8 confirming the fix held) or by false-positive masking (a flaky assertion stays "green" in CI only because of a blanket retry count, hiding the exact fragility a feature was built to fix — caught once, in 11.8's own infrastructure, by the epic's own final verification pass). Both failure modes are invisible to a single green CI run; only a dedicated "audit the gate's own wiring" pass (which 11.8 explicitly performed, and which now has its own config-contract vitest guards) catches them.
5. **A story's Dev Notes narrative guidance (line numbers, "keep X because Y") can go stale relative to the actual code by the time dev-story executes, and the correct resolution order is: verbatim AC text and the reference design outrank the story's own prose.** This happened twice in this epic (11.6's peak/best-window helper guidance; 11.1's touch-project reason strings later found stale in 11.8) and both times the dev agent correctly treated the AC/reference as authoritative rather than mechanically following stale prose — but it argues for tightening the create-story practice of re-verifying line-number references immediately before handoff, especially for stories drafted several stories deep into a sequential epic where prior stories have already reshaped the files being described.

## 5. Previous Retro (Epic 10) Follow-Through

| Epic 10 item | Status | Evidence |
|---|---|---|
| A1 — Apply the CloudObscured feedback DB CHECK-constraint migration | ✅ Done | Applied to live Supabase 2026-07-03, recorded in the epic-10 close-out commit (`c10d75a`) before Epic 11 was authored |
| A2 — Run the consolidated reference-PNG rebaseline | ✅ Done (this epic) | Epic 10's PNGs were rebaselined 2026-07-03 at its own close-out; the FURTHER-compounded rebaseline (Epic 9 + Epic 10 + every Epic-11 surface) was finally executed by Story 11.7 (12 pairs, staged, documented, blessing pending at PR) |
| A3 — Run the AC2 live-reality weather spot-check | ✅ Done | Recorded AGREE at the epic-10 close-out (2026-07-03) |
| A4 — Decide FR12 cloud-confidence-vs-coverage-cap product question | ✅ Done | Decision recorded at epic-10 close-out |
| A5 — Batch-flip sprint-status.yaml Epic-10 statuses to `done` | ✅ Done | Recorded in the epic-10 close-out commit |
| A6 — Root-cause the `sun-engine.test.ts` concurrent-load timeout flake | ⏳ Not addressed in Epic 11 | Not raised in any epic-11 retro-note or story file; still open, now carried across three epics (10→11) without an owner |
| A7 — Escalate Epic 8's A2/A3 (vercel.json swallow, `.gitattributes` EOL) to an explicitly scheduled item | ✅ Done | Story 11.7 finally closed BOTH — the exact item this action demanded, after three epics of re-deferral |
| A8 — Extend the union-extension create-story checklist to name the validation-layer sweep explicitly | ⏳ Not verified | No DTO-union extension occurred in Epic 11 to test whether the checklist update was actually applied; cannot confirm either way from this epic's evidence |

**New pattern vs Epic 9/10:** the "dev agent forbidden from self-blessing visual/live evidence" discipline, established in Epic 9 and generalized in Epic 10 (live weather spot-check), extended cleanly to a THIRD category in Epic 11 — hygiene/build-config proof (11.7's fail-loud verification via *injected* errors rather than a live deploy) — and to a fourth: the *consolidation* of what had been three separately-deferred maintainer handoffs (live p95, device checklist, PNG blessing) into one clearly-labeled protocol (11.8) rather than continuing to defer them as three loose threads. This is a maturation of the pattern, not just a repetition of it.

## 6. Next Epic Preview

No Epic 12 is authored in `epics.md` yet — Epic 11 is the last epic currently defined. This retrospective therefore closes with readiness assessment + carried debt rather than a next-epic dependency walkthrough.

**Carried forward for whenever Epic 12 (or a maintenance pass) is scoped:**
- **Maintainer blessing of the Story-11.7-staged 12-pair reference-PNG rebaseline** — the single blocking item keeping the epic PR in draft. Includes the re-captured, now-verified-distinct `map-primary`/`map-panel-venues` mobile pair.
- **Live date-change p95 (<3s target, <1.5s stretch) + time-scrub-zero-requests confirmation** against the already-deployed production, per the consolidated protocol in Story 11.8's Dev Agent Record.
- **Physical real-device checklist** over every Epic-11 surface (slider drag, sheet 4-snap + chips, quick-info states, map tint/dot/recenter, detail first paint) — the Playwright `iPhone 14`/`Pixel 5` profiles served as the automated proxy this run; no physical phone was available.
- The `sun-engine.test.ts` concurrent-load timeout flake (Epic 10's A6) — now carried across two epics (10→11) unaddressed, still worth a dedicated root-cause investigation before it starts failing CI for real.
- The fully-orphaned `windowLabelTier`/`isSunWindowStatus`/`WindowLabelTier` exports (11.7 deviation) — kept only because deleting them would remove the sole surviving `never`-exhaustiveness compile guard for `VenueSunStatus`; flagged as a future hygiene-pass candidate (re-home the guard or accept its removal) rather than resolved here.
- Several Low-severity deferred items carried from individual story reviews: 11.2's keyboard-during-drag stale `dragValue` edge case; 11.3's desktop-chip-overflow real-browser (non-jsdom) proof gap; 11.5's `OnboardingGate` grant-flyTo not made viewport-aware (a secondary, less-critical recenter site) and its hand-picked `MOBILE_TOP_BAR_COVER` constant with no mirrored CSS token.
- The `a11y-mobile` Playwright project remains defined but not CI-invoked (carries a Story-5.1 `test.fixme`) — a deliberate, repeatedly-reaffirmed non-decision, not new debt, but still open.

## 7. Action Items

| # | Action | Owner | Notes |
|---|---|---|---|
| A1 | **Bless the Story-11.7-staged consolidated reference-PNG rebaseline** (12 pairs, incl. the corrected byte-distinct `map-primary`/`map-panel-venues` mobile pair) at PR review. | Rasmus (maintainer) | Structural gate — dev agents cannot self-bless. This is the reason the epic PR ships as a draft. Highest-priority item. |
| A2 | **Run the live date-change p95 + time-scrub-zero-requests measurement** against production using the protocol consolidated in Story 11.8's Dev Agent Record. | Rasmus (maintainer) | Wall-clock/live-only; not CI-gatable. Triage any p95 miss to a root cause rather than accepting it. |
| A3 | **Run the physical real-device checklist** (slider drag, sheet snaps + chips, quick-info, map tint/dot/recenter, detail first paint) on a real phone. | Rasmus (maintainer) | The Playwright mobile/touch profiles served as this run's automated proxy; no "device passed" claim has been fabricated. |
| A4 | **Batch-flip sprint-status.yaml's 8 Epic-11 story keys (+ epic-11) from `review`/`in-progress` to `done`** once merge lands. | orchestrator / SM | Standard epic-boundary step. |
| A5 | **Root-cause the `sun-engine.test.ts` concurrent-load timeout flake**, now carried unaddressed across Epic 10 AND Epic 11. | dev | Isolated runs pass; only fails under full-suite concurrent load. Two epics of re-deferral is itself the signal. |
| A6 | **Decide the fate of the now-fully-orphaned `windowLabelTier`/`isSunWindowStatus`/`WindowLabelTier` exports** (kept in 11.7 only to preserve a compile-time `never`-exhaustiveness guard). | dev / SM | Either re-home the guard onto a genuinely-consumed switch or explicitly accept removing it; do not let it linger as a second "orphaned mapper" story. |
| A7 | **Tighten create-story practice to re-verify Dev-Notes line-number references immediately before handoff**, especially for stories drafted deep into a sequential epic where earlier stories have already reshaped the referenced files. | SM (create-story) | Two epic-11 stories (11.1's touch-project reason strings, 11.6's peak/best-window guidance) had stale line-number/narrative references caught only by the dev agent correctly prioritizing verbatim AC/reference-design text. |
| A8 | **Adopt a pairwise-distinctness check for any future reference-PNG rebaseline pass**, not just a per-screen in-state check. | SM / dev | The byte-identical `map-primary`/`map-panel-venues` defect passed every individual-state check and had silently survived at least one prior rebaseline before 11.7's own review caught it. |

## 8. Readiness Assessment

- **Testing/quality:** Strong — 143/1361 vitest all green, monotonic growth, 0 dropped tests across the epic; trace gate PASS P0 100%; NFR PASS; test-review 92/100 Grade A; Tier-B integration review ran two full iterations and fixed 6 genuine cross-story issues in-run. ✅
- **Deployment:** Not deployed. All 8 stories sit at `review` on one epic branch; the PR ships as a **draft**, explicitly pending the maintainer's reference-PNG blessing (A1) — a structural gate, not an unresolved defect. ⚠️ (tracked, expected)
- **Visual gate:** The automated `visual-validate.sh` tooling remains broken on this Windows host (carried gotcha since Epic 9); every new visual state across 11.1–11.6 was correctly withheld from self-blessing and consolidated into 11.7's rebaseline, which itself found and fixed one genuine defect (the byte-identical PNG pair) before staging. Mitigated by code-level assertions + axe AA scans throughout, but the maintainer blessing (A1) is the load-bearing remaining step. ⚠️ (tracked, A1)
- **Functional completeness:** Complete at the story level — all 3 ACs per story satisfied, all Design Gate criteria met or honestly reflected (11.7/11.8's non-standard hygiene/verification gates were explicitly and correctly NOT forced into the standard four-criteria shape). No known functional gap comparable to Epic 10's live-DB-constraint miss. ✅
- **Technical health:** Strong and improving — three epics of deferred build/config debt finally closed (11.7); the epic's own standing CI gate (request-count invariant + real-touch coverage) is now locked by config-contract tests that guard the gate's own wiring, directly addressing the "silent CI-wiring gap" failure mode that had bitten twice within this epic alone. One carried debt (the `sun-engine.test.ts` flake) now spans two epics unaddressed. ✅ with one flagged carry-forward.
- **Stakeholder acceptance:** Rasmus is both the sole stakeholder and the sole retro participant this session — no external acceptance gap to track. ✅ (self-consistent)
- **Unresolved blockers:** None that block calling the story-level work complete. The reference-PNG blessing (A1) blocks a fully truthful *merged/visual-gate-passed* claim, not the underlying code or test correctness — mirroring the Epic-9/10 pattern exactly, now applied a third time with the added maturity of the byte-identical-pair defect having been caught and fixed rather than shipped.

**Verdict:** Epic 11 is complete and solid from a story perspective — all 8 stories landed their ACs, the epic's own anti-"shipped-but-insufficient" thesis held under its own scrutiny (twice catching its own testing-infrastructure failures: a dormant CI-wiring gap and a retry-masked flake), three epics of deferred hygiene debt were finally closed, and the Tier-B integration review found and fixed 6 genuine cross-story issues rather than deferring them. The material carry-forward is the maintainer's reference-PNG blessing (A1, the reason the PR is a draft) plus the standard live-p95/device-sweep post-merge protocol (A2/A3) — none of which reflect incorrect code, all of which are exactly the kind of honest, bounded follow-up list a "no self-blessing, no fabrication" epic is supposed to produce.

## 9. Next Steps

1. **Orchestrator:** proceed with the epic-boundary git/PR flow — open the epic-11 PR as a **draft** (per the 11.7 High auto-decision), noting the reference-PNG blessing as the reason.
2. **Maintainer (Rasmus):** bless the 11.7-staged reference-PNG rebaseline (A1, blocking); run the live p95 measurement (A2) and the physical-device checklist (A3) per the Story-11.8 consolidated protocol, on convenience.
3. **SM:** batch-flip sprint-status.yaml's Epic-11 story statuses to `done` (A4) once the PR merges.
4. **Dev (next-touch):** root-cause the `sun-engine.test.ts` concurrency flake (A5, now two epics old); resolve the fully-orphaned `windowLabelTier` sibling exports (A6); fold the pairwise-distinctness rebaseline check (A8) into whatever future capture tooling is used.
5. **SM (create-story practice):** tighten line-number/narrative-reference freshness checks for stories drafted deep into a sequential epic (A7).
6. No Epic 12 is defined yet — the next planning session should scope it explicitly rather than assume Epic 11's carried debt auto-schedules itself.
