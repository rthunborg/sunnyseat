# Story 11.4: Venue Quick-Info Rework — Reference Alignment & Truthful Content

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **user**,
I want the venue quick-info card to look polished and show me what I actually need (open now? how far? how sunny?),
so that the most-used surface earns trust at a glance.

## Acceptance Criteria

**AC1 — Remove confidence + sun-window text, render real opening hours in their place (requires `openingHours` on the list DTO)**

**Given** the quick-info card (`VenueQuickInfo.tsx`) on mobile and desktop
**When** the content is reworked
**Then** the "Säkerhet: NN%" text is removed on both breakpoints (confidence remains available in the detail view; the sr-only accessible confidence text may remain), the "Sol HH:mm–HH:mm" window line is removed, and real opening hours render in their place (e.g. "Öppet till 22:00") — which requires surfacing `openingHours` on the list DTO (`VenueDataDto`) from the existing `venues.opening_hours` column; venues without opening-hours data show nothing (never a fabricated value)

**AC2 — Route CTA reads only "VISA RUTT" (drop the truncated ETA from the compact/quick-info button)**

**Given** the route CTA currently squeezes a truncated ETA ("ca 16…") inside the button
**When** `RouteButton` consumers stop passing `estimateLabel` into the compact/quick-info button (the ETA may live on in the detail/route surface if already present)
**Then** the button reads only "VISA RUTT" (+ icon) at full legibility on both breakpoints, with no truncated text at any common width

**AC3 — Mobile card layout re-aligned to the reference `QuickInfo.jsx` across all four sun states**

**Given** the mobile card layout drifted from the reference
**When** it is aligned to the reference `QuickInfo.jsx` (spacing, type hierarchy, badge placement, CTA row) incorporating the removals above
**Then** the layout holds without overflow across sun states (full sun / partial / shaded / obscured "Sol bakom moln") and common mobile widths, and the obscured two-signal treatment (Story 10.2) is preserved

**AC4 — Regenerated aria output + pruned unused i18n keys**

**Given** the card's accessible name embeds the removed content
**When** the rework lands
**Then** aria output is regenerated to essentials (name, sun %, opening hours, distance) with no orphaned separators or duplicated phrases, and unused i18n keys are pruned from `messages/{sv,en}`

### Design Gate Criteria

- **Visual:** Card matches the reference `QuickInfo` on both breakpoints with opening hours in place of the removed lines
- **Behaviour:** Heart/close/route/details actions work; opening hours reflect real venue data; no fabricated fields
- **Animation:** Card present/dismiss transitions unchanged
- **Visual validation:** Screenshot comparison (mobile + desktop, sunny + obscured states) against references passes before QA handoff

_Reading (non-verbatim guidance, not part of the acceptance text):_

- AC1 is a TWO-LAYER change. (a) DATA: `openingHours` today lives only on `VenueDetailDto` / `StoredVenueDetail`, NOT on the list `VenueDataDto`, and `toVenueData` (`venue-store.ts:240-267`) deliberately strips the detail block. AC1 requires ADDING `openingHours?: { display: string; closesAt?: string }` to `VenueDataDto` (`lib/types/api.ts:74-131`) and COPYING it in `toVenueData` (optional-guard pattern: `if (venue.openingHours !== undefined) base.openingHours = venue.openingHours;`). The real Supabase store already selects `opening_hours` (`VENUE_SELECT_COLUMNS:144`) and `fromVenueRow` already maps `row.opening_hours → detail.openingHours` (`:531`), so the value is available on `StoredVenue` — it is only dropped at the `toVenueData` boundary. `normalizeVenueForResponse` spreads `...venueWithoutUncertainty` (`venues-fixture.ts:218`) so a new DTO field passes through untouched; the `[slug]` detail DTO already carries `openingHours` and stays byte-identical. (b) RENDER: the card shows `openingHours.display` (e.g. "Öppet till 22:00") when present, NOTHING when absent (`openingHours == null` → render nothing, never a fabricated "Öppet"/fallback). Do NOT invent a closesAt-only fallback like the detail hero's `?? '22:00'` (`VenueDetailContent.tsx:138`) — that is detail-view chrome, not the honest quick-info rule.
- **CI-determinism gap (must be handled):** on the SEED path (flag OFF — what CI runs) `getVenues()` returns `VENUE_FIXTURE.map(...)` (`venue-store.ts:204-206`), and the eight `VENUE_FIXTURE` entries have NO `openingHours` (opening hours live only in the separate `VENUE_DETAIL_SEED` block, which is NOT merged into the list fixtures). So on the seed path `toVenueData` finds `openingHours === undefined` and the list DTO carries no opening hours → the quick-info shows nothing on CI seed data. To exercise the "renders opening hours" path deterministically WITHOUT live Supabase, add `openingHours: { display: 'Öppet till HH:MM', closesAt: 'HH:MM' }` to at least the two sunny `VENUE_FIXTURE` entries (and prove the absent-case with a fixture that omits it), OR drive the render via component-test props / a mocked `/api/venues` `page.route` DTO (the `epic-10-weather-matrix.spec.ts` precedent). Component tests (props-driven) are the cleanest proof of both present + absent branches; the e2e can assert the copy over a mocked DTO. Either way, the "absent → renders nothing" branch MUST be covered.
- AC1 removes the visible `confidence` text but KEEPS the sr-only accessible confidence line — the existing card already emits `<span className="sr-only"> {confidenceDisplay.accessibleText}</span>` inside the visible-confidence branch AND a standalone `sr-only` accessibleText when obscured/hidden (`VenueQuickInfo.tsx:282-292`). Removing the VISIBLE `{labels.confidence}: {confidenceDisplay.visibleText}` while keeping the sr-only accessibleText satisfies "confidence remains in detail view; sr-only accessible confidence text may remain." Do NOT remove `getConfidenceDisplayState` wiring outright if you keep the sr-only line — remove only the visible chrome.
- The reference `QuickInfo.jsx` (`src-free/` mobile, `src-desktop/` desktop) is INTENTIONALLY minimal: photo strip with the "% Sol" badge + heart, a centered name row, and exactly TWO buttons ("Visa rutt" + "Mer info") — NO confidence line, NO sun-window line, NO ETA text in the route button. The removals in AC1/AC2 move the card TOWARD the reference, not away from it. The reference does not itself show opening hours; AC1 adds opening hours in the slot the removed lines vacated (a single truthful line under the name). Keep the obscured two-signal treatment (Cloud icon + "Sol bakom moln" headline + sky line + muted `% SOL` badge, Story 10.2) — the reference predates the obscured state, so preserve it as the app's own addition.
- AC2 is a CALL-SITE change, not a `RouteButton` API change: the quick-info's `<RouteButton … estimateLabel={routeEstimateLabel} compact={isAnchoredMobile} …>` (`VenueQuickInfo.tsx:327-335`) must stop receiving `routeEstimateLabel`. Simplest: MapView stops passing `routeEstimateLabel={quickInfoRouteEstimateLabel}` into the two `<VenueQuickInfo>` renders (`MapView.tsx:1242,1273`) and the prop is dropped from `VenueQuickInfoProps`, OR the quick-info always passes `estimateLabel={undefined}` into its own `RouteButton`. `RouteButton` already renders no ETA span when `estimateLabel` is falsy (`RouteButton.tsx:63-72`) — no component edit needed there. The DETAIL/route surface (`MapView.tsx:1187,1211` — `VenueDetailContent`/`RouteOverlay`) keeps its own `estimateLabel` if already present (AC2: "the ETA may live on in the detail/route surface"); do NOT strip it there.
- AC4 aria: the current accessible name is the venue name on the `role="dialog"` (`aria-label={name}`), and the sun/confidence/distance/opening-hours copy is read inline. Regenerate so the read-out is name → sun % → opening hours → distance with NO orphaned "·" separators or duplicated phrases once the confidence/sun-window lines are gone. Watch the inline "·" separators (`:293` `' · '`, `:248` the obscured "·") — a removed segment must not leave a dangling separator. Prune any i18n key that is now unused after the removals (candidates: `venue.quickInfo.sunWindow`, and — if the visible confidence label is fully gone from quick-info AND unused elsewhere — verify before deleting `quickInfo.confidence`/`confidenceApproximate`/`confidenceUnavailable`, which the sr-only accessibleText still consumes via `confidenceDisplayLabels`). Only prune keys with NO remaining consumer; `messages-parity` (sv/en) must stay green.
- This is a NEW visual state for the quick-info card on both breakpoints and across four sun states → dev is FORBIDDEN from self-blessing / creating / editing reference PNGs. The consolidated maintainer rebaseline is owned by **Story 11.7** (note it as a maintainer follow-up in Completion Notes). The real-device pass over the reworked card is **Story 11.8's**. The LLM visual gate ignores sizing/spacing — assert code-level facts (no `Säkerhet` text node, no sun-window text node, opening-hours text present/absent, route button has no ETA span, aria name shape) in tests, not the eyeball.

## Tasks / Subtasks

- [x] **Task 1 — Surface `openingHours` on the list DTO (AC1 data layer)**
  - [x] Add `openingHours?: { display: string; closesAt?: string }` to `VenueDataDto` in `lib/types/api.ts` (near the other optional detail-adjacent fields; document it as OPTIONAL — present only where the store has `venues.opening_hours`, absent otherwise; never fabricated). `VenueDetailDto extends VenueDataDto` and re-declares `openingHours` as REQUIRED — confirm the `extends` narrowing still typechecks (a required override of an optional base field is legal in TS; verify `tsc` clean).
  - [x] Copy it through `toVenueData` (`venue-store.ts:240-267`) with the optional-guard pattern the other detail-block-adjacent fields use (`if (venue.openingHours !== undefined) base.openingHours = venue.openingHours;`). This lands it on BOTH the seed path (when the fixture carries it) AND the real-engine path (`sun-engine.ts` builds its DTO from `toVenueData(venue)` / `mergeSunFields(toVenueData(venue), …)` at `:414`,`:788`) AND the real Supabase store (`fromVenueRow:531` already populates `detail.openingHours` from `row.opening_hours`).
  - [x] Confirm `normalizeVenueForResponse` (`venues-fixture.ts:201-230`) passes the new field through (it spreads `...venueWithoutUncertainty` — a new field is preserved untouched). No edit needed there unless a normalization/validation is desired; if adding one, keep the "absent → absent, never fabricated" rule.
  - [x] CI-determinism: add `openingHours: { display: 'Öppet till HH:MM', closesAt: 'HH:MM' }` to at least the two sunny `VENUE_FIXTURE` entries (`test-venue-sunny`, `bryggeriet-soltak`) so the seed-path list DTO carries a real value for e2e/component determinism, and LEAVE at least one fixture without it to prove the absent → renders-nothing branch. (The seed `getVenues()` does NOT merge `VENUE_DETAIL_SEED`, so the fixtures themselves must carry it.)

- [x] **Task 2 — Remove the confidence + sun-window lines, render opening hours (AC1 render)**
  - [x] In `VenueQuickInfo.tsx` REMOVE the visible "Säkerhet: NN%" chrome (`:282-289` the `{labels.confidence}: {confidenceDisplay.visibleText}` span) and the `!isObscured && confidenceDisplay.visibleText && ' · '` separator (`:293`) on BOTH breakpoints. KEEP the sr-only accessible confidence text (`:286`,`:290-292`) so the accessible name is unchanged (AC1: "the sr-only accessible confidence text may remain").
  - [x] REMOVE the "Sol HH:mm–HH:mm" window line (`:268-275` — the `sunTimeRange ?? labels.sunUnavailable` paragraph). Drop the `sunTimeRange` prop from `VenueQuickInfoProps` if it becomes unused, and stop passing `sunTimeRange={resolveSunTimeRange(...)}` from the two MapView renders (`:1228`,`:1258`); remove `resolveSunTimeRange`/`quickInfoSunWindowTemplate` if they have no other consumer (verify — `resolveSunTimeRange` is quick-info-only per grep).
  - [x] RENDER real opening hours in the vacated slot: show `openingHours.display` (e.g. "Öppet till 22:00") when the venue has it, render NOTHING when absent (`openingHours == null`). Thread a new `openingHours?: { display: string; closesAt?: string }` prop into `VenueQuickInfoProps` and pass `openingHours={selectedQuickInfoVenue?.openingHours}` from both MapView renders. Use the design-system type/colour tokens (frontend-component skill — no ad-hoc hex; a muted body token that passes the axe AA gate on the cream card, mirroring the distance line's `text-text-body` choice).
  - [x] Preserve the obscured two-signal block (`:232-253`) and the muted `% SOL` badge (`VenueThumbnail:446-472`) exactly — Story 10.2's treatment stays.

- [x] **Task 3 — Drop the truncated ETA from the quick-info route button (AC2)**
  - [x] Stop passing an `estimateLabel` into the quick-info's `RouteButton` (`VenueQuickInfo.tsx:327-335`): either remove `routeEstimateLabel` from `VenueQuickInfoProps` (and stop passing it from MapView `:1242`,`:1273`) or hardcode `estimateLabel={undefined}` on the internal `RouteButton`. `RouteButton` already omits the ETA span when `estimateLabel` is falsy (`:63-72`) — NO `RouteButton` component edit.
  - [x] Leave the detail/route surfaces' `estimateLabel` intact (`VenueDetailContent` `:1187`, and `detailRouteEstimateLabel` wiring) — AC2 explicitly allows the ETA to live on there.
  - [x] Verify the button reads only "VISA RUTT" + nav icon with no truncation at common mobile widths (compact) and desktop; the accessible label falls back to just `label` when `estimateLabel` is undefined (`RouteButton.tsx:27-29`).

- [x] **Task 4 — Align the mobile card layout to the reference across four sun states (AC3)**
  - [x] Align spacing / type hierarchy / badge placement / CTA row to the reference `QuickInfo.jsx` (`src-free/` mobile) incorporating the removals — centered name row, "% Sol" badge top-left of the photo strip, heart top-right, the single opening-hours line under the name, then the two-button CTA row (Visa rutt + Mer info). Use design tokens only (frontend-component skill).
  - [x] Verify no overflow across all four sun states (Sunny / Partial / Shaded / obscured "Sol bakom moln") at common mobile widths — the obscured block adds an extra line, so confirm the `max-h-[calc(100dvh-2rem)]` card + the anchored-mobile width token (`--size-quick-info-mobile-w`) still hold without clipping. The obscured treatment (Story 10.2) is PRESERVED, not reworked.
  - [x] Keep the anchored-mobile close-pill position (`-right-3 -top-3`, Story 9.9) and the mobile compact strip heights; this is a content/spacing alignment, not a re-layout of the animation/anchoring.

- [x] **Task 5 — Regenerate aria, prune unused i18n keys (AC4)**
  - [x] Regenerate the accessible read-out to essentials: name → sun % → opening hours → distance, with NO orphaned "·" separators and no duplicated phrases once the confidence/sun-window lines are removed. Audit every inline separator (`:248`,`:293`) so a removed segment never leaves a dangling "·".
  - [x] Prune i18n keys that now have NO consumer after the removals. Strong candidate: `venue.quickInfo.sunWindow` (only `quickInfoSunWindowTemplate` used it). BEFORE deleting `quickInfo.confidence`/`confidenceApproximate`/`confidenceUnavailable`, confirm they are still consumed by the sr-only `confidenceDisplayLabels` (they are, via `getConfidenceDisplayState`) → KEEP those. Delete only genuinely-dead keys, in BOTH `messages/sv` AND `messages/en`; keep `messages-parity` green.
  - [x] If a NEW opening-hours label is needed for the quick-info (likely NOT — render the raw `openingHours.display` string, which is already localized copy from the store, matching how the detail hero renders `detail.openingHours.display` directly), do NOT add a template key that would fabricate wording. Only the raw display string is shown.

- [x] **Task 6 — Tests (AC1–AC4)**
  - [x] **Component (`VenueQuickInfo.test.tsx`, AC1/AC2/AC4):** assert NO "Säkerhet" visible text node and NO "Sol HH:mm–HH:mm" window text on both `mode="mobile"` and `mode="desktop"`; assert `openingHours.display` renders when the prop is present and renders NOTHING when absent (both branches); assert the route button has NO ETA span (query the compact `RouteButton` for the estimate text and expect absent); assert the sr-only accessible confidence text is still present; assert the regenerated accessible name has no dangling separator / duplicate phrase. Extend the existing `labels` fixture + `expectNoSensitiveSourceTerms` guard.
  - [x] **Component/render (AC3):** render all four sun states (Sunny/Partial/Shaded/obscured `currentSunStatus:'CloudObscured'` + `skyCondition`) and assert the obscured block + muted badge still render (Story 10.2 preserved) alongside the new opening-hours line, with no confidence/sun-window text — pinning behaviour, not pixels.
  - [x] **DTO/API (AC1 data):** extend the venues-route contract test / venue-store test to assert `openingHours` is present on the list DTO when the store carries `opening_hours` (real-store mapping via `fromVenueRow`) AND absent when the fixture omits it — so the "surfaced on the list DTO, never fabricated" contract is pinned at the boundary. If the seed fixtures gain `openingHours`, assert the seed-path `/api/venues` response carries it for the seeded venues.
  - [x] **E2E (optional, over a mocked `/api/venues` DTO):** if an e2e is added, assert the quick-info shows the opening-hours copy and no "Säkerhet"/sun-window text over a mocked DTO at `?_time=13:00` (no live Met.no) at both `--project=mobile` and `--project=desktop`. The request-count/perf gates are Story 11.8's — do NOT add them here.
  - [x] **i18n parity:** `messages-parity` test stays green after key pruning.

- [x] **Task 7 — Gates**
  - [x] `npx tsc --noEmit` → 0 errors. `npx eslint .` → 0 errors (no NEW warnings in touched files). `npx vitest run` → all pass (measure the baseline fresh from HEAD; the count should INCREASE with the new tests, none dropped).
  - [x] Run the axe gate for the quick-info surface (both breakpoints) — the new opening-hours line + the removals must keep `axe`/`axe-mobile` green (no contrast/focus regression; use a body token that clears AA on the cream card, mirroring the distance line's non-muted choice at `:311-313`).
  - [x] Do NOT create/edit/self-bless any reference PNG — the reworked card is a NEW visual state; the consolidated rebaseline is Story 11.7's maintainer checkpoint (note it in Completion Notes). Hand the real-device pass to Story 11.8.

## Dev Notes

### Scope fences — what this story is and is NOT

- **IS:** the quick-info content rework — remove the visible confidence text + the sun-window line (AC1), render real opening hours in their place (AC1, which requires adding `openingHours` to the list `VenueDataDto` + copying it in `toVenueData` + carrying it in the fixtures for CI determinism), drop the truncated ETA from the quick-info route button (AC2), re-align the mobile card layout to the reference across four sun states preserving the Story-10.2 obscured treatment (AC3), and regenerate the aria + prune dead i18n keys (AC4).
- **IS NOT:** the day-series / query-key / client-derivation seam (11.1/11.2 own it — this story reads already-derived `selectedQuickInfoVenue` fields, it does NOT touch `useVenueSearch`/`useFavouriteVenues` or the query key); the slider/planner rules (11.2); the mobile tag chips / bottom-sheet snaps / desktop chip overflow (11.3); the map de-dull tint / location dot / recenter (11.5); the venue-detail first-paint / "Soltider idag" removal / reviews-centering (11.6 — note: 11.6 removes the detail "Soltider idag" section; THIS story does NOT touch the detail view except that both consume `openingHours` — do not pre-empt 11.6's detail work); the `toSunStatusToken` / `vercel.json` / `.gitattributes` hygiene (11.7); the live-perf / touch-gesture / request-count regression gates (11.8). Do NOT touch those seams.
- **No new dependency. No new route/schema. No engine/weather-service edit.** The ONLY data-shape change is the additive optional `openingHours` field on `VenueDataDto` + its copy in `toVenueData` (the value already exists on `StoredVenue`/the Supabase select — it was just being stripped). The geometric meaning of `sunExposurePercent`/`sunWindow`, the Epic-10 weather gate, and the `[slug]` detail DTO stay byte-identical.

### `openingHours` is available on the store but stripped at the list boundary — the fix is additive (AC1)

The value flow today:
- `StoredVenueDetail.openingHours: { display: string; closesAt?: string }` (`venue-store.ts:34`), merged into `StoredVenue = VenueDataDto & StoredVenueDetail & StoredVenueServerOnly` (`:74`).
- Real Supabase store: `VENUE_SELECT_COLUMNS` already selects `opening_hours` (`:144`); `fromVenueRow` already maps `if (row.opening_hours) detail.openingHours = row.opening_hours` (`:531`). So a live venue's opening hours reach `StoredVenue`.
- `toVenueData(venue)` (`:240-267`) builds the LIST DTO and DELIBERATELY omits the detail block (`storedVenueDetail` extracts it separately for the detail route). This is exactly where opening hours is dropped from the list surface.
- The real-engine list path uses `toVenueData(venue)` as its base (`sun-engine.ts:414`,`:788`), and `normalizeVenueForResponse` spreads unknown fields through (`venues-fixture.ts:218`), so ADDING `openingHours` to `VenueDataDto` + copying it in `toVenueData` surfaces it end-to-end with no other plumbing.

The honest rule (AC1 + FR/Epic-11 maintainer decision "show real opening hours … venues without opening-hours data show nothing"): present the raw `openingHours.display` string when the store has it, render NOTHING when absent. Do NOT synthesize a closesAt-only fallback (the detail hero's `?? '22:00'` at `VenueDetailContent.tsx:138` is detail-view chrome, not a truthful list rule).

### CI runs the SEED path — the fixtures must carry opening hours for deterministic proof

`getVenues()` on the seed path returns `VENUE_FIXTURE.map(...)` (`venue-store.ts:204-206`) — it does NOT merge `VENUE_DETAIL_SEED`, so the eight `VENUE_FIXTURE` entries have NO `openingHours` today. CI runs flag OFF (seed path), so unless the fixtures gain `openingHours`, the seed-path list DTO carries none and the "renders opening hours" path is unreachable in CI. Add `openingHours` to at least the two sunny fixtures (present-case) and leave at least one without (absent-case) so both branches are deterministically covered. Component tests (props-driven) additionally prove both branches without any store at all — the cleanest coverage of "renders when present / nothing when absent."

### The reference `QuickInfo.jsx` is minimal by design — the removals move TOWARD it (AC1/AC2/AC3)

`docs/design/references/claude-design/project/src-free/QuickInfo.jsx` (mobile) and `src-desktop/QuickInfo.jsx` (desktop) show: photo strip with a "% Sol" badge (top-left) + a favourite heart (top-right), a centered name row, and exactly TWO buttons ("Visa rutt" + "Mer info"). There is NO confidence line, NO "Sol HH:mm–HH:mm" line, and NO ETA text inside the route button. So AC1 (remove Säkerhet + sun-window) and AC2 (drop the ETA) align the live card to the reference; AC1 then adds the single truthful opening-hours line in the freed space. The reference predates Story 10.2's obscured state — PRESERVE the obscured two-signal treatment (Cloud icon + "Sol bakom moln" + sky line + muted `% SOL` badge) as the app's own addition; the reference alignment is about spacing/hierarchy/CTA-row, not deleting the obscured chrome.

### Confidence: remove the VISIBLE text, keep the sr-only accessible line (AC1/AC4)

`VenueQuickInfo.tsx:282-292` renders the visible `{labels.confidence}: {confidenceDisplay.visibleText}` PLUS an sr-only `{confidenceDisplay.accessibleText}`. Remove only the visible chrome + its `' · '` separator; keep the sr-only accessibleText (AC1: "the sr-only accessible confidence text may remain") so the accessible name doesn't regress. That means `getConfidenceDisplayState` + `confidenceDisplayLabels` + the `quickInfo.confidence*` i18n keys STAY (they feed the sr-only line) — do NOT prune those. The `venue.quickInfo.sunWindow` key IS a prune candidate (its only consumer, `quickInfoSunWindowTemplate` → `resolveSunTimeRange`, goes away with the window line). Prune only after grepping for zero remaining consumers, in both locales, keeping `messages-parity` green.

### Aria regeneration — watch the inline separators (AC4)

The card reads inline: name (button) → obscured block (if obscured) → sun-window line (REMOVED) → confidence + distance line. Once the confidence visible text and the sun-window line are gone, ensure the read-out is name → sun % (from the badge/obscured block) → opening hours → distance with no orphaned "·" (the `' · '` at `:293` and the obscured "·" at `:248` must not dangle when a neighbour is removed). Assert the accessible name shape in a component test (no "Säkerhet" phrase duplicated, no leading/trailing separator).

### Deferred-work items that overlap this story (fold in / note; do NOT reopen unrelated ones)

- **[NOTE only] Orphaned i18n keys `quickInfo.obscuredPosition` / `detail.obscuredPosition`** (epic-10 review, Target:None) — `venue.quickInfo.obscuredPosition` (`messages/sv/venue.json:26`, mirrored en) is defined but NOT consumed by `VenueQuickInfo` (the component wires `obscuredHeadline` + `skyLine`, never `obscuredPosition`). Since AC4 prunes unused quick-info i18n keys, this dead key is IN SCOPE to remove as part of the pruning pass IF it remains unconsumed after your rework (verify no new consumer). The `detail.obscuredPosition` mirror is the DETAIL surface (Story 11.6's territory) — leave it. Removing the quick-info `obscuredPosition` key (both locales) is a legitimate fold-in of AC4's "prune unused i18n keys"; do not fabricate a new consumer just to keep it.
- **[NOTE only] Obscured thumbnail badge renders the literal "SOL" token** (epic-10 review, Target:None) — `VenueThumbnail` renders `{sunExposureText} SOL` outside the `isObscured` branch (`:470`), so an obscured card badge reads "95 SOL" in slate. This is a copy-consistency cosmetic flagged as conditional/Target:None; AC3 says PRESERVE the Story-10.2 obscured treatment, so do NOT reframe the badge token in this story unless the reference-alignment explicitly requires it — leave the deferred item as-is (do not reopen/re-defer). Note it if the reference clearly shows a different badge, else leave it.
- Everything else in `deferred-work.md` (offline shell, 404 a11y, share modal, MapLibre roots, venue-card fallbacks, `toSunStatusToken`, `vercel.json`/`.gitattributes`, the detail/timeline seams, recenter magic numbers, distance-approximate) is OUT of scope for 11.4 — do NOT touch. (`toSunStatusToken` + build-infra are Story 11.7's; the detail-view seams are 11.6's; the map/dot/recenter is 11.5's.)

### Constraints ratified earlier in Epic 11 (from the epic-11 retro-notes) — reflect directly

- **Venue query keys are date-only + coords + `isLiveNow` — NEVER reintroduce `time`.** This story reads already-derived per-time fields off `selectedQuickInfoVenue` (the client day-series derivation seam from 11.1). It does NOT fetch and does NOT touch `useVenueSearch`/`useFavouriteVenues` or the query key. Adding `openingHours` is a per-venue STATIC attribute (not time-dependent) on the list DTO — it does NOT enter the query key and does NOT vary per planner step. Keep it that way.
- **Per-step sun values come from the client day-series derivation seam (11-1) — do NOT re-derive or re-gate in the card.** The quick-info renders the client-derived `sunExposurePercent`/`currentSunStatus`/`skyCondition` for the selected time; it never re-computes or re-gates. This story only changes which content lines the card shows, not where the sun values come from.
- **Dual-variant-panel e2e selectors must use `.filter({ visible: true })`, never positional `.first()`/`.last()`.** Both `<VenueQuickInfo mode="mobile">` and `mode="desktop">` are ALWAYS mounted (CSS-hidden per breakpoint via `hidden lg:block` / `lg:hidden` at `:161-165`), so a DOM-order `.first()` on `[data-testid="venue-quick-info"]` binds the hidden variant at the wrong breakpoint. Any e2e that targets the quick-info MUST filter by visibility (retro-notes 11-1 fix-pass lesson — this is the exact dual-mounted pattern that trap applies to).
- **The amber-badge contrast class must reach >=4.5:1 wherever touched.** The quick-info's `VenueThumbnail` "% SOL" badge uses `bg-amber-gold/90 text-amber-cta-text` (a different class from the venue-card amber label). If the reference-alignment touches the badge, keep it AA. The known boundary-miss amber-badge debt (`text-amber-badge-text` on `bg-amber-primary` ≈ 4.47:1) lives on `VenueDetailContent`/`VenueCard` (Story 5.1 / 11.6 territory), NOT the quick-info — do NOT touch it here, but do NOT introduce a new sub-4.5:1 amber label on the quick-info either (the axe gate has been active since Epic 9 and must stay green).
- **Anti-"shipped-but-insufficient" (epic thesis):** the "Säkerhet: NN%" clutter, the redundant sun-window line, and the truncated "ca 16…" ETA are real live-field defects (2026-07-04 maintainer field test). REMOVE them cleanly and render one honest opening-hours line — do NOT leave a half-state (e.g. an empty separator, a hidden-but-still-plumbed sunWindow, or a fabricated "Öppet" when the store has no hours).
- **NEW visual state → dev FORBIDDEN from self-blessing reference PNGs.** The reworked card (both breakpoints, sunny + obscured) is a new visual state; Story 11.7 owns the consolidated maintainer rebaseline. The LLM visual gate ignores sizing/spacing → assert code-level facts in tests.

### API boundary + testing standards

- One additive optional DTO field (`VenueDataDto.openingHours`) + its `toVenueData` copy + fixture data for CI. No new route/schema/dependency; no engine/weather edit; the `[slug]` detail DTO stays byte-identical (it already had `openingHours`).
- Vitest for unit/component + the DTO/store contract test; Playwright (optional) for the copy assertion over a mocked `/api/venues` DTO. No live Met.no in tests (seed path flag OFF, or mocked `page.route` DTO — `epic-10-weather-matrix.spec.ts` precedent). CI runs Playwright against `next dev` (so `?_time=`/`?_date=` forcing fires).
- The axe AA gate has been ACTIVE since Epic 9 — the reworked quick-info (both breakpoints) must keep `axe`/`axe-mobile` green. Use a body token for the opening-hours line that clears AA on the cream card (mirror the distance line's non-muted `text-text-body` choice at `:311-313`, chosen precisely because `text-muted`'s 60% alpha fails AA there).
- `frontend-component` skill is MANDATORY for the opening-hours line, any spacing/type token, and the reference alignment — design-system-first, token-based, no ad-hoc hex/opacity/px.
- Standard gate: `tsc --noEmit`, `eslint`, `vitest run`, then the axe e2e projects.

### Project Structure Notes

- **Edits:** `lib/types/api.ts` (add `openingHours?` to `VenueDataDto`), `lib/services/venue-store.ts` (copy `openingHours` in `toVenueData`), `lib/services/venues-fixture.ts` (add `openingHours` to the sunny `VENUE_FIXTURE` entries for CI determinism; leave one without), `components/composed/venue/VenueQuickInfo.tsx` (remove confidence-visible + sun-window lines, render opening hours, drop the route ETA, re-align mobile layout, regenerate aria; drop unused props), `components/custom/map/MapView.tsx` (pass `openingHours`, stop passing `sunTimeRange`/`routeEstimateLabel` into the two `<VenueQuickInfo>` renders; remove `resolveSunTimeRange`/`quickInfoSunWindowTemplate` if now unused), `messages/sv/venue.json` + `messages/en/venue.json` (prune dead keys — `quickInfo.sunWindow`, and the unconsumed `quickInfo.obscuredPosition`), plus tests. `RouteButton.tsx` needs NO edit (it already omits the ETA span when `estimateLabel` is falsy).
- **New file(s):** none required (no new component; the change is content/props/DTO). Optionally a new e2e spec for the copy assertion.
- Follow the `components/` layering: `composed/` = presentational over props (VenueQuickInfo); `custom/` = smart/feature (MapView orchestrates props); `ui/` = primitives (architecture.md `:957-958`).

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Epic 11 — Story 11.4] — ACs + Design Gate (lines ~2897-2925); maintainer decision (2026-07-04 workshop, ~2803): "remove the 'Säkerhet: NN%' text (confidence becomes detail-view-only … a *different* number from the thumbnail's '% SOL') and the 'Sol HH:mm–HH:mm' window; show real opening hours instead; VISA RUTT loses the truncated 'ca 16…' ETA text"; epic root-cause context (~2793-2805).
- [Source: _bmad-output/test-artifacts/test-design/test-design-epic-11.md] — the Story-11.4 quick-info rows (remove Säkerhet/sun-window, opening hours on the list DTO, VISA RUTT no-ETA, reference alignment across sun states, aria + i18n prune); the "assert behaviour / code-level facts not the eyeball" discipline; the dedup discipline (quick-info render + a11y = COMPONENT, day-series DTO = API).
- [Source: _bmad-output/auto-bmad/retro-notes/epic-11.md] — venue query keys date-only + `isLiveNow` (never reintroduce time — the static `openingHours` field does not enter the key); per-step sun values come from the client day-series derivation seam (11-1); dual-variant-panel e2e selectors must use `.filter({ visible: true })` (both quick-info variants are always mounted, CSS-hidden per breakpoint); the amber-badge contrast class must reach >=4.5:1 wherever touched; NEW visual state → dev forbidden from self-blessing reference PNGs, Story 11.7 owns the consolidated rebaseline; anti-"shipped-but-insufficient".
- [Source: _bmad-output/implementation-artifacts/11-3-mobile-tag-filtering-bottom-sheet-overhaul.md] — the "NEW visual state → dev forbidden from self-blessing reference PNGs, Story 11.7 owns the rebaseline; real-device pass is 11.8's" convention; the seed-path / mocked-`page.route`-DTO / `?_time=13:00` no-live-Met.no e2e pattern; the axe-AA-gate-must-stay-green discipline.
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — [NOTE/FOLD] orphaned `quickInfo.obscuredPosition` i18n key (in scope for AC4's prune IF still unconsumed); [NOTE only] obscured badge "SOL" token (AC3 preserves the 10.2 treatment — do not reopen); all other entries out of scope.
- [Source: nextjs-app/components/composed/venue/VenueQuickInfo.tsx] — `VenueQuickInfoProps` (`:28-92`, add `openingHours?`, drop `sunTimeRange`/`routeEstimateLabel` if unused); the visible confidence chrome + separator `:282-293` (remove visible, keep sr-only `:286`,`:290-292`); the sun-window line `:268-275` (remove); the obscured two-signal block `:232-253` (preserve); the internal `RouteButton` `:327-335` (drop `estimateLabel`); `VenueThumbnail` "% SOL" badge `:446-472` (`bg-amber-gold/90 text-amber-cta-text`, keep AA); the distance line's `text-text-body` AA choice `:311-313`; anchored-mobile layout / close-pill (Story 9.9) `:161-203`.
- [Source: nextjs-app/components/composed/routing/RouteButton.tsx] — `estimateLabel` optional (`:9`); ETA span rendered only when truthy (`:63-72`); accessible label falls back to `label` alone when no estimate (`:27-29`) — NO edit needed.
- [Source: nextjs-app/lib/types/api.ts] — `VenueDataDto` (`:74-131`, ADD `openingHours?`); `VenueDetailDto extends VenueDataDto` with required `openingHours` (`:133-142` — verify the optional→required override typechecks); the Story-11.1 `sunDaySeries?` optional-list-only field pattern to mirror.
- [Source: nextjs-app/lib/services/venue-store.ts] — `StoredVenueDetail.openingHours` (`:34`); `StoredVenue` union (`:74`); `VENUE_SELECT_COLUMNS` incl. `opening_hours` (`:144`); `toVenueData` list-DTO builder (`:240-267`, ADD the `openingHours` copy with the optional-guard pattern); `fromVenueRow` `row.opening_hours → detail.openingHours` (`:531`); `getVenues()` seed path returns raw `VENUE_FIXTURE` (`:204-206` — no detail merge, hence the fixture-data need).
- [Source: nextjs-app/lib/services/venues-fixture.ts] — `VENUE_FIXTURE` entries (`:36-199`, add `openingHours` to the sunny ones for CI determinism, leave one without); `normalizeVenueForResponse` spreads unknown fields through (`:201-230`).
- [Source: nextjs-app/lib/services/sun-engine.ts] — real-engine DTO built from `toVenueData(venue)` / `mergeSunFields(toVenueData(venue), …)` (`:414`,`:788`,`:1067`) — the `openingHours` copy in `toVenueData` surfaces it on the real-engine list path too.
- [Source: nextjs-app/app/api/venues/route.ts] — `getVenues()` load (`:235`); real-engine `normalizeVenueForResponse` + `sunDaySeries` attach (`:334-348`); seed-path `normalizeVenueForResponse` (`:351-358`) — the new field passes through both.
- [Source: nextjs-app/components/custom/map/MapView.tsx] — the two `<VenueQuickInfo>` renders (mobile `:1224-1252`, desktop `:1254-1283`); `selectedQuickInfoVenue` memo (`:547-553`, source of `openingHours`); `quickInfoLabels` (`:1484-1509`); `resolveSunTimeRange`/`quickInfoSunWindowTemplate` (`:851`,`:1324-1333`, remove if unused); `quickInfoRouteEstimateLabel` (`:859-860`, stop passing to quick-info).
- [Source: nextjs-app/components/composed/venue/VenueDetailContent.tsx] — how the detail hero renders opening hours: `detail?.openingHours.display` (`:312`) and the `closesAt ?? '22:00'` hero fallback (`:138` — detail-only chrome, do NOT copy that fallback to the quick-info's honest rule).
- [Source: nextjs-app/messages/sv/venue.json + en/venue.json] — `quickInfo.sunWindow` (`:24`, prune candidate); `quickInfo.confidence*` (`:17-19`, KEEP — sr-only line still consumes them); `quickInfo.obscuredPosition` (`:26`, unconsumed — prune under AC4 if still dead); `detail.openingHours`/`detail.openUntil` (`:165`,`:182`, detail-view — do not touch).
- [Source: nextjs-app/lib/utils/confidence-display.ts] — `getConfidenceDisplayState` → `{ visibleText, accessibleText }` (`:24-66`); removing the visible chrome keeps `accessibleText` for the sr-only line.
- [Source: nextjs-app/docs/design/references/claude-design/project/src-free/QuickInfo.jsx] — the reference mobile quick-info (minimal: badge + heart + centered name + two buttons; NO confidence/sun-window/ETA) — the AC3 alignment target.
- [Source: nextjs-app/docs/design/references/claude-design/project/src-desktop/QuickInfo.jsx] — the reference desktop quick-info (same minimal composition, wider).
- [Source: nextjs-app/test/components/VenueQuickInfo.test.tsx] — existing component-test harness (`motion/react` mock, `labels` fixture, `expectNoSensitiveSourceTerms`) to extend for the removals + opening-hours branches.
- [Source: nextjs-app/playwright.config.ts] — projects (`mobile`=iPhone-14/WebKit `hasTouch`, `desktop`, `touch`=Pixel-5/Chromium, `a11y`, `a11y-mobile`); webServer runs `next dev` (`?_time=`/`?_date=` forcing).
- [Source: nextjs-app/docs/design/DESIGN.md] — quick-info card radius/shadow/type tokens, the amber palette AA note (NFR25), body/muted text tokens for the opening-hours line.
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md] — quick-info popover composition + present/dismiss motion (unchanged by this story per the Animation gate).
- [Source: _bmad-output/planning-artifacts/architecture.md] — TanStack Query key conventions (date-only, no time) `:585`; components layering (`custom`/`composed`/`ui`) `:957-958`; DTO/serialization boundary conventions.
- [Source: CLAUDE.md / AGENTS.md] — repo rulebook, Swedish-copy default, local-Docker rules, venue-data structure (`nextjs-app/docs/venue-data-load.md` for the `opening_hours` column).
- [Source: project-context.md] — Epic 9/10 ratified conventions; the production planner-forcing gate (`?_time=`/`?_date=`); "visual gate is an LLM eyeball" (ignores sizing/spacing — assert code-level facts for the removals + opening-hours branches, not the eyeball).

## Dev Agent Record

### Agent Model Used

Opus 4.8 (1M) — `claude-opus-4-8[1m]` (bmad-dev-story).

### Debug Log References

- `npx vitest run` → 139 files, 1300 passed (baseline at HEAD was 1288; +12 net new — no tests dropped).
- `npx tsc --noEmit` → 0 errors.
- `npx eslint .` → 0 errors, 13 warnings (all pre-existing; none introduced by touched code — the `<img>` warning at `VenueQuickInfo.tsx:407` is the pre-existing thumbnail element, unchanged).
- `npx playwright test --project=a11y --project=a11y-mobile` → 13 passed, 8 skipped (pre-existing `test.fixme` debt). The axe specs select a venue and scan the (reworked) quick-info surface on both breakpoints incl. the obscured variant — the new opening-hours line + the removals keep the AA gate green.

### Completion Notes List

- **AC1 (data):** Added an additive optional `openingHours?: { display; closesAt? }` to `VenueDataDto` (`lib/types/api.ts`) and copied it through `toVenueData` (`venue-store.ts`) with the optional-guard pattern. This surfaces the value end-to-end on the seed path (fixture-carried), the real-engine path (`sun-engine.ts` builds its DTO from `toVenueData`), and the real Supabase store (`fromVenueRow` already populated `detail.openingHours`). `VenueDetailDto`'s REQUIRED `openingHours` override typechecks. The `[slug]` detail DTO stays byte-identical.
- **AC1 (render):** Removed the visible "Säkerhet: NN%" chip + its `' · '` separator and the "Sol HH:mm–HH:mm" window line from `VenueQuickInfo.tsx` on both breakpoints. Kept the sr-only accessible confidence text (accessible name unchanged). Render `openingHours.display` when present, NOTHING when absent (no fabricated fallback). Opening-hours line uses `text-text-body` (mirrors the distance line — clears the axe AA gate on the cream card).
- **AC1 (CI determinism):** Added real `openingHours` to the two sunny `VENUE_FIXTURE` entries (`test-venue-sunny`, `bryggeriet-soltak`); the other five fixtures omit it, so both the present and absent branches are CI-provable on the flag-OFF seed path.
- **AC2:** The quick-info's internal `RouteButton` no longer receives `estimateLabel` (call-site change; no `RouteButton` edit). Dropped `routeEstimateLabel` from `VenueQuickInfoProps` and stopped passing it (and the now-dead `quickInfoRouteSummary`/`quickInfoRouteEstimateLabel` wiring) from MapView. The detail/route overlay keeps its own ETA (`ca N min promenad`).
- **AC3:** The mobile card already matched the reference composition (badge top-left, heart top-right, centered name, two-button CTA). The removals move it toward the reference; the opening-hours line fills the vacated slot; the Story-10.2 obscured two-signal block + muted `% SOL` badge are preserved unchanged. Four-sun-state render coverage added.
- **AC4:** Regenerated aria to name → sun% (badge) → opening hours → distance with the confidence sr-only line kept and NO dangling `·` (asserted). Pruned three now-dead `quickInfo.*` i18n keys from BOTH locales: `sunWindow` (its only consumer, `quickInfoSunWindowTemplate`/`resolveSunTimeRange`, was removed), `sunUnavailable` (only the removed window line used it — also dropped from the `labels` type + `quickInfoLabels`), and `obscuredPosition` (the epic-10 deferred orphan, verified still unconsumed). Kept `confidence*` (sr-only line still reads them). `messages-parity` + a new `removed-i18n-keys` pin stay green.
- **Maintainer follow-ups:** This is a NEW visual state on both breakpoints across four sun states — the consolidated reference-PNG rebaseline is **Story 11.7's** maintainer checkpoint (dev did not create/edit/self-bless any PNG). The real-device pass over the reworked card is **Story 11.8's**.
- **Scope discipline:** No new dependency/route/schema; no engine/weather edit. The only data-shape change is the additive optional `VenueDataDto.openingHours` + its `toVenueData` copy. Query keys (date-only + `isLiveNow`) untouched — `openingHours` is a static per-venue attribute and never enters the key.

### File List

- `nextjs-app/lib/types/api.ts` — added optional `openingHours` to `VenueDataDto` (M)
- `nextjs-app/lib/services/venue-store.ts` — copy `openingHours` in `toVenueData` (M)
- `nextjs-app/lib/services/venues-fixture.ts` — added `openingHours` to the two sunny fixtures (M)
- `nextjs-app/components/composed/venue/VenueQuickInfo.tsx` — removed confidence-visible + sun-window lines, render opening hours, drop the route ETA, drop `sunTimeRange`/`routeEstimateLabel`/`sunUnavailable` props, regenerate aria (M)
- `nextjs-app/components/custom/map/MapView.tsx` — pass `openingHours`, stop passing `sunTimeRange`/`routeEstimateLabel`, remove `resolveSunTimeRange`/`quickInfoSunWindowTemplate`/`quickInfoRouteSummary`/`quickInfoRouteEstimateLabel`/`sunUnavailable` wiring (M)
- `nextjs-app/messages/sv/venue.json` — pruned `quickInfo.sunWindow`/`sunUnavailable`/`obscuredPosition` (M)
- `nextjs-app/messages/en/venue.json` — pruned `quickInfo.sunWindow`/`sunUnavailable`/`obscuredPosition` (M)
- `nextjs-app/test/components/VenueQuickInfo.test.tsx` — rewrote for the reworked card (no Säkerhet/sun-window, opening-hours present+absent branches, no ETA span, sr-only confidence kept, aria shape, four sun states) (M)
- `nextjs-app/test/components/VenueQuickInfoApproximateDistance.test.tsx` — dropped removed props from the harness (M)
- `nextjs-app/test/components/MapView.test.tsx` — updated quick-info integration assertions (opening hours in place of sun-window; sun-badge as the per-time refresh proof; no ETA in the button; `makeVenue` gains an `openingHours` default) (M)
- `nextjs-app/test/unit/services/venue-store.test.ts` — updated `toVenueData`/seed-list contract tests for the surfaced `openingHours` (present + absent) (M)
- `nextjs-app/test/unit/api/venues-route.test.ts` — new seed-path `/api/venues` contract assertion for `openingHours` (present + absent) (M)
- `nextjs-app/test/unit/removed-i18n-keys.test.ts` — new pin for the three pruned `quickInfo.*` keys (+ confidence* kept) (M)

## Change Log

- 2026-07-05 — Story 11.4 context file drafted (create-story). FOURTH story of Epic 11 "Feels Instant, Reads Clear" — quick-info reference alignment + truthful content. Status → ready-for-dev.
- 2026-07-05 — Story 11.4 implemented (dev-story). Surfaced `openingHours` on the list `VenueDataDto` (+ `toVenueData` copy + sunny-fixture data); reworked `VenueQuickInfo` to remove the visible "Säkerhet: NN%" chip + the "Sol HH:mm–HH:mm" window line, render the real opening-hours line, and drop the truncated route ETA; regenerated aria (sr-only confidence kept, no dangling separator); pruned three dead `quickInfo.*` i18n keys in both locales; preserved the Story-10.2 obscured treatment. All ACs satisfied; tsc/eslint/vitest (1300 pass) + axe (a11y/a11y-mobile) green. Status → review.
- 2026-07-05 — Follow-up spec alignment. `test/e2e/map-primary.spec.ts` ("mobile: QuickInfo route opens maps…") still asserted the removed `ca \d+ min` ETA inside the quick-info card and failed at HEAD; re-anchored the assertion on the "VISA RUTT" CTA + `toHaveCount(0)` for card ETA text (AC2 contract) while preserving the real intent (route click → native handoff + dismissible route overlay). Spec green on desktop+mobile projects; no source changes.

## Review Findings

_Triage of code review round 1 (auditor-primary + security). Verdict: Approve (all findings Low; 1 Patch, 1 Dismissed noise). No Decisions, no Deferrals._

- [x] [Review][Patch][Low] Dead `routeEstimateLabel?: string;` left in `VenueQuickInfoProps` — the AC2 removal dropped `routeEstimateLabel` from the component destructuring, the internal `<RouteButton>` call, and both MapView call sites, but left the orphaned type member. This contradicts the story's own Completion Notes (AC2) claim "Dropped `routeEstimateLabel` from `VenueQuickInfoProps`" and the Dev Notes anti-"half-state" rule (no "hidden-but-still-plumbed" prop). Functionally harmless (a future call site passing it would be silently ignored — `RouteButton` correctly reads "VISA RUTT" alone). Fix: delete line. [nextjs-app/components/composed/venue/VenueQuickInfo.tsx:65]
