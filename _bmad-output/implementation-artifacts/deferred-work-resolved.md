# Deferred Work — Resolved

## Deferred from: code review of 9-5-location-onboarding-reliability (2026-06-30)

- *(CLOSED by Story 9.9 Task 3, 2026-07-01 — the honest `≈ från centrum` / `≈ from centre` label now threads into `VenueQuickInfo` via a `distanceIsApproximate` prop + a parity-guarded `quickInfo.distanceApproximate` key, wired from `locationIsApproximate` at both MapView call sites; the real path was `composed/venue/VenueQuickInfo.tsx`, not the stale `custom/venue/` path in the original note.)*

## Deferred from: code review Round 2 of 1-3-responsive-layout-shell-navigation (2026-04-20)

- ~~**`MapContext` value change re-renders every consumer on any `selectedVenueId` change**~~ **RESOLVED 2026-05-02 by Story 1.4** — the split into `MapInstanceContext` + `MapSelectionContext` (Story 1.4 Task 2) addressed this exactly. `MapInstanceContext` value is now `useMemo`'d (Round 1 patch P14). No further action.

## Deferred from: code review Round 2 of 1-4-maplibre-integration-venue-pin-layer (2026-05-03)

- ~~**`/api/map-style` no concurrent-request dedup**~~ **RESOLVED 2026-06-04 by deferred-work audit** — the MapTiler proxy route no longer exists in `nextjs-app/app/api/`; `MapContainer` uses OpenFreeMap directly. Any future provider/proxy decision remains covered by the OpenFreeMap availability fallback item above.
*(Two entries — `useVenueSearch.test.ts` confidence fixture coverage and `VenuePinData.sunStatus` literal drift — carried into Story 2.6 — Confidence Display & Auto-Refresh on 2026-05-22 (Tasks 2 and 7). Removed by SM per the deferred-work convention.)*
- **OpenFreeMap tile-quality / availability fallback** *(Target: None — conditional, only triggers if OpenFreeMap proves insufficient post-2.x)* — Resolved Round 2 D3=B: dropped MapTiler entirely; OpenFreeMap is now the only style source. OpenFreeMap is community-run with no SLA. If tile quality, label coverage for Sweden, or uptime proves insufficient after Story 2.x ships real-data flows, evaluate building a tile proxy (`/api/map-tile/{z}/{x}/{y}`) and re-introduce MapTiler with full key isolation. Trigger: user/PM signal, or sustained OpenFreeMap downtime in production logs. **AUDITED 2026-06-04:** no active tile-provider incident is recorded in this checkout; keep conditional.

## Deferred from: code review of 1-6-ci-cd-quality-gates (2026-05-07)

- ~~**W9: PRD NFR3 (`INP <=200ms`) vs architecture NFR table (`INP <=100ms`) inconsistency**~~ **RESOLVED 2026-06-04 by planning-artifact audit** — PRD, architecture, and epics now agree on INP <=200ms. No active action remains.

## Deferred from: code review of 10-2-sun-behind-clouds-two-signal-ui-state (2026-07-03)

- ✅ **Shared mapper `toSunStatusToken` is never consumed by any render surface (inert exhaustiveness guard)** *(Target: None — conditional; reactivate when either a render surface is refactored to route its DTO→UI-token decision through `toSunStatusToken`, OR the dead export is removed. Trigger of harm: a future `VenueSunStatus` member added at the DTO layer would NOT surface as a compile error at any render surface — the exact 10.1→10.2 silent-fall-through failure the mapper was meant to prevent, since every surface branches inline on `sunStatus === 'CloudObscured'` / `isObscuredSunStatus(...)` and hardcodes token strings instead)* — `nextjs-app/lib/utils/sun-status-presentation.ts:15`. Story 10.2 Task 1 advertised a `never`-exhaustive `toSunStatusToken` as "the single branch predicate every render surface uses," but a grep finds it consumed only by its own unit test; `VenueCard`/`VenuePin`/`VenueQuickInfo`/`VenueDetailContent`/`VenueList` each branch directly and hardcode `bg-pin-obscured`/`text-obscured-text` inline. `isObscuredSunStatus` and `skyConditionCopy` ARE used — only `toSunStatusToken` is orphaned. No runtime defect today (the surfaces were manually swept and render CloudObscured correctly); purely a Task-1 design deviation + dead additive export + an inert compile-time guard. Fix: either wire the mapper into each surface's token decision (realizing the advertised exhaustiveness benefit) or delete the unused export. Low, non-blocking. [Thin code review of Story 10.2] — resolved in Story 11.7: the orphaned `toSunStatusToken` export was deleted from `sun-status-presentation.ts` (the `never`-exhaustive guard now inherited by the surviving `windowLabelTier` switch), and `hygiene-config-contracts.automate.test.ts` (AC2) locks the deletion so it cannot silently return.

## Deferred from: code review of 9-7-tag-filtering-real-data-working-chips (2026-07-01)

- ✅ **Empty-state renders the loading skeleton instead of the "nothing matches" copy during a concurrent background refetch** *(Target: None — conditional; reactivate only if a user reports the 3-card skeleton flashing over a 0-match tag filter)* — both `<VenueList>` call sites pass `isLoading={venueQuery.isFetching && listVenues.length === 0}` (`nextjs-app/components/custom/map/MapView.tsx:926,970`) and `nextjs-app/components/custom/venue/VenueList.tsx:55` returns the loading skeleton BEFORE the `sortedVenues.length === 0` empty branch (`:70`). When a tag filter yields 0 matches while the underlying `useVenueSearch` query is `isFetching` in the background (e.g. a `keepPreviousData` planner-change refetch), the surface shows the 3-card skeleton rather than the `venue.list.empty` ("Inga platser hittades…") copy. In the primary/steady state (query idle) the empty copy renders correctly and the MapView tag-filter test asserts it, so this is a transient edge on pre-existing `isLoading` wiring reused unchanged — non-blocking. A fix would order the `length === 0` empty branch ahead of the `isLoading` skeleton (or exclude the tag-filtered-to-empty case from `isLoading`). [Thin code review of Story 9.7] — resolved in Story 11.3: both `VenueList` call sites now feed `isNearListLoading = venueQuery.isFetching && loadedVenueCount === 0` keyed off the PRE-FILTER loaded count (`MapView.tsx:830-831`), so a tag-filtered-to-empty list renders the `venue.list.empty` copy even during a concurrent background refetch; MapView tests assert the empty copy (not the skeleton) for filtered-to-empty while a genuine first load still shows the skeleton.

## Deferred from: code review of 11-9-venue-data-model-cleanup (2026-07-06)

- **[Med] ÖPPET badge + "Öppet till HH:MM" line have no is-open-now guard (intra-day + past-midnight)** *(Target: None — conditional; reactivate when a minute-precise is-open-now affordance is scoped, or if a live report shows the ÖPPET badge / "Öppet till" line asserting open when the venue is closed before `open` / after a past-midnight `close`)* — `nextjs-app/lib/utils/opening-hours.ts:838` (`formatOpeningHours`). The formatter derives `closesAt`/`display` from today's weekday entry whenever it is well-formed, with NO check that `now` falls within `[open, close]`: a venue opening 18:00 renders "ÖPPET · 22:00" at 09:00, and a past-midnight venue (opens 18:00 closes 02:00) genuinely open at 01:00 reads *today's* weekday row (not yesterday's still-open session) so it can show closed. This is EXACTLY the pre-existing "`closesAt` ÖPPET badge has no is-open guard" deferred item that Story 11.9 triggered — Dev Notes + Completion Notes knowingly narrow it: the weekday-correctness half is now solved (the badge reflects the CURRENT weekday), and a full minute-precise is-open-now guard was left OUT of scope (not trivial in the shared pure formatter; would risk the byte-identical visual gate). Not a regression this diff introduced; genuine honesty gap for an honesty-first app. If hardened, gate the derived `{ display, closesAt }` behind an `isWithin(now, interval)` check (and consult the prior weekday's past-midnight interval) so the badge/line hide when the venue is not actually open. [Code review Round 1 of Story 11.9; blind@primary+blind@secondary+edge@primary+edge@secondary+auditor@primary]
- **[Low] Closed-day + past-midnight opening-hours shapes are documented/unit-tested but never seeded on a live row or CI fixture** *(Target: None — conditional; reactivate if a launch venue with a genuinely closed day or a past-midnight close is added to the seed/fixtures, or on an edge-shape verification pass that seeds one such row so the "handled" claim is exercised end-to-end)* — `nextjs-app/docs/venue-data-load.md:68-77` + `_bmad-output/implementation-artifacts/11-9-venue-data-model-cleanup.sql`. The AC5 data-load doc example and the formatter unit tests both demonstrate a `null` closed day (Sunday) and a past-midnight close (`close: "02:00"` earlier than `open`), but all 7 seeded launch venues (SQL Section 4) and both sunny CI fixtures open 11:00 and close ≥19:00 on EVERY weekday with no `null` day and no past-midnight row. So the two most spec-emphasised edge shapes ("closed today" + "past-midnight handled") are verified purely in isolation — never on a live/seeded row the maintainer or running app renders. Not an AC violation: the seed's job is byte-identical gate-parity for `test-venue-sunny`, and the edge behaviour IS unit-tested; this is a verification-coverage note, not a runtime defect this diff introduced. If hardened, seed one venue (or add one fixture) with a closed day and a past-midnight close so the derived "closed → renders nothing" and "Öppet till 02:00" paths are exercised against a real row. [Code review Round 2 of Story 11.9; auditor@primary]

## Deferred from: code review of 9-9-mobile-venue-quick-info-card-rework (2026-07-01)

- **AC3 planner-clearance margin rests on fixed estimates unanchored to the live DOM** *(Target: None — conditional; RESOLVED-IN-PART by Story 9.10 — a live-DOM clearance assertion now exists; reactivate the constant-anchoring part only if the sun-% badge is reported jamming under the planner slider on a tall device or for a long-named venue)* — `nextjs-app/components/custom/map/MapView.tsx:165-169`. `MOBILE_PLANNER_HEIGHT_PX = 80` and `QUICK_INFO_MOBILE_HEIGHT_ESTIMATE = 170` are self-conceded approximations; the one-gutter (16px) AC3 clearance margin is eroded from either end if the real rendered `TimeSliderPanel` mobile height exceeds ~80px (tall common devices / calendar+label rows) OR a long venue name wraps the fixed-230px card taller than 170px. **Story 9.10 CLOSED the "assert only the arithmetic" half:** `test/e2e/epic-9-mobile-regression.spec.ts` now asserts, at the live iPhone-14 viewport, that the rendered quick-info card's top edge sits at/below the rendered `TimeSliderPanel`'s bottom edge — so a real overlap trips the mobile gate. The REMAINING (still-conditional) follow-up is only to anchor the two constants to a measured DOM height, if a real-device overlap is ever reported. [Thin code review of Story 9.9; live-layout guard added by Story 9.10]

## Deferred from: code review Round 2 of 8-2-real-venue-store-api (2026-06-18)

- **Supabase by-slug lookup matches `slug` only, not `venueSlug`** *(Target: None — conditional, only triggers if a `venue_slug` / alias / slug-history column is ever added to `public.venues`)* — `readSupabaseVenueBySlug` filters `.eq('slug', slug)` while the in-memory path (and the pre-8.2 route) matched `slug || venueSlug`. Behaviorally equivalent today: the `venues` table has a single `slug` column and `fromVenueRow` derives `venueSlug = slug`, so no request can match `venueSlug` without also matching `slug`. Two context-blind review layers rated this High; the full-context Acceptance Auditor re-confirmed it equivalent (also dismissed as noise in Round 1). Reactivate (and add the alternate-identifier match to the Supabase query) the moment a `venue_slug`/alias column or slug-history is introduced. [`nextjs-app/lib/services/venue-store.ts`]
*(One entry — opt-in Supabase venue-store pre-cutover verification — RESOLVED by Story 8.5 (Task 6.1/6.2) on 2026-06-22: `validateVenueUniqueness` aligned to the DB unique keys `id` + `slug` (the rounded-coordinate check removed so co-located distinct venues no longer 500 the list route); the mocked venue-store tests now assert the query contract (`.select(VENUE_SELECT_COLUMNS)` 20-col list + `.eq('slug', …)`). Live venue-data integrity is the maintainer cutover check (8-5-cutover-run-2026-06-22.md). Removed per the deferred-work convention.)*

## Deferred from: code review Round 1 of 8-5-production-config-security-hardening (2026-06-22)

- ✅ **`vercel.json` `installCommand` swallows a `lightningcss` install failure** — the pre-fix command was `npm install --include=dev && (cd .. && npm install --no-package-lock lightningcss@1.31.1 2>&1 || true)`, whose `|| true` swallowed native-package install failures; current `nextjs-app/vercel.json` removes the swallow tail, `nextjs-app/docs/vercel-deployment.md` mirrors the fail-loud command, and `nextjs-app/test/unit/hygiene-config-contracts.automate.test.ts` guards against `|| true`, `; true`, `|| :`, and `|| exit 0`. [`nextjs-app/vercel.json`, `nextjs-app/docs/vercel-deployment.md:154`] — resolved in Story 11.7 (`nextjs-app/vercel.json` and `nextjs-app/test/unit/hygiene-config-contracts.automate.test.ts`).
*(One entry — Met.no contact-of-record — RESOLVED by the maintainer on 2026-06-22: Rasmus supplied a real, monitored inbox `rasmus.thunborg@enhancior.se`, now wired as the `DEFAULT_USER_AGENT` fallback in `lib/weather/met-no-service.ts` (so even the unset-env fallback is TOS-compliant) and as the documented `MET_NO_USER_AGENT` example in `.env.example` / `docs/environment-variables.md` / `docs/vercel-deployment.md`; the two default-UA unit-test assertions updated to match. Removed per the deferred-work convention.)*
- ✅ **Line-ending (LF↔CRLF) churn inflates the 8.5 diff** — `lib/solar/types.ts` and several env/docs files had LF↔CRLF re-add churn that buried substantive `validAt` changes; current `.gitattributes` applies a scoped source/config/script/data `text eol=lf` policy with binary guards, and `nextjs-app/lib/solar/types.ts` is LF-only. [`nextjs-app/lib/solar/types.ts`] — resolved in Story 11.7 (`.gitattributes` scoped LF policy; `nextjs-app/lib/solar/types.ts` is LF-only).

## Deferred from: epic review of epic-9 (2026-07-01)

- ✅ **Buildings-cache key uses a re-implemented `polygonCentroid` that must stay byte-identical to the engine's internal `getCentroid`** — the prior independent centroid implementation is gone from `nextjs-app/lib/services/sun-engine.ts`; current compute-cache, day-series-cache, buildings-cache, and RPC coordinate paths use the shared `seatingCentroidWgs84(resolveVenueGeometry(...))` helper. [`nextjs-app/lib/services/sun-engine.ts:polygonCentroid`] — resolved in Story 12.3 (`nextjs-app/lib/services/sun-engine.ts` uses shared `seatingCentroidWgs84` for cache and RPC coordinates).
- **`localizeTag`'s 16-entry Swedish→English map must track the live DB `tags` vocabulary with no runtime signal on drift** *(Target: None — same "unverified metadata" class the epic targets)* — an unmapped live tag silently falls back to the raw Swedish string as a user-facing untranslated chip; the contract test only covers the closed 15-tag fixture set, not live DB values. Add a coverage assertion against the production tag vocabulary when a real tag audit is done. [`nextjs-app/lib/utils/venue-tags.ts`]

## Deferred from: epic review of epic-11 (2026-07-05)

- ✅ [x] [Review][Defer][Med] Query-key planner derivation is hand-duplicated across `useVenueSearch` and `useFavouriteVenues` with no shared helper — both hooks now consume the shared `deriveQueryKeyPlanner(planner?.date)` helper, which emits the date-only key fragment and strips `time` consistently. `[nextjs-app/hooks/queries/useVenueSearch.ts:94-100, nextjs-app/hooks/queries/useFavouriteVenues.ts:41-45]` — resolved in `nextjs-app/lib/utils/venue-query-planner.ts` and both hook consumers.

## Deferred from: post-dev test automation of 12-12-venue-photos-supabase-storage-hosting-render-fallback-fixes (2026-07-19)

- ✅ [x] **[Med] Protected live Supabase Storage verification** *(Target: None — conditional, triggered when protected Supabase credentials became available for Story 12.12 storage-policy verification)* — resolved 2026-08-17. Remote migration `20260817111301` applied the public, WebP-only, size-capped `venue-media` bucket contract. A bounded dynamic smoke proved service-role create-only card/hero upload, duplicate-key refusal, anonymous `200 image/webp` reads with SHA-256 parity, anonymous/authenticated write denial, delete no-op retention, service-role cleanup, and protected SQL `remaining_objects=0`; the temporary auth user and local fixtures were also removed. Evidence: `_bmad-output/test-artifacts/epic-12-protected-validation/protected-validation-report-2026-08-08.md` and Story 12.12's dated protected-smoke addendum. [Post-dev test automation of Story 12.12; bmad-testarch-automate]
