# Epic 11 Tier-B Integration Review — Triaged Findings

Review: **Epic 11 Tier-B integration review, 2026-07-05** — 48 lens files (blind /
edge-case / acceptance-auditor × primary + secondary, over 8 per-story chunks 11-1…11-8)
plus a whole-epic security review. Security reported **0 findings**. Epic trace gate PASSed
(P0 100%); suite green (vitest 1361; Playwright mobile 52 / desktop 35 / touch 3 / a11y 12).

Every story already passed an individual Tier-A review + security whose findings were fixed.
The blind lenses are spec-blind by design, so the bulk of their raw output is
already-handled-elsewhere / intentional-per-spec / test-noise and is dismissed below with the
category counts retained. Findings were normalized, deduplicated across chunks + reviewers
(merged severity = MAX), and classified. Only survivors are listed. Decision-first, then
Patch, then Defer.

### Review Findings

- [x] [Review][Decision][Med] Recenter mobile bottom padding omits the 52 px mobile nav-bar cover — the user dot lands ~52 px low at every non-dismissed snap — `computeRecenterPadding` mobile branch returns `bottom = SHEET_COVER_H[snap]` only, but the bottom sheet is positioned `bottom-[var(--size-mobile-nav-h)]` ABOVE the `fixed bottom-0` MobileNavBar (52 px + safe-area, `lg:hidden`). VERIFIED: the true obstructed band from the viewport bottom is `snapHeight + 52`, so AC3's "dot lands in the visual center of the unobscured area" under-shifts by the nav-bar height on mobile. `[nextjs-app/lib/utils/recenter-padding.ts:90]` Recommended: fix: add a `MOBILE_NAV_BAR_COVER = 52` (mirroring `--size-mobile-nav-h`) to the mobile `bottom` for every non-`dismissed` snap; keep `dismissed` at 0. Small, cheap correctness win; if the maintainer prefers to fold it into the Story-11.8 real-device recenter pass that is also defensible, but the offset is deterministic and worth fixing now.
- [x] [Review][Decision][Med] `applyDaySeriesDerivation` overrides only `currentSunStatus` + `sunExposurePercent`; `skyCondition` (the obscured sub-line) stays frozen at the server single-instant on a time scrub — scrubbing from a clear "now" step to a cloud-gated step flips the muted "Sol bakom moln" chrome on the DERIVED status while the plain-language sky phrase beside it still reads the stale server `skyCondition` (e.g. "Klart"), a self-contradicting obscured card. `confidence`/`sunWindow`/`peakTime` are likewise frozen but are lower-stakes (confidence is sr-only post-11.4; sunWindow/peakTime are not rendered on quick-info). `[nextjs-app/components/custom/map/MapView.tsx applyDaySeriesDerivation]` Recommended: fix: carry `skyCondition` on `VenueDaySeriesEntry` and override it in `applyDaySeriesDerivation` so the obscured sky sub-line tracks the scrub (parity with the status it sits beside). If the maintainer judges the honesty-regression risk acceptable because the status chrome already flips, this can be deferred — but the clear→obscured self-contradiction is exactly the Epic-10 honesty class, so fixing is preferred.
- [x] [Review][Defer][Med] ~~auto-resolved: defer (triage recommendation, epic mode)~~ `enforceWindow: false` server opt-out vs. client date-clamp disagree on the authoritative date for a stale/bookmarked far-future deep link — `/api/venues?date=today+11` succeeds (route serves season-clamped-only data), while the same date via the app's `?_date=` param is clamped back to today by `stateFromForcedPlanner`; the stale `?date=` lingers in the URL with no redirect/replace, so the URL "lies" about the shown state. Intentional split per the 11.2 maintainer decision; no functional break, no AC requires agreement. `[nextjs-app/lib/services/venue-planner.ts parseVenuePlannerParams / nextjs-app/lib/contexts/TimeContext.tsx stateFromForcedPlanner]` Recommended: defer: accepted design per Story 11.2; low real-world incidence (only a hand-edited/stale bookmark hits it). Optional hardening (URL `replace` to the clamped date) can ride the Story-11.8 live-verification pass — not worth a code change now.
- [x] [Review][Decision][Med] `TimeSlider` drag-decouple reads `isDragging` from rendered `dragValue` state, not a synchronous ref — if a browser dispatches the range input's first `change` in the same event turn as `pointerdown` (before React commits `dragValue` null→number), `handleChange` reads a stale `isDragging === false` and takes the discrete-tap commit branch, reintroducing the per-step commit the story kills. jsdom flushes a render between the two events so the ATDD passes, masking a possible real-device double-commit. `[nextjs-app/components/composed/time/TimeSlider.tsx onPointerDown/handleChange]` Recommended: fix: back the drag flag with a synchronous `isDraggingRef` set in the pointerdown handler (used by `handleChange`), keeping `dragValue` state for render only. Cheap robustness fix; if the Story-11.8 real-touch `--project=touch` gate is trusted to have exercised the real pointer ordering on Pixel-5 and stayed green, deferring is tenable — but a ref is the correct decoupling primitive and removes the ordering dependency entirely.
- [x] [Review][Defer][Low] ~~auto-resolved: defer (triage recommendation, epic mode)~~ `closesAt`-gated "ÖPPET · {time}" badge has no is-currently-open guard — it renders "OPEN · {time}" purely on truthiness of `openingHours.closesAt`, so a venue that is currently closed but whose DTO still populates `closesAt` shows "ÖPPET" incorrectly. `[nextjs-app/components/composed/venue/VenueDetailContent.tsx:163-171]` Recommended: defer: today's store only populates `closesAt` for the current open interval (the badge copy is honest for present data), and there is no `isOpenNow` signal on the DTO to gate against; adding one is a data-layer change out of Epic 11 scope. Revisit if/when live rows begin carrying non-current `closesAt`.

- [x] [Review][Patch][Low] `a11y` CI-wiring guard regex matches `a11y-mobile` (word-boundary bug) — `/--project=a11y\b/` matches `--project=a11y-mobile` because `\b` sits between `y` and `-` (VERIFIED empirically: `.test('--project=a11y-mobile') === true`). Currently benign (workflow line 123 has a literal `--project=a11y`), but a future edit swapping `a11y`→`a11y-mobile` would pass this "gate" vacuously while dropping the desktop axe run. `[nextjs-app/test/unit/epic-11-standing-gate-ci-wiring.automate.test.ts:80]`
- [x] [Review][Patch][Low] `DatePickerDialogLabels.windowDate` is optional-with-fallback to `unavailableDate` — a beyond-window but in-season date renders "Datum utanför säsong" (wrong copy) for any consumer that omits the new key; only the one production caller wires it, so the type system does not catch a future omission. `[nextjs-app/components/composed/time/DatePickerDialog.tsx:149-153]` (make `windowDate` required and update the single caller so the AC3 copy fix is un-regressable)
- [x] [Review][Patch][Low] `addDaysToDateKey` throws `RangeError` on a malformed date key — destructuring defaults yield `Number('abc') = NaN`, `Date.UTC(NaN,…)` = Invalid Date, `.toISOString()` throws; unlike its sibling `isValidDateKey`/`isPlannerDateSelectable` it has no guard. Only fed `stockholmDateKey(now)` (always valid) today, so latent, but an exported util that throws on bad input is a footgun. `[nextjs-app/lib/utils/time-planner.ts addDaysToDateKey]` (add an `if (!isValidDateKey(date)) return date;` guard at the top)

- [x] [Review][Defer][Low] `formatPeakHour` is a dead/orphaned export after 11.6 removed its sole caller — VERIFIED zero repo-wide callers outside its own definition. `[nextjs-app/lib/utils/venue-visual-metadata.ts:227]` — deferred, same dead-export-cleanup class as the already-tracked `windowLabelTier`/`isSunWindowStatus`; behaviour-inert, no lint break (unused export ≠ tsc error). Roll into the next hygiene sweep alongside the tracked `windowLabelTier` candidate.
- [x] [Review][Defer][Med] Query-key planner derivation is hand-duplicated across `useVenueSearch` and `useFavouriteVenues` with no shared helper — VERIFIED both build an equivalent `keyPlanner = date ? { date } : undefined` and both correctly strip `time`, but a future story editing one and not the other could silently reintroduce a `time`-keyed fetch in only one hook (the R-001 zero-fetch invariant is proven by two independently-written branches). `[nextjs-app/hooks/queries/useVenueSearch.ts:94-96, nextjs-app/hooks/queries/useFavouriteVenues.ts:41-42]` — deferred, no current defect (invariant holds in both); extract a shared `deriveQueryKeyPlanner` helper before the next story builds on this seam.
- [x] [Review][Defer][Low] Quick-info for a venue absent from both the Närmast list and favourites rows (deep-link `?venue=<slug>` or out-of-radius selected preview) is sourced from the `[slug]` detail DTO, which by design carries no `sunDaySeries`, so its `%`/status freeze against a same-date scrub. `[nextjs-app/components/custom/map/MapView.tsx selectedVenuePreviewForMap merge]` — deferred, accepted boundary (the detail route intentionally has no series); AC1's "ANY planner time" has a documented detail-view exception. Only the pure out-of-list preview freezes; in-list selections track correctly.
- [x] [Review][Defer][High] Recenter `flyTo` padding is never clamped against the actual canvas size — `full` snap sums `top 72 + bottom 560 = 632 px` vertical, and desktop sums `left 340 + right 390`; on a short/narrow viewport `padding.top+bottom` (or `left+right`) can approach/exceed the map dimension, where MapLibre `flyTo` can mis-center or degenerate. `[nextjs-app/lib/utils/recenter-padding.ts / nextjs-app/components/custom/map/MapControls.tsx]` — deferred, no reproduced failure (only the two most-obstructed snaps on the smallest devices approach the bound) and the whole recenter-framing is slated for the Story-11.8 real-device pass; add a viewport-relative clamp there rather than guessing device bounds now.
- [x] [Review][Defer][Med] `isDesktopViewport()` is a one-shot non-reactive `matchMedia` read at fly-time — a resize/rotation crossing the 1024 px breakpoint between the geolocation request and its resolution computes padding for the wrong layout; no `matchMedia` change listener. `[nextjs-app/components/custom/map/MapControls.tsx]` — deferred, narrow race window (mid-geolocation rotation) with a self-correcting next recenter; fold into the Story-11.8 device recenter checklist.
- [x] [Review][Defer][Low] `MOBILE_TOP_BAR_COVER = 72` is applied unconditionally and is the sole padding input NOT derived from a mirrored `--size-*` token — the top search bar is a variable-height dismissible card, so 72 can over/under-pad the top across states. `[nextjs-app/lib/utils/recenter-padding.ts:48]` — deferred, within AC3's "small top if it materially covers" latitude and already logged Low in the story; token-hygiene candidate for a future pass.
- [x] [Review][Defer][Low] The four hardcoded slider/sheet gesture magic numbers (`DRAG_TO_COLLAPSED_PX 64`, `DRAG_TO_PEEK_PX 96`, `FAST_SWIPE_VELOCITY`, drag `bounds.bottom 320`) and the recenter `collapsed: 44 + 24` safe-area guess are tuned-by-feel with the ordering/derivation only informally justified; correctness rests on the `--project=touch` CDP e2e's fixed-delta drags, not a unit test pinning the threshold ordering. `[nextjs-app/components/custom/sheets/MobileBottomSheet.tsx / nextjs-app/lib/utils/recenter-padding.ts]` — deferred, behaviour is green under the real-touch gate; a threshold-ordering unit test + safe-area empirical check are hardening for a future gesture-tuning story.
- [x] [Review][Defer][Med] The new standing-gate CI-wiring guard (`epic-11-standing-gate-ci-wiring.automate.test.ts`) validates config purely by regex over raw YAML/TS text — it can false-pass on commented-out wiring (`# npx playwright test … --project=touch` still matches), false-fail on benign flag reordering / line-wrapping, and its `use:\{[^}]*\}` project-block matcher truncates at the first nested brace (`viewport: { … }`). `[nextjs-app/test/unit/epic-11-standing-gate-ci-wiring.automate.test.ts]` — deferred, the guard currently matches the live config and is a net-positive tripwire; hardening it to parse YAML/TS (or match balanced braces / anchor per-`name:` block) is a test-quality follow-up, not a shipped defect.
- [x] [Review][Defer][Med] The regressed real-touch pin-select hardening (`epic-11-sheet-touch-gestures.spec.ts`) replaces a ~66%-flaky center-tap with a bounded 4-attempt retry whose correctness hinges on a 1200 ms poll vs. render race (a late-registering tap can be re-tapped and toggled off), an unscoped `[data-pin-state*="selected"]` locator (matches ANY selected pin, not the tapped one), and a `box.height * 0.35` re-aim magic constant untied to the real pill geometry. The underlying ~66% miss rate is a genuine marker hit-target UX observation answered only test-side. `[nextjs-app/test/e2e/epic-11-sheet-touch-gestures.spec.ts]` — deferred, the spec is green and dispatches a genuine touchscreen tap (real-touch acceptance intact); scope the success locator to `targetPin`, derive the aim from the pill sub-element box, and file the marker hit-target as a UX item for the Story-11.8 real-device pass.

#### Iteration 2 (2026-07-05)

Re-review of the CHANGED window (Story 11-8 verification + epic-end gate docs + the six iteration-1 fixes) with the same 6-lens roster + security (security: 0 findings). The bulk of the fresh output re-critiques the now-landed iteration-1 fixes (all verified correct + all four locked invariants INTACT by both auditors) or re-raises items already recorded above — dismissed as already-tracked/noise. Two spec-blind High claims were VERIFIED against source and fall: H1 "`skyCondition: string` breaks assignability" is a FALSE POSITIVE (`VenueDataDto.skyCondition` is itself `?: string` at `lib/types/api.ts:92`, so the override is trivially assignable and the suite is green), and H3 "required `windowDate` breaks an unproven caller" is a FALSE POSITIVE (the sole caller `TimeSliderPanel.tsx:119` wires it and both i18n files carry the key). Two genuinely-new, concrete-but-minor items survive as Defers.

- [ ] [Review][Defer][Med] `applyDaySeriesDerivation`'s "skyCondition-only-change" branch is real-data-reachable but has NO direct unit test — the equality gate now returns a NEW object when `currentSunStatus` + `sunExposurePercent` are unchanged yet `skyCondition` differs (the exact self-contradiction the iteration-1 fix targets). VERIFIED reachable: the cloud GATE fires only at `cloudCover >= CLOUD_GATE_THRESHOLD_PERCENT` (>60) while `skyConditionFromCloudCover` buckets split at 20/60, so two adjacent geometrically-sunlit steps can share `Sunny` + equal exposure yet read `clear` vs `partly-cloudy`. The code is correct (the added `&& nextSkyCondition === venue.skyCondition` term handles it) and the behaviour is auditor-verified, so this is a COVERAGE gap, not a shipped defect. `venue-day-series.edge.test.ts` exercises `deriveVenueSunAtMinutes` (the field carrier), and `sun-engine.day-series-parity.atdd.test.ts:202` asserts field equality at only ONE requested step — omitting the rain-gated and weather-`undefined` ("unavailable") branches. `[nextjs-app/components/custom/map/MapView.tsx applyDaySeriesDerivation / nextjs-app/test/unit/services/sun-engine.day-series-parity.atdd.test.ts]` — deferred: add a direct `applyDaySeriesDerivation` skyCondition-only-change assertion (extract/export or test through MapView) and widen the parity test to the `rain`/`unavailable` skyCondition branches; no code change needed.
- [ ] [Review][Defer][Low] `TimeSlider` `isDraggingRef` can stick `true` if pointer capture is lost mid-drag with no `pointerup`/`pointercancel` (OS gesture interrupt / capture loss / unmount), so the next unrelated `change` on the still-mounted instance is misread as still-dragging and silently drops a commit — the ref is the correct iteration-1 decoupling primitive but adds an end-of-drag reset path the `onPointerUp`/`onPointerCancel` handlers do not fully cover. No reproduced failure (the `--project=touch` gate is green). `[nextjs-app/components/composed/time/TimeSlider.tsx onPointerDown/endDrag]` — deferred: add `onLostPointerCapture={endDrag}` and a one-line comment tying the `isDraggingRef`/`dragValue` lifecycles together (the two-source-of-truth split is subtle and only the happy-path sequence is asserted); hardening for a future gesture pass, not a shipped defect.

#### External review (Codex, PR #17) — 2026-07-05

Codex flagged 8 findings against `epic/11-feels-instant-reads-clear`. Each was VERIFIED against current source; all 8 were CONFIRMED real and FIXED (no false positives). Gates after fixes: `tsc --noEmit` clean; eslint clean on all changed files (0 errors); FULL vitest **1385/145 green** (baseline 1365/143 + 2 new test files + net new tests); Playwright green — scrub-zero-fetch 4 (desktop+mobile), slider-touch-drag 1 (touch), map-primary 5 (desktop), chip-filter-parity 2, sheet-touch-gestures 2 (touch). All locked invariants (scrub=0/date-change=1, seed-path byte parity, one-commit-per-gesture, FLOOR today-min, no `time` in query keys) intact.

- [x] [Review][Patch][Med] Query-key planner derivation diverged between callers — the always-mounted `DesktopNavBar` (and `VenueSearchShell`) spread the raw `plannerTime.plannerQuery` (undefined on live-today → the `list` key) while `MapView` passed `isLiveNow` + date (→ the `planner` key), so on live-today the nav was on a DIFFERENT key and, on the FIRST scrub away from live, flipped `list`→`planner` and fired a hidden second `/api/venues` request mid-scrub (breaks the scrub=0 invariant). VERIFIED by tracing all 3 callers + the two hooks. `[nextjs-app/hooks/queries/useVenueSearch.ts:94-101]` — fixed: extracted the shared `venuePlannerQueryArgs` (caller-side, used identically by all 3 callers so they are un-divergeable) + `deriveQueryKeyPlanner` (hook-side, used by BOTH hooks) into `lib/utils/venue-query-planner.ts`; pinned with `venue-query-planner.test.ts` (a caller omitting `isLiveNow` stays on ONE date key across live→scrub; time never in the key).
- [x] [Review][Patch][Med] `MapControls` recenter-fly effect re-fired on obstruction changes — `mobileSheetState` + `isVenueDetailOpen` were effect deps, so after a geolocation success ANY sheet-snap change or detail-panel open re-ran `flyTo` and yanked the map to the user location with no locate action. VERIFIED by reading the effect deps. `[nextjs-app/components/custom/map/MapControls.tsx:145-150]` — fixed: obstruction state held in refs (synced in a dedicated effect, never written during render — react-hooks/refs), read at fly time; the fly effect deps are now only `[geolocation.status, geolocation.coords, mapInstance]`. Tests: snap change AND detail-panel open after success do NOT re-fly.
- [x] [Review][Patch][Med] Collapsed bottom-sheet body was `aria-hidden`+`pointer-events-none` but its children stayed TABBABLE (keyboard users tab into visually-clipped controls). VERIFIED. `[nextjs-app/components/custom/sheets/MobileBottomSheet.tsx:231-235]` — fixed: added the React 19 boolean `inert` prop to the collapsed body (kept `aria-hidden` for AT parity); the handle sits outside the body so it stays focusable. Test asserts the collapsed body has the `inert` attribute + the handle keeps focus; peek body is not inert.
- [x] [Review][Patch][Med] Server sorted by single-instant sun then `.slice(0, MAX_RESULTS=50)`, so with >50 matches a venue outside the instant top-N that becomes sunniest at a scrubbed time was truncated before the client (which derives scrubbed steps from the cached `sunDaySeries`) ever saw it — incomplete pins/"Mest sol" for that time. VERIFIED (`sunListRank` reads instant fields; truncation is before nothing recovers it client-side). Current live store has 7 venues (<50) so no live impact, but the guard is correct for scale. `[nextjs-app/app/api/venues/route.ts:371-380]` — fixed: two-stage selection — TRUNCATE by the venue's DAY-PEAK rank (`venuePeakSunRank`, max over `sunDaySeries`, no payload growth), ORDER by the single-instant rank (client re-sorts anyway). Seed/fixture path has no series → peak == instant → byte-parity + identical order/truncation preserved. Regression test (`venues-route-peak-truncation.test.ts`): a low-instant/high-peak venue survives the top-50 slice; response order stays instant-based.
- [x] [Review][Patch][Med] Chip-strip scroll-arrow buttons were `size-9` (36px), below the repo's 44px interactive minimum. VERIFIED. `[nextjs-app/components/custom/layout/DesktopNavBar.tsx:319-321]` — fixed: `size-11` (44px), icon `size-5` unchanged. Test pins `size-11` (not `size-9`) on both arrows.
- [x] [Review][Patch][Low] Chip-strip arrow `scrollBy` + the scroller CSS `scrollBehavior` were unconditionally `smooth`, ignoring prefers-reduced-motion. VERIFIED. `[nextjs-app/components/custom/layout/DesktopNavBar.tsx:229-235]` — fixed: gate both on `useReducedMotion()` → `auto` for reduced-motion users (+ `motion-reduce:scroll-auto` CSS backstop). Tests assert `smooth` (default) and `auto` (reduced motion) on both `scrollBy` and the inline style.
- [x] [Review][Patch][Med] `TimeSlider` native range input had `min={effectiveMin}` while the custom thumb/progress render against the full 06:00–21:00 track, so with an active today-min the native pointer-coordinate mapping disagreed with the visuals (grabbing the visible thumb selected the wrong time); the forced-`?_time=` e2e disables the today-min so it never caught this. VERIFIED (`progressPercent` anchors at PLANNER_START; native `min` was the effective min). `[nextjs-app/components/composed/time/TimeSlider.tsx:161-166]` — fixed: native `min={PLANNER_START_MINUTES}` (geometry matches visuals); the below-min FLOOR is still enforced by `handleChange`/`commit` clamps AND the TimeContext state-layer `clampBelowStateMin`; `aria-valuemin` keeps the effective min for AT. Behaviour tests still assert "cannot select below min"; attribute assertions updated to the new contract; new geometry-mismatch regression test. Touch drag e2e green (one-commit-per-gesture intact).
- [x] [Review][Patch][Low] `planner-date-next` stayed enabled at the today+3 window end and silently no-op'd (dead control for keyboard/SR). VERIFIED (`shiftSelectedDate(1)` clamps to the window). `[nextjs-app/components/custom/time/TimeSliderPanel.tsx:138-143]` — fixed: `disabled` + `aria-disabled` when `isPlannerDateSelectable(addDaysToDateKey(selectedDate, 1), currentTime)` is false; dimmed + cursor-not-allowed. From today (and +1/+2) the next date is in-window → stays enabled, so the scrub-zero e2e (clicks it from today) is unaffected (verified green). Boundary tests: disabled at today+3, enabled at today/today+2.

---

## Dismissed (noise) — 71 total; counts retained by category

- **Already-handled by the shared combiner / already Tier-A-reviewed (parity path): 6** — empty-forecast "whole-day ungating" (`effectiveCloudCover(null)→undefined` is the intended geometry-only Epic-10 path, fed through the same `gatedStepValue`/`applyCloudGate` shared by the single-instant field; VERIFIED); per-step nowcast "N serial awaits" (relies on the batch-dedup fetcher, intended); `nearestForecastSlice` staleness/tie-order (shared with single-shot); byte-parity-via-`gatedStepValue` (VERIFIED single shared combiner); degraded-buildings series (mirrors single-instant degrade); `normalizeVenueForResponse` re-attach safety (attaches after normalize, detail route never reads it).
- **False positive (VERIFIED against source): 4** — `formatLabel`/`routeText` "dead after 11.4" (used at MapView.tsx:1573/1636/1647 and 851/864/928); both e2e describes "still `.skip`" (VERIFIED plain `test.describe` at scrub L153 / slider L154, headers correctly say LIVE); `VenueDetailDto` optional→required override "unverifiable/missing" (extends VenueDataDto, typechecks); `deriveVenueSunAtMinutes` 24*60/NaN "contradicts clamp" (guards Array.isArray+length+snap+exact-find, returns null cleanly).
- **Already tracked / recorded elsewhere (do-not-re-raise per brief): 9** — `windowLabelTier`/`isSunWindowStatus` dead-export cleanup candidate; the maintainer PNG-blessing / consolidated rebaseline handoff; live p95 (AC3 wall-clock) post-merge measurement; real-device / physical-phone (AC1) device-sweep handoff; stale RED-PHASE header cleanups (already done); `a11y-mobile` deliberately-not-CI-wired decision; the 11.6 centered-reviews vs left-aligned-reference tension (dev-deferred Low); the `.prettierignore` "16 not 15 files" renorm count (prior-review Defer); the stale test-count accounting in the story record.
- **Intentional-per-spec / by-design boundary: 8** — detail-DTO omits `sunDaySeries` (Task 2 mandate); `enforceWindow:false` route opt-out (11.2 maintainer decision, also surfaced as an open Decision above); confidence text moved sr-only (11.4 AC); `sunWindow` field retained though quick-info stops rendering it (list card still consumes it; separate surface); `--project=touch` CDP is Chromium-only, WebKit can't drive it (honest documented limitation); reduced-motion static halo (intentional); `NextDayButton` season-clamp no-op at today+3 (context enforces the cap); CDP-touch specs excluded from mobile/desktop/a11y projects (intentional routing).
- **Test-quality preference (no concrete defect): 18** — source-scan tests using `process.cwd()`/`readFileSync` cwd fragility (×5 files); regex brittleness of the various hygiene/CI-wiring/label-prune guards beyond the two kept above; `@ts-expect-error` closesAt-only test load-bearing directive; `toBe` reference-equality assertion on `openingHours`; loose `toBeLessThanOrEqual(1)` commit bound; `makeVenue` default flips opening-hours on; substring i18n-prune scans; `ERROR_SWALLOW` regex narrowness; `void browserName` vestige; duplicated `matchMedia` mock; `waitForTimeout(400)` in the touch e2e; payload-ceiling 8000 B "4.5× headroom" loosies; and similar assertion-shape nits.
- **Design-gate-eyeball / no automated-legibility-artifact (expected for design stories): 7** — AC1 basemap legibility has only opacity-number guards (`maxAlpha<=0.05`, `bg-surface-sand<=25`) not a contrast/axe assertion; de-dull vs pin/dot separation uncovered; halo `inset`/keyframe-motion not asserted; contrast claims (`#5c4300 ~5.6:1`, `text-text-body` AA) asserted in comments only; elapsed inert-segment width/placement no visual guard pre-11.7; user-dot vs venue-pin distinctness unguarded; skeleton `w-24` layout-jump guess. All ride the deferred human visual-validation checkpoint by design.
- **Low nits below the concrete-defect bar (no realistic trigger): 19** — comment/doc drift and story-tag coupling (R-001/R-005/Story-11.x cross-refs, DESIGN.md stale timeline-bar row, `met-no-service` Gothenburg-literal/temperature-`?? 0` in an EOL-only file); duplicated magic literals across fixtures; redundant `<span>`/`display:contents` wrappers; nested-`<nav>` landmark labelling; `?? 0` dead-defensive fallbacks over exhaustive `Record`s; `bg-drag-handle` token reuse for the elapsed band; `color-mix(… 0%, transparent)` no-op token; `will-change` omission; smooth-scroll arrow flicker; ResizeObserver content-box vs border-box; `MapControls` default-`'mid'` prop convenience; `text-text-body`/`contents` anchored-mode sizing; whitespace-only `openingHours.display`; midnight-rollover series-day race; and similar.
