# Deferred Work

Items intentionally not fixed in the story that raised them. Each entry links back to its source review/audit and the reason it was deferred.

> **Convention (binding — see CLAUDE.md §"Critical rules", added 2026-05-02):** every entry MUST carry a `*(Target: <story-id> — <story-title>)*` tag (or `*(Target: None — conditional, only triggers if <X>)*` for items whose trigger is a future state change). The agent adding the entry MUST simultaneously update the target's source artifact: if the target story is still `backlog`, append the item as a Given/When/Then AC or footnote in `_bmad-output/planning-artifacts/epics.md` under that story; if the target story already has a story file, append it to `## Tasks / Subtasks` or `### Notes`. When the target story is later drafted via `bmad-create-story`, the SM removes the entry from this file once the AC has been carried in. **`deferred-work.md` is a queue, not an archive — every entry has a target, and tagged entries leave the file when their target story is drafted.** Untagged entries are blocked at code-review time and flagged for explicit triage at the next sprint-planning pass.

## Deferred from: code review of 11-3-mobile-tag-filtering-bottom-sheet-overhaul (2026-07-05)

- **[Med] AC4 arrow/overflow reachability proven only with jsdom-mocked scroll metrics; no real-overflow integration proof** *(Target: None — conditional; reactivate on a real-browser e2e hardening pass for the desktop chip strip, or if a live viewport-width report shows a tag unreachable behind the arrows/edge-fades)* — `nextjs-app/test/components/DesktopNavBar.test.tsx` (AC4 chip-strip tests). Every AC4 test drives `Object.defineProperty(strip, 'scrollWidth'/'clientWidth'/'scrollLeft', …)` + a synthetic `scroll` event because jsdom reports 0 for all scroll geometry; `epic-11-chip-filter-parity.spec.ts` runs on desktop but only toggles a chip — it never exercises the arrows/edge-fades/overflow. So "all tags reachable at any viewport width" is asserted via mocked geometry, not observed real overflow, on exactly the surface whose original bug was a real mid-chip overflow clip. Implementation is correct and unit-covered; this is coverage adequacy, not a defect this diff introduced (pre-existing jsdom limitation). If hardened, add a real-browser (Playwright) desktop spec that renders a genuinely overflowing chip set and asserts a right-arrow click reveals a previously-clipped chip and the arrow disables at the end. [Thin code review of Story 11.3; auditor@primary]

## Deferred from: code review of 11-2-time-slider-drag-fix-planner-range-rules (2026-07-04)

- **[Low] Keyboard commit during an un-released pointer drag leaves a stale `dragValue`** *(Target: None — conditional; reactivate only if a real user reports the slider thumb visually jumping back after using arrow/Home/End keys while a finger/pointer is still held down, or on a drag-state hardening pass)* — `nextjs-app/components/composed/time/TimeSlider.tsx:158-175`. The keyboard handlers call `commit(...)` (fires `onMinutesChange`) but do NOT clear `dragValue`; if a `pointerDown` set `dragValue` and the user presses an arrow/Home/End before `pointerUp`, `isDragging` stays true, so a subsequent `onChange` keeps writing the local value and the committed keyboard value can be visually overwritten until a `pointerUp`/`pointerCancel` reconciles. Uncommon interleaved pointer+keyboard sequence; self-heals on the next pointer settle; no test exercises it. If hardened, have the keyboard `commit(...)` path also clear `dragValue` (or gate the keyboard handlers on `!isDragging`) so the keyboard-settle path is fully decoupled from an in-flight drag. [Thin code review of Story 11.2; auditor@primary]

## Deferred from: epic review of epic-10 (2026-07-03)
- [Med] Rain-gated outcome cached 15 min despite ~5-min radar cadence — auto-deferred at epic E_review (epic mode, triage recommendation): accept one-bucket staleness as consistent with the existing sun-freshness horizon; exclude isNearNow rain outcomes from the compute cache only if live spot-checks show the stale-rain window is user-visible.

- **[Med] The epic's "confidence drops with cloud" (FR12 / 10.1 AC3 / 10.5 AC4) is not observable to real users** *(Target: None — conditional; reactivate when a venue cluster ships `status:'eligible'` (or the coverage cap is raised) so `applyShadowDataCoverageCap` no longer clips both the clear and 100%-overcast displayed confidence to 60 — matches 10.5 maintainer follow-up #3)* — `nextjs-app/lib/solar/shadow-data-coverage.ts:182`. The FR12 `cloudConfidenceFactor` term fires in `calcCloudCertainty`, but `applyShadowDataCoverageCap` clips the DISPLAYED value to `0.6` for every venue whose cluster ships `status:'unknown'` (`CONSERVATIVE_CLUSTER_COVERAGE`), so both the clear-sky and 100%-overcast outcomes display 60 for today's venues. Story 10.5 surfaced this ("expected 60 to be less than 60"), re-homed the AC4 assertion from the displayed-engine layer to the calculator layer, and logged it as maintainer follow-up #3. Real but knowingly-postponed partial delivery of the epic's user-facing intent, not a regression this diff introduced. [Epic-10 integration review; auditor@primary] [Maintainer decision 2026-07-03: keep the Story-3.0.5 coverage cap as-is — deliberate honesty mechanism for unvalidated venues; the future fix should apply the cloud factor AFTER the cap (multiplicative, can only lower displayed confidence, never raise) so FR12 becomes user-visible without overclaiming; revisit in a follow-up epic run if user feedback shows confusion.]
- **[Low] Orphaned i18n keys `quickInfo.obscuredPosition` / `detail.obscuredPosition`** *(Target: None — conditional; reactivate when VenueQuickInfo/VenueDetailContent are wired to consume `obscuredPosition` (mirroring VenueCard), or on an i18n-cleanup pass to remove the dead keys)* — `nextjs-app/messages/sv/venue.json:26` (+`:169`, mirrored en). The keys are defined in both locales but no component consumes them: `MapView.tsx:1330,1361` wire only `obscuredHeadline` into the quick-info/detail labels; `obscuredPosition` is consumed only by `VenueCard` (`:135`) and `VenueList` (`:144`). Dead translation keys can silently drift from the wired `list.obscuredPosition` copy with no test or type catching it. Latent maintainability drift, no user-facing defect. [Epic-10 integration review; edge@secondary]
- **[Low] Obscured thumbnail badge still renders the literal "SOL" token next to the muted cloud chrome** *(Target: None — conditional; reactivate on a copy-consistency pass to reframe the badge token to the obscured "solläge / sun position" wording used elsewhere)* — `nextjs-app/components/composed/venue/VenueQuickInfo.tsx:446` (+`VenueCard.tsx` thumbnail badge). Under the gate the photo-strip badge mutes to `bg-pin-obscured` + a `Cloud` icon but still renders `{sunExposureText} SOL` (the `SOL` string sits outside the `isObscured` branch), so an obscured card's badge reads e.g. "95 SOL" in slate. Borderline vs Story 10.2 AC1 ("no FULL SOL / DELVIS SOL"): this is the numeric photo pill, not the FULL SOL status label, and the muted hue + Cloud icon make the state unmistakable, but the residual "SOL" text is inconsistent with the "solläge / sun position" reframing applied everywhere else. Cosmetic, no functional defect. [Epic-10 integration review; auditor@primary+edge@secondary]
- **[Low] `nearestToNowEntry` has no staleness/future cap on the selected nowcast slice** *(Target: None — conditional; reactivate if live spot-checks ever show a badly-shifted or degraded nowcast timeseries surfacing stale/future rain, or on a nowcast-robustness hardening pass)* — `nextjs-app/lib/weather/nowcast-service.ts:110-125`. The helper returns the entry with the minimum `|t - now|` (falling back to `timeseries[0]` when all timestamps are unparseable) with NO constraint that the chosen slice is at/before now or within a freshness window, so a stale-nearest or future radar step could be trusted as "raining now". Under normal operation the nowcast/2.0 product starts at ≈now at ~5-min cadence so the nearest entry is fresh; only reachable under a severe upstream shift (whole timeseries 40+ min off) or a degraded response whose only entries are hours away — largely mitigated by the `revalidate:60` HTTP TTL and the `radar!=='ok'` coverage guard (`:88`), and rain is a one-way additive gate that never gates Shaded/NoSun. Genuine-but-minor robustness gap, unrealistic trigger, bounded consequence. If hardened, add a max-staleness bound so a slice more than a few minutes from now degrades to `undefined`/unknown (non-gating) rather than "raining now". [Epic-10 integration review iteration 2; blind@primary+blind@secondary+edge@primary]
- **[Low] `applyCloudGate`'s `isRaining = false` default silently drops the rain signal for any future caller that forgets to thread it** *(Target: None — conditional; reactivate on a type-safety hardening pass, or if a new `applyCloudGate` caller/refactor is added)* — `nextjs-app/lib/services/sun-engine.ts` (`applyCloudGate` signature). The load-bearing rain flag defaults to the "don't gate" value, so a new production caller (or a `computeRealSunEngineResult` refactor) that omits `isRaining` compiles cleanly and silently degrades to "never rains" — a false-negative "it's sunny during rain", the worst outcome for an honesty-first app — with zero type/compile error. The sole current caller threads it correctly (rain gate is one-way/additive, verified), so no live defect today; latent hardening gap only. If hardened, make `isRaining` a required parameter and update the legacy 3-arg pure-helper test call sites to pass `false` explicitly so a future omission becomes a compile error. [Epic-10 integration review iteration 2; blind@primary+blind@secondary]

## Deferred from: code review of 10-5-weather-reality-verification-pass-regression-guards (2026-07-03)

- **[Med] AC1 e2e matrix does not differentiate scenario 2 (clear) from scenario 3 (high-cirrus-only) at the presentation layer** *(Target: None — conditional; reactivate only if presentation-layer e2e distinction between clear and high-cirrus-only is wanted, e.g. if a future UI surfaces a distinct sky line for cirrus)* — `nextjs-app/test/e2e/epic-10-weather-matrix.spec.ts:120`. Both non-obscured DTOs render byte-identically (amber Sunny, `95%`, no obscured chrome; the sky line renders only inside the `isObscured` block), so the matrix would still pass if `high-cirrus-only` were dropped or set to `skyCondition:'clear'`. The "thin cirrus ≠ blocking stratus" 10.3 differentiator IS guarded at the engine layer in the net-new `sun-engine.two-signal-invariants.atdd.test.ts` (high-cirrus-only NOT obscured vs overcast obscured), exactly where the story states the gate is already exhaustively unit-tested and mocking at the DTO boundary is the right altitude. The residual presentation-layer non-distinction is correct-by-design (the app intentionally shows both as amber Sunny). Non-blocking; minor e2e presentation-coverage gap. [Thin code review of Story 10.5]
- **[Low] Shared no-live-Met.no fetch guard `afterEach` never restores the original fetch, so guards nest across every test** *(Target: None — conditional; reactivate only if a future test installs its own `globalThis.fetch` stub without restoring it and a next test's guard captures the leftover, or on a general test-infra cleanup pass)* — `nextjs-app/test/setup/setup.ts:54`. `beforeEach` captures `realFetch = globalThis.fetch` (the prior test's `guardedFetch`) and the paired `afterEach` is a deliberate no-op, so a fresh wrapper nests on every test for the whole run. Behaviourally idempotent for the met.no check and the full suite is verified green today, but it leaves a latent cross-test leakage path. The clean equivalent (a `vi.stubGlobal('fetch', …)` + `vi.unstubAllGlobals()` in `afterEach`, as the red scaffold itself suggested) removes the nesting. Non-blocking; minor test-infra hygiene, not a correctness defect. [Thin code review of Story 10.5]

## Deferred from: code review of 10-2-sun-behind-clouds-two-signal-ui-state (2026-07-03)

- **Shared mapper `toSunStatusToken` is never consumed by any render surface (inert exhaustiveness guard)** *(Target: None — conditional; reactivate when either a render surface is refactored to route its DTO→UI-token decision through `toSunStatusToken`, OR the dead export is removed. Trigger of harm: a future `VenueSunStatus` member added at the DTO layer would NOT surface as a compile error at any render surface — the exact 10.1→10.2 silent-fall-through failure the mapper was meant to prevent, since every surface branches inline on `sunStatus === 'CloudObscured'` / `isObscuredSunStatus(...)` and hardcodes token strings instead)* — `nextjs-app/lib/utils/sun-status-presentation.ts:15`. Story 10.2 Task 1 advertised a `never`-exhaustive `toSunStatusToken` as "the single branch predicate every render surface uses," but a grep finds it consumed only by its own unit test; `VenueCard`/`VenuePin`/`VenueQuickInfo`/`VenueDetailContent`/`VenueList` each branch directly and hardcode `bg-pin-obscured`/`text-obscured-text` inline. `isObscuredSunStatus` and `skyConditionCopy` ARE used — only `toSunStatusToken` is orphaned. No runtime defect today (the surfaces were manually swept and render CloudObscured correctly); purely a Task-1 design deviation + dead additive export + an inert compile-time guard. Fix: either wire the mapper into each surface's token decision (realizing the advertised exhaustiveness benefit) or delete the unused export. Low, non-blocking. [Thin code review of Story 10.2]

## Deferred from: code review of 10-1-cloud-gated-sun-state-weather-truth-fixes (2026-07-03)

- **Latent DTO contract inconsistency: legacy `SunExposureResult.weatherData.cloudCover` optional but public `WeatherDataDto.cloudCover` stays required `number`** *(Target: None — conditional; reactivate only if `calculateSunExposure`/`SunExposureResult` is ever wired into a route emitting `VenueSunExposureResponse`, which would surface an `undefined`-into-required-`number` mismatch)* — `nextjs-app/lib/types/api.ts:284`. Story 10.1 widened the legacy service's local `weatherData.cloudCover?` (`nextjs-app/lib/solar/sun-exposure-service.ts`) to carry AC2's "unknown cloud" honestly, and it now assigns a `number | undefined` value, but the public DTO `WeatherDataDto.cloudCover` (embedded in `VenueSunExposureResponse`) is still declared required `number`. No route wires `calculateSunExposure` → `VenueSunExposureResponse` today (grep over `app/**` finds no consumer), so `tsc --noEmit` stays green (0 errors) and no runtime path is affected — the sun-exposure-service is legacy and NOT on the live venues route. Non-blocking, Low; a fix would either widen `WeatherDataDto.cloudCover?: number` (mirroring the internal slice) or map `undefined`→a sentinel at the DTO boundary, done only when/if that DTO is re-wired. [Thin code review of Story 10.1]

## Deferred from: epic review of epic-9 iteration 2 (2026-07-01)

- **VenueDetailContent Avstånd card can show a hardcoded distance string with the honest "≈ från centrum" qualifier suppressed** *(Target: None — conditional; fixture-only seam, reactivate only if a real venue's `BY_SLUG` metadata ever hardcodes a `distance` string while `venue.distanceMeters` is NaN on the centrum fallback)* — `nextjs-app/components/composed/venue/VenueDetailContent.tsx:245-252`. The FactCard `value` is `metadata.distance ?? formatVenueDistance(venue.distanceMeters)` but the new approximate-label gate is `Number.isFinite(venue.distanceMeters)`, so for a slug whose `BY_SLUG` metadata hardcodes `distance: '340 m'` (only `test-venue-sunny` today — all 6 real entries + `DEFAULT_METADATA` omit `distance`) the card can render a real-looking `340 m` while the qualifier is suppressed purely because `venue.distanceMeters` is NaN, defeating AC3's honest-annotation intent on exactly the fabricated-metadata path Epic 9 targets. Non-production: on the real Gothenburg-centrum fallback `distanceMeters` is finite so the qualifier shows correctly; both acceptance auditors judged this non-blocking pre-existing interplay, not a regression the iter-1 fix introduced. If hardened, gate the label on `metadata.distance == null && Number.isFinite(venue.distanceMeters)` (or drive both the value and the gate from a single source) so the qualifier tracks the value actually rendered. [Iteration-2 epic review of Epic 9; `nextjs-app/lib/utils/venue-visual-metadata.ts:18-41`]

## Deferred from: code review of 9-9-mobile-venue-quick-info-card-rework (2026-07-01)

- **AC3 planner-clearance margin rests on fixed estimates unanchored to the live DOM** *(Target: None — conditional; RESOLVED-IN-PART by Story 9.10 — a live-DOM clearance assertion now exists; reactivate the constant-anchoring part only if the sun-% badge is reported jamming under the planner slider on a tall device or for a long-named venue)* — `nextjs-app/components/custom/map/MapView.tsx:165-169`. `MOBILE_PLANNER_HEIGHT_PX = 80` and `QUICK_INFO_MOBILE_HEIGHT_ESTIMATE = 170` are self-conceded approximations; the one-gutter (16px) AC3 clearance margin is eroded from either end if the real rendered `TimeSliderPanel` mobile height exceeds ~80px (tall common devices / calendar+label rows) OR a long venue name wraps the fixed-230px card taller than 170px. **Story 9.10 CLOSED the "assert only the arithmetic" half:** `test/e2e/epic-9-mobile-regression.spec.ts` now asserts, at the live iPhone-14 viewport, that the rendered quick-info card's top edge sits at/below the rendered `TimeSliderPanel`'s bottom edge — so a real overlap trips the mobile gate. The REMAINING (still-conditional) follow-up is only to anchor the two constants to a measured DOM height, if a real-device overlap is ever reported. [Thin code review of Story 9.9; live-layout guard added by Story 9.10]

## Deferred from: code review of 9-8-venue-sharing-real (2026-07-01)

- **Automated visual-validation gate not satisfiable within this diff (host `/tmp` tooling bug)** *(Target: None — conditional; maintainer visual rebaseline action — reactivate when the visual-validate host tooling is fixed or a maintainer rebaselines the venue-detail + new share-modal references)* — the `venue-detail` reference PNGs predate the enabled/mobile-share button state (Story 9.1 drift) and there is NO reference PNG for the new desktop share modal; `.claude/scripts/visual-validate.sh` errors on this Windows host (`/tmp/impl-*.png` unwritable). The dev agent correctly did NOT self-bless or edit reference PNGs. The Design-Gate "Visual validation: PASS before QA handoff" line is only formally met once a maintainer rebaselines. Not a code defect. [Thin code review of Story 9.8]
- **Clipboard-write silent no-op with no user-visible signal when the clipboard API is entirely absent** *(Target: None — conditional; only triggers if a user on an insecure-context/legacy browser reports the "Kopiera länk" button doing nothing)* — `nextjs-app/components/custom/venue/ShareModal.tsx:159-170`: when `navigator.clipboard?.writeText` is missing, `handleCopy` returns with no state change and no feedback; the button keeps showing "Kopiera länk". The URL text stays visible for manual select-copy (tested at `ShareModal.test.tsx`), so the path is not dead — only unacknowledged. Prod is HTTPS (secure context), so the trigger is narrow. Consistent with the story's "modern clipboard only" decision (spec line 54); a fix would show a fallback toast or auto-select the URL text on click. [Thin code review of Story 9.8]
- **Share-target tiles render text glyphs instead of brand icons/logos** *(Target: None — maintainer visual-gate/asset decision at rebaseline)* — `nextjs-app/components/custom/venue/ShareModal.tsx:238-244` draws each share target as a letter glyph (`Wa`/`f`/`X`/`Tg`/`@`) in a brand-colored square rather than a brand mark; functionally correct (real share-intent hrefs, brand colors), but reads different from the reference tiles. Auto-deferred (Tier-A thin review, epic mode) per triage recommendation — brand-mark fidelity is a design/asset task for the maintainer at the venue-detail rebaseline, not a blind code swap. [Thin code review of Story 9.8]

## Deferred from: code review of 9-7-tag-filtering-real-data-working-chips (2026-07-01)

- **Empty-state renders the loading skeleton instead of the "nothing matches" copy during a concurrent background refetch** *(Target: None — conditional; reactivate only if a user reports the 3-card skeleton flashing over a 0-match tag filter)* — both `<VenueList>` call sites pass `isLoading={venueQuery.isFetching && listVenues.length === 0}` (`nextjs-app/components/custom/map/MapView.tsx:926,970`) and `nextjs-app/components/custom/venue/VenueList.tsx:55` returns the loading skeleton BEFORE the `sortedVenues.length === 0` empty branch (`:70`). When a tag filter yields 0 matches while the underlying `useVenueSearch` query is `isFetching` in the background (e.g. a `keepPreviousData` planner-change refetch), the surface shows the 3-card skeleton rather than the `venue.list.empty` ("Inga platser hittades…") copy. In the primary/steady state (query idle) the empty copy renders correctly and the MapView tag-filter test asserts it, so this is a transient edge on pre-existing `isLoading` wiring reused unchanged — non-blocking. A fix would order the `length === 0` empty branch ahead of the `isLoading` skeleton (or exclude the tag-filtered-to-empty case from `isLoading`). [Thin code review of Story 9.7]

## Deferred from: code review of 9-5-location-onboarding-reliability (2026-06-30)

- **AC4 hard `PERMISSION_DENIED` is collapsed into `'fallback'` — no distinct "denied, enable in settings" affordance** *(Target: None — conditional; only triggers if distinct "permission denied — enable it in settings" copy vs "we couldn't find you — retry" is wanted)* — `nextjs-app/hooks/useGeolocation.tsx:108-121` maps both `PERMISSION_DENIED` and timeout/unavailable to the single `'fallback'` status; `nextjs-app/components/custom/map/MapControls.tsx:135` therefore emits identical `data-locate-state="fallback"` for a hard-denied user and a timed-out user, and a re-click on a hard denial silently re-resolves to fallback. AC4's retry affordance (its core intent — "instead of silently using the fallback") IS present and the button stays clickable, so this is a literal-vs-implemented gap, not a defect. A distinct `'denied'` status derived from `error.code` would unlock the "enable it in settings" copy. Already recorded as Open Question 2 in the 9.5 story + a maintainer follow-up. [Thin code review of Story 9.5]

## Deferred from: code review of 1-1-project-scaffold-design-system-foundation (2026-04-17)

*(One entry — `app/not-found.tsx` hardcoded Swedish outside `NextIntlClientProvider` — carried into Story 7.2 — 404 Page (Task 2.1) on 2026-06-25. Removed by SM per the deferred-work convention.)*

*(One entry — `queryKeys.list(filters)` normalization — carried into Story 2.4 — Venue Search on 2026-05-18 (Task 2). Removed by SM per the deferred-work convention.)*

*(Four entries carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: `i18n/request.ts` try/catch verification (Task 1), font generic-family fallback (Task 2.8), build-time deps → devDependencies (Task 4.5), `--ease-*` vs `easing-*` reconciliation (Task 2.7). Removed by SM per the deferred-work convention.)*

## Deferred from: code review of 7-3-pwa-installation-offline-shell (2026-06-29)

- **Offline transition synchronously unmounts the whole venue subtree (no debounce)** *(Target: None — conditional; reactivate if flappy-network state loss is reported)* — `nextjs-app/components/custom/map/MapView.tsx:307,737-987`. `showOfflineShell = forcedState === 'map-primary-offline' || !isOnline` flips on any `offline` event, instantly unmounting `VenuePinLayer` + the search shell + an open detail sheet + the time-slider, etc. A transient `navigator.onLine` blip (common on mobile) therefore drops in-progress UI state (selection, scroll, slider position) and skips the inner `AnimatePresence` exit animations; the subtree remounts fresh on reconnect. Consider a short grace period before showing the offline shell on real `offline` events (the forced dev state should bypass it). [Round 1 code review of Story 7.3]
- **SW `skipWaiting` + `clientsClaim` can break open tabs across a deploy** *(Target: None — conditional; triggers if a deploy lands while users hold open tabs and chunk-load errors appear)* — `nextjs-app/app/sw.ts:213-214`. A freshly deployed service worker activates and claims already-open tabs without a reload; with Next content-hashed chunks, the still-running old page can then request chunk URLs the new precache no longer contains, producing `ChunkLoadError`. Mitigations to weigh: drop to a conservative update (let the new SW wait until all tabs close) or add a controllerchange/chunk-error reload prompt. [Round 1 code review of Story 7.3]
- **Offline "cached map background" (AC3) degrades to the sand fallback on a true cold offline launch** *(Target: None — conditional; accepted app-shell-only tradeoff — reactivate only if offline map caching enters scope)* — `nextjs-app/app/sw.ts` (no `runtimeCaching`) + `nextjs-app/components/custom/map/MapContainer.tsx`. The MapLibre style/tiles are remote and deliberately NOT cached (the "no offline DATA cache" anti-pattern), so a PWA opened for the first time offline (or after the HTTP cache for the style/tiles is evicted) hard-fails the style fetch → `tilesFailed` → sand fallback + "tile load failed" overlay shown alongside the "Ingen anslutning" banner, rather than a cached map. The failure path is handled and AC3's connectivity-required intent is still met; flagged only because the documented appearance ("cached map background") holds only when the HTTP cache still has the assets. [Round 1 code review of Story 7.3]
- **`app/sw.ts` is not type-checked by the repo `tsc` gate** *(Target: None — conditional; reactivate if the SW gains non-trivial logic)* — `nextjs-app/tsconfig.json:42` excludes `app/sw.ts` (its `/// <reference lib="webworker" />` conflicts with the DOM lib), and esbuild bundling does not type-check, so the file with the trickiest typing (worker globals + `declare global` augmentation) has no type enforcement. A dedicated `tsc -p tsconfig.sw.json` (webworker lib) wired into the gate would close it. Standard Serwist-on-Next pattern; ~35 lines of boilerplate today, so low exposure. [Round 1 code review of Story 7.3]
- **`inert` on the failed map canvas lacks a paired `aria-hidden` / sub-baseline-browser fallback** *(Target: None — conditional; sub-baseline-browser a11y, could fold into a Story 5.1 a11y pass)* — `nextjs-app/components/custom/map/MapContainer.tsx:166`. `inert={tilesFailed}` removes the dead canvas from focus + the a11y tree only where `inert` is supported; below the baseline (iOS Safari < 15.4 — the story's own PWA target) the invisible-but-present, tabindex'd MapLibre canvas stays keyboard/SR-reachable under the sand overlay. `OnboardingGate` sets both `inert` and `aria-hidden` for the same intent; mirror that here for belt-and-suspenders SR coverage. The offline + failed-map axe gate passes on current Chromium, so non-blocking. [Round 1 code review of Story 7.3]
- **No automated coverage of the real Serwist registration / precache / offline-serving** *(Target: None — conditional; add a built-app SW E2E if SW behavior regresses)* — `nextjs-app/app/ServiceWorkerProvider.tsx`, `nextjs-app/app/serwist/[path]/route.ts`. The SW is `disable`d in dev and every offline test uses the dev forced-state or a mocked `navigator.onLine`, so the actual SW registration, `Service-Worker-Allowed: /` root-scope claim, precache fetch, and offline document serving (AC2) are exercised only by the manual head/route checks recorded in the Debug Log. A built-app Playwright run with the real SW would close the gap. [Round 1 code review of Story 7.3]

## Deferred from: code review of 7-2-404-page (2026-06-26)

- **Inert desktop-nav icons on the 404 page are announced as disabled buttons + carry dead `focus-visible` classes** *(Target: None — conditional polish; reactivate if a11y copy/SR audit revisits the 404)* — `nextjs-app/components/custom/NotFoundPage.tsx` `InertHeaderIcon` renders the decorative location/settings chrome (mirroring the live DesktopNavBar visual) as `disabled` `<button aria-label="…">`s. SR browse mode therefore announces "My location / Settings, unavailable" on a static dead-end page, and the `focus-visible:ring-*` classes are unreachable because a disabled control is not focusable. Consider `aria-hidden="true"` on the purely-decorative icons (and drop the dead focus classes), or keep the visual parity as a deliberate choice. axe is currently clean, so this is non-blocking polish, not a violation. [Round 1 code review of Story 7.2]

## Deferred from: code review of 1-2-dev-only-state-forcing-mechanism (2026-04-19)

*(All entries from this review have been carried into Story 1.5 — Onboarding & Geolocation, AC7 / Task 11. Removed by SM on 2026-05-04 per the deferred-work convention.)*

## Deferred from: code review of 1-3-responsive-layout-shell-navigation (2026-04-20)

*(One entry — touch target effective <44 px at viewport bottom edge / `env(safe-area-inset-bottom)` — carried into Story 7.3 — PWA Installation & Offline Shell (Task 8.1) on 2026-06-25. Removed by SM per the deferred-work convention.)*
*(One entry — `TimeProvider` initial `new Date()` hydration mismatch — carried into Story 2.5 — Free Time & Date Planner on 2026-05-20 (Tasks 2 and 8). Removed by SM per the deferred-work convention.)*
*(One entry — nested-route active-state highlighting — carried into Story 2.7 — Save & View Favourites on 2026-05-28 (Task 5.4). Removed by PM per the deferred-work convention.)*
*(One entry — systemic `--spacing-*` scale mismatch — carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05 (Tasks 2.1–2.3). Removed by SM per the deferred-work convention.)*

## Deferred from: code review Round 1 of 1-4-maplibre-integration-venue-pin-layer (2026-05-01)

- ~~**Lat/lng query-key rounding bucket**~~ **CARRIED INTO Story 1.5 (Task 7) on 2026-05-04** — bucketed inside `useVenueSearch` to 4 decimal places.
- **`mapVenueDtoToPinData` carries `isPartner` but `VenuePin` never reads it** *(Target: Story 5.1 — Golden Pin / Partner Visual Enhancement)* — forward-compat for the partner pin variant. Until 5.1 introduces the styling, it is data-only. [`nextjs-app/components/custom/map/MapView.tsx:1289-1300`, `VenuePin.tsx`]
- **`validateLongitude` wraparound at ±180°** *(Target: None — conditional, only triggers if fixture geography expands beyond Sweden)* — `greatCircleMeters` does not normalise across the date line before computing distance. Irrelevant for Gothenburg (12°E). Reactivate this entry (and assign a target) the moment any fixture or production venue list extends beyond ±170° E/W. [`nextjs-app/app/api/venues/route.ts:90-99`]
- ~~**Loading-fallback flash between `MapLoadingFallback` unmount and first MapLibre tile paint**~~ **CARRIED INTO Story 1.5 (Task 8) on 2026-05-04** — `MapView` now extends the Skeleton via a `sourcedata`-driven `tilesPainted` state.

*(Two entries carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: `MapContainer` slow-load metric removal (Task 11), vendored Claude Design 260+ ESLint audit (Task 8 / AC6). Removed by SM per the deferred-work convention.)*

## Deferred from: code review Round 2 of 1-4-maplibre-integration-venue-pin-layer (2026-05-03)

- **OpenFreeMap tile-quality / availability fallback** *(Target: None — conditional, only triggers if OpenFreeMap proves insufficient post-2.x)* — Resolved Round 2 D3=B: dropped MapTiler entirely; OpenFreeMap is now the only style source. OpenFreeMap is community-run with no SLA. If tile quality, label coverage for Sweden, or uptime proves insufficient after Story 2.x ships real-data flows, evaluate building a tile proxy (`/api/map-tile/{z}/{x}/{y}`) and re-introduce MapTiler with full key isolation. Trigger: user/PM signal, or sustained OpenFreeMap downtime in production logs. **AUDITED 2026-06-04:** no active tile-provider incident is recorded in this checkout; keep conditional.
- **`MutableRefObject<T>` deprecated in React 19** *(Target: None — conditional, only triggers if React 19 strict-mode lints elevate)* — `nextjs-app/lib/contexts/MapInstanceContext.tsx:10,17,45` uses `MutableRefObject<MapLibreMap | null>`; React 19 prefers `RefObject<T | null>`. Trivial migration; non-blocking until lint elevation. [`nextjs-app/lib/contexts/MapInstanceContext.tsx`]
- ~~**`LoadingPill` 3s timer resets on every refetch**~~ **CARRIED INTO Story 1.5 (Task 9) on 2026-05-04** — refactored to a cumulative-fetching timer driven by `isFetching` + `dataUpdatedAt`.
- **`AnimatePresence initial={false}` suppresses morph entrance fade on first commit** *(Target: None — conditional, only triggers if AC6 visually fails after Story 2.x re-baseline)* — AC6 (per-pin entrance fade) is met via separate layer-level fade in `VenuePinLayer`. The morph component itself doesn't fade in on its first commit. Cosmetic, non-blocking. [`nextjs-app/components/custom/map/VenuePin.tsx:69`]
- **Rapid select/deselect within 200ms morph window may briefly show overlap** *(Target: None — conditional, only triggers if reported in user testing)* — `<AnimatePresence>` cross-fade with same key keeps both `motion.div` children alive; rapid taps cause overlapping fades and may settle at intermediate opacity. Cosmetic. [`nextjs-app/components/custom/map/VenuePin.tsx:69-95`]
- **`venue.name` containing RTL/control chars in aria-label** *(Target: None — conditional, only triggers if SR issues observed)* — `next-intl` interpolation does not escape; screen readers may misread certain Unicode. Unlikely with Swedish venue data. [`nextjs-app/components/custom/map/VenuePin.tsx:43-45`]
- **`gcTime: 0` in test queries flaky in StrictMode dev double-render** *(Target: None — conditional, only triggers if test flakes seen in CI)* — Round 1 P12 chose `gcTime: 0` to avoid stale closures. Revisit if `waitFor(isSuccess)` flakes in CI. [`nextjs-app/test/setup/test-utils.tsx:2186`, `nextjs-app/test/unit/queries/useVenueSearch.test.ts`]

*(Nine entries from this review carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: NFR8 bundle overrun (Task 4), e2e for 1.4 AC3+AC4 (Tasks 3.5, 3.6), `useVenueSearch` coverage gaps (Task 3.1), `MapControls` cleanup test (Task 3.2), `pointer-events-none` e2e (Task 3.3), desktop mobile-navbar-hidden assertion (Task 3.4), `role="status"` repeated announcements (Task 3.7), `mapRef.current` vs `mapInstance` convention (Task 3.9), zoom button disabled-during-load (Task 3.8). Removed by SM per the deferred-work convention.)*

### Newly deferred from Round 2 batch-apply (2026-05-03)

These were originally classified as `patch` in Round 2 triage but moved to `defer` during option-0 batch-apply because they require architectural decisions or cross-component refactors that don't belong in a Round 2 triage cleanup pass.

- **Locale change does not propagate to existing markers** *(Target: Story 7.1 — About Page)* — refs are updated but existing markers keep old language until selection or venue change. Dormant in 1.4 (no runtime locale switcher exists). Re-enable when a locale switcher lands by adding `locale`/`messages` to the venues effect deps, OR re-rendering markers in the ref-sync effect. If Story 7.1 does not introduce a runtime locale switcher, retarget this entry to the first i18n-switcher story. [`nextjs-app/components/custom/map/VenuePinLayer.tsx:66-71,73-152`] **AUDITED 2026-06-04:** still active until a runtime locale switcher exists; carry-in reflected under Story 7.1 in `epics.md`.
*(Four entries from this batch-apply set carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: `MapView` `useMemo([venueQuery.data])` rebuilds (Task 4.8), stagger `appendIndex` resets (Task 4.10), `<NextIntlClientProvider>` per pin (Task 4.9), hardcoded navbar heights `40px`/`84px` (Task 2.11). Removed by SM per the deferred-work convention.)*

## Deferred from: Story 1.5 — Onboarding & Geolocation (2026-05-04)

- **`OnboardingGate` desktop-onboarding visual baseline is implementation-derived, not designer-blessed** *(Target: None — conditional, only triggers if a definitive desktop onboarding design is produced or the mobile reference is updated)* — `nextjs-app/docs/design/references/screens/desktop/onboarding.png` was auto-captured from the running Story-1.5 implementation as a self-fulfilling baseline (see `nextjs-app/docs/design/references/REBASELINE-LOG.md` 2026-05-04 second entry). The gate cannot catch desktop regressions until the reference is sourced from a real design. Re-evaluate when (a) a designer produces a desktop-specific onboarding layout that supersedes "identical to mobile", or (b) the mobile reference is updated (the desktop should re-derive from the same source so they stay in sync). [`nextjs-app/docs/design/references/screens/desktop/onboarding.png`, `nextjs-app/docs/design/references/REBASELINE-LOG.md`]

*(Two entries carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: Tailwind v4 `--z-*` namespace reconciliation (Tasks 2.4–2.6), `Accept-Language` locale-negotiation investigation (Task 10). Removed by SM per the deferred-work convention.)*

## Deferred from: code review of Story 1.5 — Onboarding & Geolocation, Round 1 (2026-05-05)

*(One entry — cross-tab `localStorage` onboarded-flag propagation (`storage` listener on `OnboardingGate`) — carried into Story 7.3 — PWA Installation & Offline Shell (Task 8.2) on 2026-06-25. Removed by SM per the deferred-work convention.)*
- **`devIndicators: false` is project-wide instead of per-screenshot suppression** *(Target: None — conditional, only triggers if a less invasive visual-capture-only suppression mechanism is identified)* — Disabled the Next.js dev indicator project-wide to keep visual-gate captures clean (Story 1.5 Completion Note #5). Less invasive: hide the indicator inside Playwright's page context only when running `capture-claude-design-refs.mjs`. **AUDITED 2026-06-04:** Story 1.6 is complete and `next.config.ts` still has `devIndicators: false`; no less-invasive mechanism is present in this checkout, so keep as conditional only.
*(One entry — `AmberCTAButton` extraction — carried into Story 3.2 — Sun Accuracy Feedback on 2026-06-07 (Task 4). Removed by SM per the deferred-work convention.)*
- **`pulse-cta` keyframes module-scope rather than `motion-safe` media-query gated at the CSS level** *(Target: None — conditional, only triggers if a reduced-motion user observes the animation in practice)* — `@keyframes pulse-cta` is declared unconditionally in `globals.css`. The Tailwind v4 `motion-safe:` variant on the button class wraps the utility in `@media (prefers-reduced-motion: no-preference)` — functionally correct in principle. Add an explicit `@media (prefers-reduced-motion: reduce)` override only if reduced-motion users report seeing the animation. [`nextjs-app/app/globals.css` — `@keyframes pulse-cta` block]
*(Two entries from this review carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: `FLY_DURATION_MS` / `MY_LOCATION_DURATION_MS` magic-number reconciliation (Task 2.10), inline RGBA/hex in `OnboardingScreen` decoratives (Task 2.9). Removed by SM per the deferred-work convention.)*

## Deferred from: code review of 1-6-ci-cd-quality-gates (2026-05-07)

- **W1: Per-pin `createRoot` overhead for 50+ venues** *(Target: Story 5.1 — Golden Pin / Partner Visual Enhancement)* — VenuePinLayer creates one React root per pin; pre-existing from Story 1.4. Architecture NFR claims pin-render <=100 ms for 50 venues; multiple roots are fragile against that target. Re-evaluate when partner-pin styling lands. [`nextjs-app/components/custom/map/VenuePinLayer.tsx`] **AUDITED 2026-06-04:** still active (`VenuePinLayer` still creates one root per pin); carry-in reflected under Story 5.1 in `epics.md`.
- **W2: Top-level `import maplibregl` from MapContainer + VenuePinLayer** *(Target: Story 5.1 — Golden Pin / Partner Visual Enhancement)* — Dynamic-chunk discipline relies on a single `next/dynamic` boundary in MapView. If a future story imports MapView statically (e.g. test util), MapLibre is pulled into the parent chunk. Story 1.6's `verify-maplibre-async.mjs` catches this empirically, but the codebase invites the regression. [`nextjs-app/components/custom/map/MapContainer.tsx`, `nextjs-app/components/custom/map/VenuePinLayer.tsx`] **AUDITED 2026-06-04:** still active in `MapContainer` and `VenuePinLayer`; `MapControls` no longer imports MapLibre; carry-in reflected under Story 5.1 in `epics.md`.
*(Two entries — W3 `maplibre-gl.css` static-import hoisting vs the offline/async-map gate, and W6 `MapContainer` full-viewport tile-failure overlay focus coherence — carried into Story 7.3 — PWA Installation & Offline Shell (Tasks 8.3 and 8.4) on 2026-06-25. Removed by SM per the deferred-work convention.)*
- ~~**W7: VenuePinLayer `tagWrapperPresentational` lacks MutationObserver defense**~~ *(Carried into Story 1.6 patch P50 on 2026-05-07 under D4=A resolution; closes the wrapper-attribute re-apply concern in the same operation as D4's restructure to `removeAttribute('role')`. Removed from defer list per the queue-not-archive convention.)*
- **W8: Lighthouse Performance threshold 0.55 vs measured floor 0.53** *(Target: None — conditional, only triggers if Lighthouse CI flakes below 0.55 under the current Plan B gate)* — 3-run median 0.59-0.61 with min 0.53 leaves 0.02-0.05 headroom on cold runners; CI may flake. Either `numberOfRuns: 5` median-of-5 OR drop threshold to 0.50. Story 1.6 ships 0.55 per dev's empirical baseline. [`nextjs-app/lighthouserc.json`] **AUDITED 2026-06-04:** Plan B threshold is now durable in PRD/architecture/epics and `lighthouserc.json` remains `numberOfRuns: 3`, `minScore: 0.55`; no action unless CI flakes.
- **W11: `implementation-readiness-report-2026-04-15.md` cites Plan A targets** *(Target: None — conditional, only triggers if the 2026-04-15 implementation-readiness report is used as active scope guidance)* — The historical report still references 2.5s LCP / 400KB JS budget. Frozen artefact dated pre-Plan-B but no cross-ref to the corrected PRD/architecture/epics targets. [`implementation-readiness-report-2026-04-15.md`] **AUDITED 2026-06-04:** authoritative PRD/architecture/epics are corrected; leave the frozen report untouched unless someone uses it as live guidance, then add a header pointer or update the stale Plan A references.
*(One entry — DesktopNavBar fake search affordance — carried into Story 2.4 — Venue Search on 2026-05-18 (Tasks 3-4). Removed by SM per the deferred-work convention.)*

## Visual source refresh closeout (2026-05-22)

*(Story 2.1/2.2/2.3/2.4 planner/date, warm-map, selected-callout, bottom-sheet composition, desktop header-control, desktop venue-list width, and venue-detail first-viewport visual debts were carried into Story 2.5 — Free Time & Date Planner and closed by the refreshed MVP visual gate pass on 2026-05-21/2026-05-22. Removed from the active defer queue per the queue-not-archive convention.)*

*(Old MVP lock-badge/paywall visual debt is obsolete for MVP after the 2026-05-21 visual-source refresh. Season Pass, Swish, payment recovery, locked favourites, and paywall states are Post-MVP/Future Monetization only and are not active deferred work for Epic 2.)*

## Future visual/data contracts after Story 2.5

*(One entry — API-backed confidence and sun-percentage display contract — carried into Story 2.6 — Confidence Display & Auto-Refresh on 2026-05-22 (Tasks 2, 5, and 7). Removed by SM per the deferred-work convention.)*
*(One entry — persisted free favourites across map/list/detail — carried into Story 2.7 — Save & View Favourites on 2026-05-28 (Tasks 2, 4, 5, 7, and 8). Removed by PM per the deferred-work convention.)*
*(One entry — venue attribute/tag data source — carried into Story 3.2 — Sun Accuracy Feedback on 2026-06-07 (Task 7). Removed by SM per the deferred-work convention.)*
*(One entry — ratings, review counts, and price metadata contract — carried into Story 3.3 — Venue Reviews on 2026-06-08 (Task 7). Removed by SM per the deferred-work convention.)*
- **Partner `SOL NU` badge semantics** *(Target: Story 5.2 — "SOL NU" Badge & Partner Deep-Links)* — Story 2.5 aligns the current refreshed MVP detail composition, including the visible desktop `SOL NU` treatment. Story 5.2 must define the partner/sunny-now eligibility rules, fixture/seed expectations, and deep-link behaviour, then verify the relevant detail gates remain passing. **AUDITED 2026-06-04:** still active; carry-in already reflected under Story 5.2 in `epics.md`.
- **Share action behaviour** *(Target: Story 6.4 — Share Venue Sun Status)* — Story 2.5 preserves screenshot-scope detail chrome but does not implement native share/copy behaviour. Story 6.4 must define and implement share mechanics, fallback copy, and accessibility, then verify venue-detail visual gates remain passing. **AUDITED 2026-06-04:** still active; carry-in already reflected under Story 6.4 in `epics.md`.

## Deferred from: code review of 3-0-2-shadow-caster-schema-rpc-contract (2026-06-04)

*(One entry — empty active shadow-caster coverage can still report full sun with high confidence — carried into Story 3.0.5 — Confidence Engine Data Coverage on 2026-06-05. Removed by SM per the deferred-work convention.)*

## Deferred from: code review of 3-4-routing-visit-loop-hardening (2026-06-11)

*(One entry — mobile-viewport axe a11y-gate extension — carried into Story 7.3 — PWA Installation & Offline Shell (Task 8.5) on 2026-06-25. Removed by SM per the deferred-work convention.)*
- **`VenueCard` hardcoded Swedish sun-label fallbacks** *(Target: Story 5.1 — Golden Pin / Partner Visual Enhancement)* — `VenueCard.tsx:110-113` still carries `?? 'MEST SKUGGA'` / `?? 'FULL SOL'` / `?? 'DELVIS SOL'` fallbacks, the same defect class Story 3.4 Task 6.4 removed from `VenueQuickInfo`. Non-violating today (Swedish is the source language and `VenueList.tsx:119-121` always passes localized labels); remove the fallbacks and make the labels required when the surface is next touched. [`nextjs-app/components/composed/venue/VenueCard.tsx`] **TARGETED 2026-06-12:** carry-in reflected under Story 5.1 in `epics.md` (first story that reworks the venue card/pin surfaces).

## Deferred from: code review Round 2 of 3-4-routing-visit-loop-hardening (2026-06-14)

*(One entry — silent rejection of an invalid review photo (R2-D1) — carried into Story 8.4 — Feedback & Review Persistence Enablement (Task 4) on 2026-06-21. Removed by SM per the deferred-work convention.)*
*(One entry — `isVenueNotFoundError` 404 detection coupled to the error-message string — carried into Story 8.2 — Real Venue Store & API (AC #4 / Task 4) on 2026-06-17. Removed by SM per the deferred-work convention.)*
- **`messages-parity` ICU test checks argument names, not formatter types/styles** *(Target: None — conditional, only triggers if an ICU formatter-style sv/en drift is suspected)* — the test pins per-key ICU argument names (catching a dropped `{minutes}`) but not formatter type/style, so an sv/en drift like `{minutes, number, integer}` vs `{minutes}` passes. Enhancement only; the primary drop-an-argument risk is covered. [`nextjs-app/test/unit/messages-parity.test.ts`]
- **`routing-boundary` test matching is brittle** *(Target: None — conditional, only triggers if a legitimate new routing consumer is blocked by the assertion)* — `.toEqual([...exact consumer paths])` forces an unrelated future story to edit the test on any legitimate new consumer, and the raw-text `includes('window.open(')` / native-URL pattern scans count occurrences inside comments or string literals as real matches. Deliberate boundary pin; tighten to ignore comments/strings and assert a subset/allowlist when next touched. [`nextjs-app/test/unit/routing-boundary.test.ts`]
- **`VenueDetailError` lacks an actionless-combo invariant** *(Target: None — conditional, only triggers if the detail-error precedence logic is refactored)* — `{isNotFound:true, showBack:false}` would render an alert with neither a retry nor a back control. Currently unreachable (the sole call site always pairs `isNotFound:true` with `showBack:true`), but a future refactor of the precedence IIFE could strand the user. Add a `showBack || !isNotFound` guard (or always render Back) when the component is next touched. [`nextjs-app/components/custom/map/MapView.tsx:1380-1421`]

## Deferred from: code review Round 2 of 8-2-real-venue-store-api (2026-06-18)

- **Supabase by-slug lookup matches `slug` only, not `venueSlug`** *(Target: None — conditional, only triggers if a `venue_slug` / alias / slug-history column is ever added to `public.venues`)* — `readSupabaseVenueBySlug` filters `.eq('slug', slug)` while the in-memory path (and the pre-8.2 route) matched `slug || venueSlug`. Behaviorally equivalent today: the `venues` table has a single `slug` column and `fromVenueRow` derives `venueSlug = slug`, so no request can match `venueSlug` without also matching `slug`. Two context-blind review layers rated this High; the full-context Acceptance Auditor re-confirmed it equivalent (also dismissed as noise in Round 1). Reactivate (and add the alternate-identifier match to the Supabase query) the moment a `venue_slug`/alias column or slug-history is introduced. [`nextjs-app/lib/services/venue-store.ts`]
*(One entry — opt-in Supabase venue-store pre-cutover verification — RESOLVED by Story 8.5 (Task 6.1/6.2) on 2026-06-22: `validateVenueUniqueness` aligned to the DB unique keys `id` + `slug` (the rounded-coordinate check removed so co-located distinct venues no longer 500 the list route); the mocked venue-store tests now assert the query contract (`.select(VENUE_SELECT_COLUMNS)` 20-col list + `.eq('slug', …)`). Live venue-data integrity is the maintainer cutover check (8-5-cutover-run-2026-06-22.md). Removed per the deferred-work convention.)*

## Deferred from: code review of 8-1-shadow-caster-geodata-import (2026-06-16)

- **Non-dict `source_geom_3007` raises AttributeError in the artifact validator** *(Target: None — conditional, only triggers when `scripts/geodata/shadow_caster_pipeline.py` validation is next touched)* — in `validate_rows`, when `source_geom_3007` is non-null but not a dict (e.g. a JSON array/string/number from a corrupted or hand-edited artifact), `source_geom_3007.get("coordinates")` raises `AttributeError` and aborts the entire validation pass instead of emitting a clean per-row error (the preceding `validate_geojson_geometry` already returns `"source_geom_3007 geometry is required"`, but the bbox call still executes unconditionally). Pre-existing; Story 8.1's diff only added the `tolerance=` kwarg to this line. Not reachable via normal pipeline output (the validator validates its own dict/None output). Cheap guard: `coords = source_geom_3007.get("coordinates") if isinstance(source_geom_3007, dict) else None`. [`scripts/geodata/shadow_caster_pipeline.py:2564`]

## Deferred from: code review Round 1 of 8-3-real-sun-shadow-weather-computation (2026-06-21)

All items below are on the **opt-in real-engine path** (`SUNNYSEAT_SUN_ENGINE=real` — off in CI; the default seed path is byte-identical and unaffected).

*(Three entries — weather valid-time freshness fidelity, unbounded fan-out + redundant Met.no calls, and the Promise.all "never 500" invariant — RESOLVED by Story 8.5 (Tasks 5.3/5.1/5.2) on 2026-06-22: `WeatherSlice.validAt` (from Met.no `entry.time`) now drives both `weatherUpdatedAt` and the >2h "approximate" staleness signal (forecast slices flagged via `isForecast`), and the nearest-slice pick uses `validAt`; the list fan-out has a concurrency cap (`mapWithConcurrency`=6) + Met.no forecast dedupe by rounded ≤4-decimal coords (`createDedupedForecastFetcher`); and a structural per-venue degrade (`safeSeedOutcome` wrapper) makes the "never 500" invariant hold even if a future adapter refactor throws. Default path byte-identical. Removed per the deferred-work convention.)*
- **`extractSunlitWindow` returns the longest contiguous window and quantizes edges to the sample grid** *(Target: None — conditional, only triggers if sun-window edge fidelity is reported insufficient)* — on a day split into two sun windows by a building shadow the reported `sunWindow` may not contain the requested instant, and a single sunlit 30-min sample yields a zero-length `start==end` window with edges up to ~30 min short. MVP-acceptable (the code already documents "Coarse for MVP"); refine the extractor (or shrink the interval / interpolate edges) if window fidelity matters. [`nextjs-app/lib/services/sun-engine.ts`]
- **`synthesizeFootprint` divides by `cos(lat)` with no pole guard** *(Target: None — conditional, only triggers if the helper is reused outside Gothenburg latitudes)* — `dLng = halfM / (111320 * Math.cos(lat·π/180))` → Infinity at `±90°` (and NaN for a NaN lat), producing a degenerate polygon. Not reachable for Gothenburg venues; add `Math.max(cosLat, 1e-6)` if the exported helper is reused. [`nextjs-app/lib/services/sun-engine.ts`]
- **`applyRealSunEngine` catch-all returns seed values even when only weather failed** *(Target: None — conditional, only triggers if preserving partial geometry results on weather failure becomes desired)* — a venue whose shadow geometry computed successfully but whose weather fetch threw is reported with the 8.2 **seed** sun status, not its geometric status + `geometry-only` freshness. Mostly unreachable (`getForecast` returns `[]` rather than throwing, so the normal weather-unavailable path already preserves the geometric result); refine the catch to fall back to the geometric result if it's worth distinguishing total failure from weather-only failure. [`nextjs-app/lib/services/sun-engine.ts`]
- **Whitespace / line-ending churn in the engine extraction** *(Target: None — conditional, only triggers when `shadow-calculation-service.ts` / `lib/solar/index.ts` are next edited — normalize then)* — the Story 8.3 diff re-adds hundreds of unchanged lines in `shadow-calculation-service.ts` and the entire `lib/solar/index.ts` export block with only line-ending/indentation differences, obscuring the behaviour-preserving `computeShadowInfo` extraction and inflating the review surface. tsc/eslint pass (no runtime impact); normalize to the repo line-ending convention to keep the extraction auditable. [`nextjs-app/lib/solar/shadow-calculation-service.ts`, `nextjs-app/lib/solar/index.ts`]

## Deferred from: code review Round 1 of 8-4-feedback-review-persistence-enablement (2026-06-21)

*(One entry — live review read building the PostgREST `.or()` filter by raw interpolation — RESOLVED by Story 8.5 (Task 6.3 / AC#4d) on 2026-06-22: `readSupabaseReviewsForVenue` now quotes + backslash-escapes each operand via `orFilterValue`, so a reserved PostgREST token (`,` `.` `(` `)` `"`) in an arbitrary live slug cannot corrupt the filter or match unintended rows (regression test with a comma/quote slug). The live venue store (8.2/8.5) is the trigger condition that activated this. Removed per the deferred-work convention.)*

## Deferred from: code review Round 1 of 8-5-production-config-security-hardening (2026-06-22)

All items below are low-severity / defensive-only or pre-existing; Round 1 found **no HIGH/MEDIUM blockers** and all five ACs satisfied. Real-engine items are on the **opt-in real path** (`SUNNYSEAT_SUN_ENGINE=real` — off in CI; default seed path byte-identical).

- **Deduped forecast fetcher never evicts in-flight entries** *(Target: None — conditional, only triggers if `getForecast` is ever refactored to throw)* — `createDedupedForecastFetcher` does `inFlight.set(key, pending)` with no `.finally(() => inFlight.delete(key))`. Harmless today because `getForecast` catches all errors and resolves to `[]` (so a transient failure coalesces to the correct per-venue degrade for co-located venues, batch-scoped), but a future throwing fetcher would memoize a rejection across the whole batch. Add `.finally`-eviction (or a TTL) if the fetcher ever throws. [`nextjs-app/lib/services/sun-engine.ts:140-150`]
- **`weatherUpdatedAt` / `X-Weather-Updated-At` can carry a future valid-time for planner-future requests** *(Target: None — verify at the AC#5 cutover round-trip)* — for a future planner time, `fetchWeatherForVenue` picks the forecast slice nearest the requested instant and `weatherUpdatedAt = validAt(slice).toISOString()` (a future timestamp); `aggregateSunFreshness` then reports that future time as the response freshness. By ratified design (validAt-as-honest-freshness, not a misleading "now"); the `weather` "approximate" reason still fires via `isForecast`, so the UI signal is correct. Confirm during the cutover round-trip that any "updated N min ago" / freshness-header consumer reads sensibly for a future planner time. [`nextjs-app/lib/services/sun-engine.ts:204-213,326-333`]
- **`validateVenueUniqueness` skips the duplicate check for a falsy/empty slug** *(Target: None — conditional, only triggers if a nullable/empty-slug venue path is ever introduced)* — the `if (slug)` guard means an empty-string slug bypasses the uniqueness check the function exists to enforce (DB `slug` is `NOT NULL UNIQUE`). Not reachable today: `slug` is a required non-empty `string` on `VenueDataDto` and `fromVenueRow` throws on a blank slug. [`nextjs-app/app/api/venues/route.ts:200-206`]
- **`mapWithConcurrency` yields 0 workers for `NaN`/`0`/negative concurrency** *(Target: None — conditional, defensive-only)* — `Math.max(1, Math.min(NaN, len))` → `NaN` → `Array.from({length:NaN})` → 0 workers → results stay `undefined` (and `outcomes.map(o => o.freshness)` would then throw). Only the constant `SUN_ENGINE_LIST_CONCURRENCY=6` is ever passed; the function is exported for tests. Add an input-validation guard if it gains other callers. [`nextjs-app/lib/services/sun-engine.ts:159-176`]
- **Unparseable Met.no `entry.time` → Invalid-Date `validAt`** *(Target: None — conditional, only triggers on a non-conformant Met.no response)* — `new Date(entry.time)` on a bad timestamp yields an Invalid Date; `getTime()` is `NaN`, so the slice is silently treated as fresh and the nearest-by-`validAt` selection keeps `forecast[0]` regardless of the requested instant. Not reachable with a conformant ISO-8601 Met.no payload; add a `Number.isNaN(validAt.getTime())` skip if defensiveness is wanted. [`nextjs-app/lib/weather/met-no-service.ts:75`, `nextjs-app/lib/services/sun-engine.ts:380-389`]
- **`vercel.json` `installCommand` swallows a `lightningcss` install failure** *(Target: None — pre-existing build-infra debt, out of 8.5 scope)* — the real (pre-existing, not in the 8.5 diff) `installCommand` is `npm install --include=dev && (cd .. && npm install --no-package-lock lightningcss@1.31.1 2>&1 || true)`: it reaches into the repo root and `|| true` swallows any failure, so a broken `lightningcss` native binary would ship a broken build silently. 8.5 only *documented* it (reconciling `docs/vercel-deployment.md`). Track as a build-pipeline follow-up (drop `|| true` or fail loudly). [`nextjs-app/vercel.json`, `nextjs-app/docs/vercel-deployment.md:154`]
*(One entry — Met.no contact-of-record — RESOLVED by the maintainer on 2026-06-22: Rasmus supplied a real, monitored inbox `rasmus.thunborg@enhancior.se`, now wired as the `DEFAULT_USER_AGENT` fallback in `lib/weather/met-no-service.ts` (so even the unset-env fallback is TOS-compliant) and as the documented `MET_NO_USER_AGENT` example in `.env.example` / `docs/environment-variables.md` / `docs/vercel-deployment.md`; the two default-UA unit-test assertions updated to match. Removed per the deferred-work convention.)*
- **Concurrency unit test uses a timing-dependent `maxActive > 1` assertion** *(Target: None — conditional, only triggers if the test flakes in CI)* — the test proves concurrency by overlapping `setTimeout(5ms)` tasks and asserting `maxActive > 1`, which can intermittently observe `1` under a contended CI runner. The cap assertion (`<= 3`) is sound; make the "ran concurrently" assertion deterministic (controlled deferred promises) when next touched. [`nextjs-app/test/unit/services/sun-engine.test.ts`]
- **Line-ending (LF↔CRLF) churn inflates the 8.5 diff** *(Target: None — conditional, normalize when next editing these files)* — `lib/solar/types.ts` and several env/docs files show large LF↔CRLF re-add churn (git warns on each), burying the substantive `validAt` change and hurting `git blame`. Same class as the 8.3 R1 churn defer; confirm `.gitattributes` normalizes line endings so generated/edited-file diffs stay minimal. [`nextjs-app/lib/solar/types.ts`]

<!-- The "Story 8.5 follow-up — venue elevation" deferred item is fully resolved and
     removed per the queue-not-archive convention: Story 8.6 (Tier-1 rooftop/raised
     height gate, DONE) + Story 8.7 (Tier-2 terrain ground delta, implemented
     2026-06-22) both shipped. No residual elevation scope remains deferred. -->

## Deferred from: code review of 8-7-elevation-aware-shadow-terrain (2026-06-25)

- **Confidence/coverage caps include casters excluded by the elevation/terrain gate** *(Target: None — conditional, only triggers if a venue with a non-null `seating_elevation_m`/`ground_elevation_m` is observed to have its sun-confidence wrongly capped by a downhill/below-surface caster that does not shadow it)* — `obstructionRisks` and `shadowDataCoverage` in `computeShadowInfo` are computed over **all** nearby buildings *before* the height/terrain gate loop, so a caster the 8.6/8.7 gate drops (downhill, or below an elevated seating surface) still feeds `getObstructionRiskConfidenceCap` and the coverage cap and can lower the confidence of a venue it does not geometrically shadow. The pattern is **pre-existing** (sub-3 m casters already behaved this way before 8.6 — the obstruction-risk extraction was extracted verbatim in Story 8.3 "no math change"); the elevation work only *widens* the gated-out set, and the effect is **dormant** because every launch venue leaves both elevation columns null (flat path is byte-identical → AC2 not regressed). Resolving it is a design call — should the obstruction-risk / coverage contributor set respect the geometric `effectiveHeight < MIN_MEANINGFUL_HEIGHT` gate? Reactivate (and assign a target story) the first time a live elevated venue surfaces a wrongly-capped confidence. [`nextjs-app/lib/solar/shadow-calculation-service.ts:141-149,233-240`]

## Deferred from: code review Round 1 of 7-1-about-page (2026-06-26)

Round 1 (Blind Hunter + Edge Case Hunter + Acceptance Auditor) found **no HIGH/MEDIUM blockers**; the Acceptance Auditor confirmed **all 7 ACs PASS**. Two low-severity, non-blocking items below (one tooling, one design-token debt). The one actionable patch (count-up one-time guard) is tracked in the story's Review Findings, not here.

- **Rebaseline capture scripts can silently poison a visual baseline** *(Target: None — tooling hardening)* — `scripts/capture-navbar-ripple-rebaseline.mjs` catches the wait-selector timeout and only logs `WARN` before taking the screenshot anyway (lines 54-63), so a reference PNG can be regenerated from a page whose expected content never loaded; and both new one-off scripts (`capture-about-rebaseline.mjs`, `capture-navbar-ripple-rebaseline.mjs`) default-write directly into `docs/design/references/screens/...` unless an `*_REBASELINE_OUT` env var is set. This round's baselines were captured + visual-gate-verified PASS, so no live impact; harden (abort on the wait timeout; default to a dry-run/scratch dir) when these scripts are next touched. [`nextjs-app/scripts/capture-navbar-ripple-rebaseline.mjs:54-63`, `nextjs-app/scripts/capture-about-rebaseline.mjs`]
- **Focal accuracy stat uses an un-tokenized arbitrary font size** *(Target: None — design-system token gap, already flagged in Story 7.1 Completion Notes)* — the TRÄFFSÄKERHET stat renders at `text-[56px] lg:text-[64px]`; the largest DESIGN.md type token is `text-display-xl` (28px), far too small for the focal stat, so an arbitrary size stands in. Add a `text-display-stat`-style token (and swap the arbitrary values to it) when the type scale is next revised. [`nextjs-app/components/custom/about/AboutPage.tsx:114`]

## Deferred from: Story 7.3 — PWA Installation & Offline Shell (2026-06-26)

- **PWA app icons are token-derived placeholders, not a real brand mark** *(Target: None — conditional, only triggers when a real SunnySeat brand mark / icon set is produced)* — no source brand mark exists in the repo (the onboarding/navbar "logo" is a CSS gradient, not an exportable asset), so Task 1.2 ships placeholder icons generated from the design tokens (amber `#ffbf00` square + cream sun) by `nextjs-app/scripts/generate-pwa-icons.mjs` into `public/icons/` (192/512 "any" + 512 maskable + 180 apple-touch). Maintainer-approved (Rasmus, 2026-06-26: "token-derived placeholder, swap for a real mark later"). They satisfy AC1 installability now; regenerate via the script (or hand-author) and re-run the manifest smoke test when a real mark lands. [`nextjs-app/public/icons/`, `nextjs-app/scripts/generate-pwa-icons.mjs`, `nextjs-app/app/manifest.ts`]
- **AC5 "Lighthouse PWA ≥ 90" is reconciled to an installability checklist, not a numeric PWA score** *(Target: None — re-evaluate only if a future Lighthouse re-introduces a PWA category or a dedicated installability audit is wanted)* — Lighthouse 12.6.1 (installed via `@lhci/cli`) **removed the PWA category** (gone since Lighthouse 12, 2024), so the literal AC5 metric is no longer computable; `lighthouserc.json` already asserts only performance + accessibility. Maintainer-approved (Rasmus, 2026-06-26) to verify installability by a concrete checklist instead (valid manifest linked once + registered SW served at `/serwist/sw.js` + HTTPS in prod + 192/512 icons + theme colour), covered by the manifest smoke test + the live dev-server head/route verification recorded in the story Completion Notes. No numeric PWA gate is added. [`nextjs-app/lighthouserc.json`, `nextjs-app/app/manifest.ts`, `nextjs-app/app/serwist/[path]/route.ts`]
- **Pre-existing color-contrast (WCAG 1.4.3 / NFR25) failures surfaced by the a11y gate — NOT Story 7.3 code** *(Target: Story 5.1 — Golden Pin & Partner Visual Enhancement)* — running the Playwright a11y suite (axe-core 4.11.4, identical at git HEAD — these are red at HEAD, independent of Story 7.3) flagged SERIOUS color-contrast on surfaces this story does not own: (1) the venue-card decorative `aria-hidden` sun-% label `text-amber-text` (#fbbc00) on cream ≈ **1.63:1** — appears on `map-primary`, the `venue-detail` sheet, and the feedback/review states at the mobile viewport (the new `a11y-mobile` project's four venue-card-bearing scans are `test.fixme` pending this fix); (2) the About/Privacy footer wordmark `<span class="text-display-sm text-text-muted">sunnyseat</span>` (#938e81 on cream, 16px/800 ⇒ not "large text") ≈ **3.13:1**; (3) a `venue-detail` desktop amber label `#6e5101` ≈ **4.47:1** (borderline near-miss). Story 7.3's own surface — the offline shell — passes axe clean on both viewports. Remediation requires a design-token contrast change + a visual-reference rebaseline cascade across about / venue-cards / venue-detail / map-primary, which overlaps Story 5.1's venue-card/amber work (NFR25 "Amber palette verified against cream backgrounds" is its concern); the non-venue-card footer-wordmark fix is folded into the same AA-contrast pass since there is no other owning story. On completion, flip the `test.fixme` scans back to `test`: the four `a11y-mobile` venue-card scans AND the desktop `about` + `privacy` scans in `axe.spec.ts` (the latter two were set to `test.fixme` on 2026-06-29 — maintainer fast-follow decision at Epic 7 close-out / the `main` merge, so the pre-existing footer-wordmark contrast would not gate the MVP merge & launch); then re-confirm the desktop `venue-detail` scan (left as `test`) still passes. **CARRY-IN REFLECTED under Story 5.1 in `epics.md` (2026-06-29), per the deferred-work convention** (the SM removes this entry when Story 5.1 is drafted). [`nextjs-app/components/composed/venue/VenueCard.tsx`, `nextjs-app/components/custom/about/AboutPage.tsx`, `nextjs-app/components/custom/PrivacyPage.tsx`, `nextjs-app/test/e2e/axe-mobile.spec.ts`, `nextjs-app/test/e2e/axe.spec.ts`]
- **`app/sw.ts` is excluded from the main `tsconfig.json`, so the service worker source is not type-checked by the gate** *(Target: None — conditional, only triggers if `app/sw.ts` grows beyond the trivial Serwist boilerplate or a SW type regression is suspected)* — the SW source needs the `webworker` lib (`ServiceWorkerGlobalScope`, `self.__SW_MANIFEST`), which conflicts with the project's `dom` lib if added globally, so `app/sw.ts` is in tsconfig `exclude` and bundled by `@serwist/turbopack` via esbuild (transpile-only, no type-check) — the standard Serwist-on-Next pattern. The file is ~30 lines of canonical boilerplate today, so the exposure is low; if it gains real logic, restore type-safety with a dedicated `tsconfig.sw.json` (lib `["webworker","esnext"]`) wired into the lint/typecheck gate. [`nextjs-app/app/sw.ts`, `nextjs-app/tsconfig.json`]


## Deferred from: epic review of epic-9 (2026-07-01)

Integration pass over the landed epic-9 diff (stories 9.0–9.10). Per-story Tier-A reviews already ran; the items below are real-but-non-blocking cross-story / robustness findings deferred out of the integration pass (the 2 Patches + 4 Decisions are actioned separately via `epic-9-review-findings.md`). Security pass found 0 exploitable vulnerabilities.

- **Edge rate limiter is per-isolate in-memory (`Map`), weaker than the pre-move Node lambda** *(Target: None — DoS-hardening only, security-cleared, documented MVP-scale tradeoff)* — the relocated per-IP token bucket lives in a module-scoped `Map` invoked from the Edge proxy; each edge isolate holds its own bucket and buckets vanish on cold start, so effective per-attacker quota is `120 × concurrent isolates` rather than the comment's claimed parity. No authz/data-exposure consequence. Revisit only if a shared/durable rate-limit store is wanted. [`nextjs-app/lib/utils/rate-limit.ts`]
- **Buildings-cache key uses a re-implemented `polygonCentroid` that must stay byte-identical to the engine's internal `getCentroid`** *(Target: None — no live bug; hardening)* — two independent centroid implementations feed the cache key vs. the actual RPC query point with no shared source of truth and no cross-check test; they currently agree, but a future edit to one could collapse a co-located venue onto a different point's shadow casters. Export/reuse one centroid fn when next touched. [`nextjs-app/lib/services/sun-engine.ts:polygonCentroid`]
- **Stale `middleware.ts` naming in comments across ~4 files** *(Target: None — comment-only)* — the relocated limiter's comments say it "MOVED to `middleware.ts` (Edge)" but the actual file is `proxy.ts` (Next 16 rename). No runtime effect; a reader grepping `middleware.ts` finds nothing. [`nextjs-app/app/api/venues/route.ts`, `nextjs-app/lib/utils/rate-limit.ts`, the venues-route ATDD/test files]
- **`useServiceWorkerUpdateReload` first-install classification races async SW registration + can reload on an unrelated SW** *(Target: None — SW-update hardening, pre-existing pattern)* — `hadControllerAtArm` is latched once at effect-mount; on a returning visitor whose SW registers after mount the guard can mis-classify a later genuine stale-shell swap, and any `controllerchange` (even from an unrelated SW on the origin) forces a full `window.location.reload()` with no scope allow-list. [`nextjs-app/hooks/useServiceWorkerUpdate.ts:29`]
- **`OnboardingGate` cross-tab dismissal effect: incomplete dep array + session-sticky ref that never resets** *(Target: None — timing-latent, render-stable in practice)* — `wroteFlagThisSessionRef` stays true for the tab's session, so a later genuine cross-tab onboarding dismissal can be permanently ignored; deps omit `initialHasOnboarded`/`wroteFlagThisSessionRef`. Consider a reducer/state-machine if this area is next touched. [`nextjs-app/components/custom/onboarding/OnboardingGate.tsx:968`]
- **`localizeTag`'s 16-entry Swedish→English map must track the live DB `tags` vocabulary with no runtime signal on drift** *(Target: None — same "unverified metadata" class the epic targets)* — an unmapped live tag silently falls back to the raw Swedish string as a user-facing untranslated chip; the contract test only covers the closed 15-tag fixture set, not live DB values. Add a coverage assertion against the production tag vocabulary when a real tag audit is done. [`nextjs-app/lib/utils/venue-tags.ts`]
- **`STRIPPED_SHARE_PARAMS` is a denylist, not an allowlist** *(Target: None — privacy fail-safe hardening)* — any future transient/dev query param (`_debug`, `_forced`, `sort`, `view`, …) leaks into shared venue links by default; the test even asserts unrelated params like `ref=newsletter` are preserved. Switch to keeping only `venue`+explicitly-safe params to fail safe. [`nextjs-app/lib/utils/share.ts:STRIPPED_SHARE_PARAMS`]
- **9.9 mobile-clearance magic numbers duplicated across production + transform + unit test, synced only by comment** *(Target: None — already logged in 9.9/9.10 findings; e2e-guarded)* — `MOBILE_PLANNER_HEIGHT_PX=80` (an eyeballed estimate), `QUICK_INFO_MOBILE_ANCHOR_GAP=40` (must match the `translate(…, calc(-100% - 40px))` literal in `VenueQuickInfo`), and `EXPECTED_MOBILE_MIN_Y=437` (the re-derived sum in the test) can silently desync on any planner-panel Tailwind change; 9.10's live-DOM bounding-box e2e assertion guards the real behaviour, so risk is mitigated. Extract shared constants if next touched. [`nextjs-app/components/custom/map/MapView.tsx:165`]
- **`ShareModal` does not restore focus to the trigger on close + gives no feedback when clipboard API is absent** *(Target: None — a11y/robustness polish on the new component)* — WCAG 2.4.3: after closing the modal, focus is not returned to the share button; and `handleCopy` silently early-returns on an insecure-context/old browser with no "copy manually" hint. Add focus-restore + an absent-clipboard fallback message when the modal is next revised. [`nextjs-app/components/custom/venue/ShareModal.tsx:371`]
- **Server still computes fabricated `predictionUncertainty` (full reasons/short/description) + `venue-visual-metadata` `exposure`/`bestAt`/`seats` though 9.1 removed the card/detail consumers** *(Target: a future venue-metadata reduction story)* — the fabricated-metadata cleanup is deliberately half-applied across the 9.1↔9.7 seam: the conservative KEEP path was taken whole for the route overlay (which needs only a single label word), leaving the reason-derivation + i18n block dead across the rest of the app. Reduce to what the % still needs when the metadata source is next revisited. [`nextjs-app/lib/services/venue-visual-metadata.ts`, `nextjs-app/lib/services/sun-engine.ts`]
- **`.skip` ATDD scaffolds shipped alongside their landed real coverage + brittle source-text `.not.toContain` guards + a dead `filterChips` test fixture** *(Target: None — test-hygiene cleanup, non-blocking)* — the RED-phase ATDD scaffolds (dynamic-import tsc-dodging) were never flipped/deleted once the real modules landed, so they are permanent dead test weight; `MapControls.test.tsx` and `removed-i18n-keys.test.ts` grep component/source *text* rather than behaviour (false-fail on reformat); `DesktopNavBar.test.tsx` keeps a `filterChips` fixture no component reads. Prune when the test suite is next groomed. [`nextjs-app/test/**/*.atdd.test.ts*`, `nextjs-app/test/components/DesktopNavBar.test.tsx`]
- **`UserLocationLayer` leaks a stale marker/root on null `mapInstance` while `status==='success'`, and does not validate NaN coords before `setLngLat`** *(Target: None — imperative MapLibre lifecycle robustness)* — the create/update effect early-returns on `!mapInstance` without removal, so `entryRef.current` can point at a marker bound to a destroyed map; a later map instance then `setLngLat`s the stale marker instead of creating a fresh one. NaN coords from a malformed geolocation position are not guarded before marker placement. [`nextjs-app/components/custom/map/UserLocationLayer.tsx`]
- **Epic-9 visual references not yet rebaselined at mobile breakpoints + 4 pre-existing e2e reds carried** *(Target: maintainer rebaseline cascade; pre-existing reds unowned by epic 9)* — the mobile visual references for `map-with-selected-venue`/`venue-detail`/`map-primary` (+ new 9.8 share-modal and 9.5 dot refs) are deferred to a maintainer rebaseline per the visual-gate-inversion convention (dev forbidden from editing refs) + the host `/tmp` tooling bug; the desktop `map-primary` planner-bar-width red and mobile `onboarding.spec.ts:22/:88` + `favourites.spec.ts` reds are pre-existing and not introduced by epic 9. Rebaseline + re-run once the tooling bug is cleared. [`docs/design/references/screens/`]
