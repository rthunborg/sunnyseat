# Deferred Work

Items intentionally not fixed in the story that raised them. Each entry links back to its source review/audit and the reason it was deferred.

> **Convention (binding — see CLAUDE.md §"Critical rules", added 2026-05-02):** every entry MUST carry a `*(Target: <story-id> — <story-title>)*` tag (or `*(Target: None — conditional, only triggers if <X>)*` for items whose trigger is a future state change). The agent adding the entry MUST simultaneously update the target's source artifact: if the target story is still `backlog`, append the item as a Given/When/Then AC or footnote in `_bmad-output/planning-artifacts/epics.md` under that story; if the target story already has a story file, append it to `## Tasks / Subtasks` or `### Notes`. When the target story is later drafted via `bmad-create-story`, the SM removes the entry from this file once the AC has been carried in. **`deferred-work.md` is a queue, not an archive — every entry has a target, and tagged entries leave the file when their target story is drafted.** Untagged entries are blocked at code-review time and flagged for explicit triage at the next sprint-planning pass.

## Deferred from: code review of 1-1-project-scaffold-design-system-foundation (2026-04-17)

- **`app/not-found.tsx` hardcoded Swedish, outside `NextIntlClientProvider`** *(Target: Story 7.2 — 404 Page)* — spec explicitly defers detailed 404 work to 7.2; acceptable stub for Story 1.1. [`nextjs-app/app/not-found.tsx`]

*(One entry — `queryKeys.list(filters)` normalization — carried into Story 2.4 — Venue Search on 2026-05-18 (Task 2). Removed by SM per the deferred-work convention.)*

*(Four entries carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: `i18n/request.ts` try/catch verification (Task 1), font generic-family fallback (Task 2.8), build-time deps → devDependencies (Task 4.5), `--ease-*` vs `easing-*` reconciliation (Task 2.7). Removed by SM per the deferred-work convention.)*

## Deferred from: code review of 1-2-dev-only-state-forcing-mechanism (2026-04-19)

*(All entries from this review have been carried into Story 1.5 — Onboarding & Geolocation, AC7 / Task 11. Removed by SM on 2026-05-04 per the deferred-work convention.)*

## Deferred from: code review of 1-3-responsive-layout-shell-navigation (2026-04-20)

- **Touch target effective <44 px at viewport bottom edge** *(Target: Story 7.3 — PWA Installation & Offline Shell)* — after patching the `h-10` → `h-[40px]` fix, the 44 px `min-h-11` link still has 2 px below the viewport on a `fixed bottom-0` nav, worse on iPhone where it overlaps the home-indicator safe area. Add `env(safe-area-inset-bottom)` handling alongside the standalone-PWA chrome work. [`nextjs-app/components/custom/layout/MobileNavBar.tsx:46,59`]
*(One entry — `TimeProvider` initial `new Date()` hydration mismatch — carried into Story 2.5 — Free Time & Date Planner on 2026-05-20 (Tasks 2 and 8). Removed by SM per the deferred-work convention.)*
*(One entry — nested-route active-state highlighting — carried into Story 2.7 — Save & View Favourites on 2026-05-28 (Task 5.4). Removed by PM per the deferred-work convention.)*
*(One entry — systemic `--spacing-*` scale mismatch — carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05 (Tasks 2.1–2.3). Removed by SM per the deferred-work convention.)*

## Deferred from: code review Round 2 of 1-3-responsive-layout-shell-navigation (2026-04-20)

- ~~**`MapContext` value change re-renders every consumer on any `selectedVenueId` change**~~ **RESOLVED 2026-05-02 by Story 1.4** — the split into `MapInstanceContext` + `MapSelectionContext` (Story 1.4 Task 2) addressed this exactly. `MapInstanceContext` value is now `useMemo`'d (Round 1 patch P14). No further action.

*(One entry — `useMediaQuery` lazy initializer — carried into Story 2.2 — Venue List & Bottom Sheet on 2026-05-13 (Task 5). Removed by SM per the deferred-work convention.)*

## Deferred from: code review Round 1 of 1-4-maplibre-integration-venue-pin-layer (2026-05-01)

- ~~**Lat/lng query-key rounding bucket**~~ **CARRIED INTO Story 1.5 (Task 7) on 2026-05-04** — bucketed inside `useVenueSearch` to 4 decimal places.
- **`mapVenueDtoToPinData` carries `isPartner` but `VenuePin` never reads it** *(Target: Story 5.1 — Golden Pin / Partner Visual Enhancement)* — forward-compat for the partner pin variant. Until 5.1 introduces the styling, it is data-only. [`nextjs-app/components/custom/map/MapView.tsx:1289-1300`, `VenuePin.tsx`]
- **`validateLongitude` wraparound at ±180°** *(Target: None — conditional, only triggers if fixture geography expands beyond Sweden)* — `greatCircleMeters` does not normalise across the date line before computing distance. Irrelevant for Gothenburg (12°E). Reactivate this entry (and assign a target) the moment any fixture or production venue list extends beyond ±170° E/W. [`nextjs-app/app/api/venues/route.ts:90-99`]
- ~~**Loading-fallback flash between `MapLoadingFallback` unmount and first MapLibre tile paint**~~ **CARRIED INTO Story 1.5 (Task 8) on 2026-05-04** — `MapView` now extends the Skeleton via a `sourcedata`-driven `tilesPainted` state.

*(Two entries carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: `MapContainer` slow-load metric removal (Task 11), vendored Claude Design 260+ ESLint audit (Task 8 / AC6). Removed by SM per the deferred-work convention.)*

## Deferred from: code review Round 2 of 1-4-maplibre-integration-venue-pin-layer (2026-05-03)

- **OpenFreeMap tile-quality / availability fallback** *(Target: None — conditional, only triggers if OpenFreeMap proves insufficient post-2.x)* — Resolved Round 2 D3=B: dropped MapTiler entirely; OpenFreeMap is now the only style source. OpenFreeMap is community-run with no SLA. If tile quality, label coverage for Sweden, or uptime proves insufficient after Story 2.x ships real-data flows, evaluate building a tile proxy (`/api/map-tile/{z}/{x}/{y}`) and re-introduce MapTiler with full key isolation. Trigger: user/PM signal, or sustained OpenFreeMap downtime in production logs. **AUDITED 2026-06-04:** no active tile-provider incident is recorded in this checkout; keep conditional.
- **`MutableRefObject<T>` deprecated in React 19** *(Target: None — conditional, only triggers if React 19 strict-mode lints elevate)* — `nextjs-app/lib/contexts/MapInstanceContext.tsx:10,17,45` uses `MutableRefObject<MapLibreMap | null>`; React 19 prefers `RefObject<T | null>`. Trivial migration; non-blocking until lint elevation. [`nextjs-app/lib/contexts/MapInstanceContext.tsx`]
- ~~**`LoadingPill` 3s timer resets on every refetch**~~ **CARRIED INTO Story 1.5 (Task 9) on 2026-05-04** — refactored to a cumulative-fetching timer driven by `isFetching` + `dataUpdatedAt`.
- ~~**`/api/map-style` no concurrent-request dedup**~~ **RESOLVED 2026-06-04 by deferred-work audit** — the MapTiler proxy route no longer exists in `nextjs-app/app/api/`; `MapContainer` uses OpenFreeMap directly. Any future provider/proxy decision remains covered by the OpenFreeMap availability fallback item above.
*(Two entries — `useVenueSearch.test.ts` confidence fixture coverage and `VenuePinData.sunStatus` literal drift — carried into Story 2.6 — Confidence Display & Auto-Refresh on 2026-05-22 (Tasks 2 and 7). Removed by SM per the deferred-work convention.)*
- **`AnimatePresence initial={false}` suppresses morph entrance fade on first commit** *(Target: None — conditional, only triggers if AC6 visually fails after Story 2.x re-baseline)* — AC6 (per-pin entrance fade) is met via separate layer-level fade in `VenuePinLayer`. The morph component itself doesn't fade in on its first commit. Cosmetic, non-blocking. [`nextjs-app/components/custom/map/VenuePin.tsx:69`]
- **Rapid select/deselect within 200ms morph window may briefly show overlap** *(Target: None — conditional, only triggers if reported in user testing)* — `<AnimatePresence>` cross-fade with same key keeps both `motion.div` children alive; rapid taps cause overlapping fades and may settle at intermediate opacity. Cosmetic. [`nextjs-app/components/custom/map/VenuePin.tsx:69-95`]
- **`venue.name` containing RTL/control chars in aria-label** *(Target: None — conditional, only triggers if SR issues observed)* — `next-intl` interpolation does not escape; screen readers may misread certain Unicode. Unlikely with Swedish venue data. [`nextjs-app/components/custom/map/VenuePin.tsx:43-45`]
- **`gcTime: 0` in test queries flaky in StrictMode dev double-render** *(Target: None — conditional, only triggers if test flakes seen in CI)* — Round 1 P12 chose `gcTime: 0` to avoid stale closures. Revisit if `waitFor(isSuccess)` flakes in CI. [`nextjs-app/test/setup/test-utils.tsx:2186`, `nextjs-app/test/unit/queries/useVenueSearch.test.ts`]

*(Nine entries from this review carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: NFR8 bundle overrun (Task 4), e2e for 1.4 AC3+AC4 (Tasks 3.5, 3.6), `useVenueSearch` coverage gaps (Task 3.1), `MapControls` cleanup test (Task 3.2), `pointer-events-none` e2e (Task 3.3), desktop mobile-navbar-hidden assertion (Task 3.4), `role="status"` repeated announcements (Task 3.7), `mapRef.current` vs `mapInstance` convention (Task 3.9), zoom button disabled-during-load (Task 3.8). Removed by SM per the deferred-work convention.)*

### Newly deferred from Round 2 batch-apply (2026-05-03)

These were originally classified as `patch` in Round 2 triage but moved to `defer` during option-0 batch-apply because they require architectural decisions or cross-component refactors that don't belong in a Round 2 triage cleanup pass.

- **Locale change does not propagate to existing markers** *(Target: Story 7.1 — About Page)* — refs are updated but existing markers keep old language until selection or venue change. Dormant in 1.4 (no runtime locale switcher exists). Re-enable when a locale switcher lands by adding `locale`/`messages` to the venues effect deps, OR re-rendering markers in the ref-sync effect. If Story 7.1 does not introduce a runtime locale switcher, retarget this entry to the first i18n-switcher story. [`nextjs-app/components/custom/map/VenuePinLayer.tsx:66-71,73-152`] **AUDITED 2026-06-04:** still active until a runtime locale switcher exists; carry-in reflected under Story 7.1 in `epics.md`.
*(Four entries from this batch-apply set carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: `MapView` `useMemo([venueQuery.data])` rebuilds (Task 4.8), stagger `appendIndex` resets (Task 4.10), `<NextIntlClientProvider>` per pin (Task 4.9), hardcoded navbar heights `40px`/`84px` (Task 2.11). Removed by SM per the deferred-work convention.)*

## Deferred from: Story 1.5 — Onboarding & Geolocation (2026-05-04)

- **`OnboardingGate` desktop-onboarding visual baseline is implementation-derived, not designer-blessed** *(Target: None — conditional, only triggers if a definitive desktop onboarding design is produced or the mobile reference is updated)* — `nextjs-app/docs/design/references/screens/desktop/onboarding.png` was auto-captured from the running Story-1.5 implementation as a self-fulfilling baseline (see `nextjs-app/docs/design/references/REBASELINE-LOG.md` 2026-05-04 second entry). The gate cannot catch desktop regressions until the reference is sourced from a real design. Re-evaluate when (a) a designer produces a desktop-specific onboarding layout that supersedes "identical to mobile", or (b) the mobile reference is updated (the desktop should re-derive from the same source so they stay in sync). [`nextjs-app/docs/design/references/screens/desktop/onboarding.png`, `nextjs-app/docs/design/references/REBASELINE-LOG.md`]

*(Two entries carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: Tailwind v4 `--z-*` namespace reconciliation (Tasks 2.4–2.6), `Accept-Language` locale-negotiation investigation (Task 10). Removed by SM per the deferred-work convention.)*

## Deferred from: code review of Story 1.5 — Onboarding & Geolocation, Round 1 (2026-05-05)

- **Cross-tab `localStorage` flag changes not propagated** *(Target: Story 7.3 — PWA Installation & Offline Shell)* — `OnboardingGate` reads the onboarded flag once on mount; if user A onboards in tab 1, tab 2 (already open) keeps showing the overlay until reload. Add a `storage` event listener to `OnboardingGate` so cross-tab dismissal propagates. Multi-tab considerations are already in 7.3's scope. [`nextjs-app/components/custom/onboarding/OnboardingGate.tsx`] **AUDITED 2026-06-04:** still active; carry-in reflected under Story 7.3 in `epics.md`.
- **`devIndicators: false` is project-wide instead of per-screenshot suppression** *(Target: None — conditional, only triggers if a less invasive visual-capture-only suppression mechanism is identified)* — Disabled the Next.js dev indicator project-wide to keep visual-gate captures clean (Story 1.5 Completion Note #5). Less invasive: hide the indicator inside Playwright's page context only when running `capture-claude-design-refs.mjs`. **AUDITED 2026-06-04:** Story 1.6 is complete and `next.config.ts` still has `devIndicators: false`; no less-invasive mechanism is present in this checkout, so keep as conditional only.
*(One entry — `AmberCTAButton` extraction — carried into Story 3.2 — Sun Accuracy Feedback on 2026-06-07 (Task 4). Removed by SM per the deferred-work convention.)*
- **`pulse-cta` keyframes module-scope rather than `motion-safe` media-query gated at the CSS level** *(Target: None — conditional, only triggers if a reduced-motion user observes the animation in practice)* — `@keyframes pulse-cta` is declared unconditionally in `globals.css`. The Tailwind v4 `motion-safe:` variant on the button class wraps the utility in `@media (prefers-reduced-motion: no-preference)` — functionally correct in principle. Add an explicit `@media (prefers-reduced-motion: reduce)` override only if reduced-motion users report seeing the animation. [`nextjs-app/app/globals.css` — `@keyframes pulse-cta` block]
*(Two entries from this review carried into Story 1.6 — CI/CD Quality Gates on 2026-05-05: `FLY_DURATION_MS` / `MY_LOCATION_DURATION_MS` magic-number reconciliation (Task 2.10), inline RGBA/hex in `OnboardingScreen` decoratives (Task 2.9). Removed by SM per the deferred-work convention.)*

## Deferred from: code review of 1-6-ci-cd-quality-gates (2026-05-07)

- **W1: Per-pin `createRoot` overhead for 50+ venues** *(Target: Story 5.1 — Golden Pin / Partner Visual Enhancement)* — VenuePinLayer creates one React root per pin; pre-existing from Story 1.4. Architecture NFR claims pin-render <=100 ms for 50 venues; multiple roots are fragile against that target. Re-evaluate when partner-pin styling lands. [`nextjs-app/components/custom/map/VenuePinLayer.tsx`] **AUDITED 2026-06-04:** still active (`VenuePinLayer` still creates one root per pin); carry-in reflected under Story 5.1 in `epics.md`.
- **W2: Top-level `import maplibregl` from MapContainer + VenuePinLayer** *(Target: Story 5.1 — Golden Pin / Partner Visual Enhancement)* — Dynamic-chunk discipline relies on a single `next/dynamic` boundary in MapView. If a future story imports MapView statically (e.g. test util), MapLibre is pulled into the parent chunk. Story 1.6's `verify-maplibre-async.mjs` catches this empirically, but the codebase invites the regression. [`nextjs-app/components/custom/map/MapContainer.tsx`, `nextjs-app/components/custom/map/VenuePinLayer.tsx`] **AUDITED 2026-06-04:** still active in `MapContainer` and `VenuePinLayer`; `MapControls` no longer imports MapLibre; carry-in reflected under Story 5.1 in `epics.md`.
- **W3: `maplibre-gl/dist/maplibre-gl.css` static import** *(Target: Story 7.3 — PWA Installation & Offline Shell)* — Static CSS import in `MapContainer.tsx` is hoisted into the dynamic chunk graph (verified visually) but the `verify-maplibre-async.mjs` script audits JS only. If CSS hoisting strategy ever shifts, the gate misses regressions. [`nextjs-app/components/custom/map/MapContainer.tsx`] **AUDITED 2026-06-04:** still active; carry-in reflected under Story 7.3 in `epics.md`.
- **W6: MapContainer tile-failure overlay full-viewport `pointer-events-none`** *(Target: Story 7.3 — PWA Installation & Offline Shell)* — Status overlay covers the full viewport; sighted users see only the cream fallback while keyboard tab continues underneath. UX consistency concern, not a 1.6 defect. [`nextjs-app/components/custom/map/MapContainer.tsx`] **AUDITED 2026-06-04:** still active; keep with the platform/offline shell polish pass and reflect under Story 7.3 in `epics.md`.
- ~~**W7: VenuePinLayer `tagWrapperPresentational` lacks MutationObserver defense**~~ *(Carried into Story 1.6 patch P50 on 2026-05-07 under D4=A resolution; closes the wrapper-attribute re-apply concern in the same operation as D4's restructure to `removeAttribute('role')`. Removed from defer list per the queue-not-archive convention.)*
- **W8: Lighthouse Performance threshold 0.55 vs measured floor 0.53** *(Target: None — conditional, only triggers if Lighthouse CI flakes below 0.55 under the current Plan B gate)* — 3-run median 0.59-0.61 with min 0.53 leaves 0.02-0.05 headroom on cold runners; CI may flake. Either `numberOfRuns: 5` median-of-5 OR drop threshold to 0.50. Story 1.6 ships 0.55 per dev's empirical baseline. [`nextjs-app/lighthouserc.json`] **AUDITED 2026-06-04:** Plan B threshold is now durable in PRD/architecture/epics and `lighthouserc.json` remains `numberOfRuns: 3`, `minScore: 0.55`; no action unless CI flakes.
- ~~**W9: PRD NFR3 (`INP <=200ms`) vs architecture NFR table (`INP <=100ms`) inconsistency**~~ **RESOLVED 2026-06-04 by planning-artifact audit** — PRD, architecture, and epics now agree on INP <=200ms. No active action remains.
- **W11: `implementation-readiness-report-2026-04-15.md` cites Plan A targets** *(Target: None — conditional, only triggers if the 2026-04-15 implementation-readiness report is used as active scope guidance)* — The historical report still references 2.5s LCP / 400KB JS budget. Frozen artefact dated pre-Plan-B but no cross-ref to the corrected PRD/architecture/epics targets. [`implementation-readiness-report-2026-04-15.md`] **AUDITED 2026-06-04:** authoritative PRD/architecture/epics are corrected; leave the frozen report untouched unless someone uses it as live guidance, then add a header pointer or update the stale Plan A references.
*(One entry — DesktopNavBar fake search affordance — carried into Story 2.4 — Venue Search on 2026-05-18 (Tasks 3-4). Removed by SM per the deferred-work convention.)*

## Visual source refresh closeout (2026-05-22)

*(Story 2.1/2.2/2.3/2.4 planner/date, warm-map, selected-callout, bottom-sheet composition, desktop header-control, desktop venue-list width, and venue-detail first-viewport visual debts were carried into Story 2.5 — Free Time & Date Planner and closed by the refreshed MVP visual gate pass on 2026-05-21/2026-05-22. Removed from the active defer queue per the queue-not-archive convention.)*

*(Old MVP lock-badge/paywall visual debt is obsolete for MVP after the 2026-05-21 visual-source refresh. Season Pass, Swish, payment recovery, locked favourites, and paywall states are Post-MVP/Future Monetization only and are not active deferred work for Epic 2.)*

## Future visual/data contracts after Story 2.5

*(One entry — API-backed confidence and sun-percentage display contract — carried into Story 2.6 — Confidence Display & Auto-Refresh on 2026-05-22 (Tasks 2, 5, and 7). Removed by SM per the deferred-work convention.)*
*(One entry — persisted free favourites across map/list/detail — carried into Story 2.7 — Save & View Favourites on 2026-05-28 (Tasks 2, 4, 5, 7, and 8). Removed by PM per the deferred-work convention.)*
*(One entry — venue attribute/tag data source — carried into Story 3.2 — Sun Accuracy Feedback on 2026-06-07 (Task 7). Removed by SM per the deferred-work convention.)*
*(One entry — ratings, review counts, and price metadata contract — carried into Story 3.3 — Venue Reviews on 2026-06-08 (Task 7). Removed by SM per the deferred-work convention.)*
- **Partner `SOL NU` badge semantics** *(Target: Story 5.2 — "SOL NU" Badge & Partner Deep-Links)* — Story 2.5 aligns the current refreshed MVP detail composition, including the visible desktop `SOL NU` treatment. Story 5.2 must define the partner/sunny-now eligibility rules, fixture/seed expectations, and deep-link behaviour, then verify the relevant detail gates remain passing. **AUDITED 2026-06-04:** still active; carry-in already reflected under Story 5.2 in `epics.md`.
- **Share action behaviour** *(Target: Story 6.4 — Share Venue Sun Status)* — Story 2.5 preserves screenshot-scope detail chrome but does not implement native share/copy behaviour. Story 6.4 must define and implement share mechanics, fallback copy, and accessibility, then verify venue-detail visual gates remain passing. **AUDITED 2026-06-04:** still active; carry-in already reflected under Story 6.4 in `epics.md`.

## Deferred from: code review of 3-0-2-shadow-caster-schema-rpc-contract (2026-06-04)

*(One entry — empty active shadow-caster coverage can still report full sun with high confidence — carried into Story 3.0.5 — Confidence Engine Data Coverage on 2026-06-05. Removed by SM per the deferred-work convention.)*

## Deferred from: code review of 3-4-routing-visit-loop-hardening (2026-06-11)

- **Mobile-viewport axe extension for the a11y gate** *(Target: Story 7.3 — PWA Installation & Offline Shell)* — The `a11y` Playwright project runs Desktop Chrome only, so mobile-sheet variants (mobile venue-detail sheet, mobile review form, `FactCard` muted labels) are outside the automated axe gate. Disclosed in Story 3.4 Completion Notes; mobile surfaces remain pinned by approved mobile visual references. [`nextjs-app/playwright.config.ts`, `nextjs-app/test/e2e/axe.spec.ts`] **TARGETED 2026-06-12:** carry-in reflected under Story 7.3 in `epics.md` alongside the other platform/gate items (W3, W6).
- **`VenueCard` hardcoded Swedish sun-label fallbacks** *(Target: Story 5.1 — Golden Pin / Partner Visual Enhancement)* — `VenueCard.tsx:110-113` still carries `?? 'MEST SKUGGA'` / `?? 'FULL SOL'` / `?? 'DELVIS SOL'` fallbacks, the same defect class Story 3.4 Task 6.4 removed from `VenueQuickInfo`. Non-violating today (Swedish is the source language and `VenueList.tsx:119-121` always passes localized labels); remove the fallbacks and make the labels required when the surface is next touched. [`nextjs-app/components/composed/venue/VenueCard.tsx`] **TARGETED 2026-06-12:** carry-in reflected under Story 5.1 in `epics.md` (first story that reworks the venue card/pin surfaces).

## Deferred from: code review Round 2 of 3-4-routing-visit-loop-hardening (2026-06-14)

- **Silent rejection of an invalid review photo** *(Target: Story 8.4 — Feedback & Review Persistence Enablement)* — when `isSafeOptionalPhoto` rejects a pick (oversized >5 MB, non-`image/*`, 0-byte, or name >120 chars) the handler calls `setPhoto(undefined)` and clears the input without writing anything to the always-mounted `role="status"` region, so no user (sighted or screen-reader) is told the photo was refused — the Camera button just appears to do nothing. Pre-existing from Story 3.3; Round 2 of Story 3.4 only added the announcement region. Fix needs a localized rejection string surfaced in the same status region; fold it in when real photo persistence reworks the upload UX. [`nextjs-app/components/composed/feedback/ReviewForm.tsx:193-197,219-229`] **TARGETED 2026-06-14:** carry-in reflected under Story 8.4 in `epics.md`.
*(One entry — `isVenueNotFoundError` 404 detection coupled to the error-message string — carried into Story 8.2 — Real Venue Store & API (AC #4 / Task 4) on 2026-06-17. Removed by SM per the deferred-work convention.)*
- **`messages-parity` ICU test checks argument names, not formatter types/styles** *(Target: None — conditional, only triggers if an ICU formatter-style sv/en drift is suspected)* — the test pins per-key ICU argument names (catching a dropped `{minutes}`) but not formatter type/style, so an sv/en drift like `{minutes, number, integer}` vs `{minutes}` passes. Enhancement only; the primary drop-an-argument risk is covered. [`nextjs-app/test/unit/messages-parity.test.ts`]
- **`routing-boundary` test matching is brittle** *(Target: None — conditional, only triggers if a legitimate new routing consumer is blocked by the assertion)* — `.toEqual([...exact consumer paths])` forces an unrelated future story to edit the test on any legitimate new consumer, and the raw-text `includes('window.open(')` / native-URL pattern scans count occurrences inside comments or string literals as real matches. Deliberate boundary pin; tighten to ignore comments/strings and assert a subset/allowlist when next touched. [`nextjs-app/test/unit/routing-boundary.test.ts`]
- **`VenueDetailError` lacks an actionless-combo invariant** *(Target: None — conditional, only triggers if the detail-error precedence logic is refactored)* — `{isNotFound:true, showBack:false}` would render an alert with neither a retry nor a back control. Currently unreachable (the sole call site always pairs `isNotFound:true` with `showBack:true`), but a future refactor of the precedence IIFE could strand the user. Add a `showBack || !isNotFound` guard (or always render Back) when the component is next touched. [`nextjs-app/components/custom/map/MapView.tsx:1380-1421`]

## Deferred from: code review Round 2 of 8-2-real-venue-store-api (2026-06-18)

- **Supabase by-slug lookup matches `slug` only, not `venueSlug`** *(Target: None — conditional, only triggers if a `venue_slug` / alias / slug-history column is ever added to `public.venues`)* — `readSupabaseVenueBySlug` filters `.eq('slug', slug)` while the in-memory path (and the pre-8.2 route) matched `slug || venueSlug`. Behaviorally equivalent today: the `venues` table has a single `slug` column and `fromVenueRow` derives `venueSlug = slug`, so no request can match `venueSlug` without also matching `slug`. Two context-blind review layers rated this High; the full-context Acceptance Auditor re-confirmed it equivalent (also dismissed as noise in Round 1). Reactivate (and add the alternate-identifier match to the Supabase query) the moment a `venue_slug`/alias column or slug-history is introduced. [`nextjs-app/lib/services/venue-store.ts`]
- **Opt-in Supabase venue-store path — pre-cutover verification (uniqueness-key alignment + query-contract test coverage)** *(Target: Story 8.5 — Production Config & Security Hardening)* — both bite only with `SUNNYSEAT_VENUE_STORE=supabase` (off in CI), so they belong with the live cutover. (a) **Uniqueness-key mismatch:** `validateVenueUniqueness` dedupes on `id` + rounded coordinates, while the SQL contract enforces unique `id` + `slug` — neither enforces the other, so two live venues sharing rounded coordinates would 500 the list route, and a duplicate slug is caught only by the DB unique index, not the route validator. Align the assumptions (or relax the coord check) and verify live venue-data integrity at cutover. (b) **Query-contract untested:** the mocked Supabase tests ignore the args to `.select(VENUE_SELECT_COLUMNS)` / `.eq('slug', …)`, so a column/filter typo in the snake_case query surfaces only against the real table — add query-contract assertions (or a live round-trip check) when enabling the read. [`nextjs-app/app/api/venues/route.ts`, `nextjs-app/lib/services/venue-store.ts`, `nextjs-app/test/unit/services/venue-store.test.ts`]

## Deferred from: code review of 8-1-shadow-caster-geodata-import (2026-06-16)

- **Non-dict `source_geom_3007` raises AttributeError in the artifact validator** *(Target: None — conditional, only triggers when `scripts/geodata/shadow_caster_pipeline.py` validation is next touched)* — in `validate_rows`, when `source_geom_3007` is non-null but not a dict (e.g. a JSON array/string/number from a corrupted or hand-edited artifact), `source_geom_3007.get("coordinates")` raises `AttributeError` and aborts the entire validation pass instead of emitting a clean per-row error (the preceding `validate_geojson_geometry` already returns `"source_geom_3007 geometry is required"`, but the bbox call still executes unconditionally). Pre-existing; Story 8.1's diff only added the `tolerance=` kwarg to this line. Not reachable via normal pipeline output (the validator validates its own dict/None output). Cheap guard: `coords = source_geom_3007.get("coordinates") if isinstance(source_geom_3007, dict) else None`. [`scripts/geodata/shadow_caster_pipeline.py:2564`]

## Deferred from: code review Round 1 of 8-3-real-sun-shadow-weather-computation (2026-06-21)

All items below are on the **opt-in real-engine path** (`SUNNYSEAT_SUN_ENGINE=real` — off in CI; the default seed path is byte-identical and unaffected).

- **Weather slices carry no valid-time → `weatherUpdatedAt` is always the fetch instant (AC #3 freshness fidelity)** *(Target: Story 8.5 — Production Config & Security Hardening)* — `getForecast` stamps `createdAt: new Date()` on every slice incl. forecasts (`nextjs-app/lib/weather/met-no-service.ts:79`) and discards `entry.time`; the adapter picks a slice by rounded hour-offset and reports its `createdAt` as `weatherUpdatedAt`. The confidence-display "approximate" state (weather >2h old) can therefore never fire from a successful fetch, and a future-planner slice is advertised as fresh "now". Implementation matches Task 3's literal mapping; fixing it means re-tuning the weather adapter to carry the slice valid-time (against the Story 8.3 "consume, don't re-tune" guardrail), so it belongs with the live cutover. At 8.5, either extend `WeatherSlice` with a `validAt`/slice-time field (used for both `weatherUpdatedAt` and staleness) or derive the approximate/stale signal from `isForecast`. **Decision-needed at Story 8.3 review Round 1; deferred here by Rasmus (2026-06-21).** [`nextjs-app/lib/weather/met-no-service.ts`, `nextjs-app/lib/services/sun-engine.ts`]
- **Unbounded concurrent fan-out + N redundant per-venue Met.no calls** *(Target: Story 8.5 — Production Config & Security Hardening)* — the real list path issues 2 Supabase RPCs + 1 Met.no forecast per venue via an uncapped `Promise.all`, and nearby venues fetch near-duplicate forecasts (each `getForecast(lat,lng)` is a distinct fetch-cache key). Sanctioned by DECISION D (compute-on-request MVP; precompute/Cron is the explicit follow-up), but the Met.no TOS / connection-exhaustion risk should be addressed before the live cutover — add a concurrency cap and/or dedupe forecasts by rounded coordinates across the batch. [`nextjs-app/app/api/venues/route.ts`, `nextjs-app/lib/services/sun-engine.ts`]
- **List route uses `Promise.all`, not `Promise.allSettled`, to uphold the "never 500" invariant** *(Target: Story 8.5 — Production Config & Security Hardening)* — safe today because `applyRealSunEngine` wraps all work in a try/catch and never throws, so the stated per-venue degrade invariant rests on that single catch. `Promise.allSettled` (or a defensive wrapper) would encode the invariant structurally so a future refactor that lets the adapter throw can't 500 the whole endpoint. Harden alongside the real-path enablement. [`nextjs-app/app/api/venues/route.ts`]
- **`extractSunlitWindow` returns the longest contiguous window and quantizes edges to the sample grid** *(Target: None — conditional, only triggers if sun-window edge fidelity is reported insufficient)* — on a day split into two sun windows by a building shadow the reported `sunWindow` may not contain the requested instant, and a single sunlit 30-min sample yields a zero-length `start==end` window with edges up to ~30 min short. MVP-acceptable (the code already documents "Coarse for MVP"); refine the extractor (or shrink the interval / interpolate edges) if window fidelity matters. [`nextjs-app/lib/services/sun-engine.ts`]
- **`synthesizeFootprint` divides by `cos(lat)` with no pole guard** *(Target: None — conditional, only triggers if the helper is reused outside Gothenburg latitudes)* — `dLng = halfM / (111320 * Math.cos(lat·π/180))` → Infinity at `±90°` (and NaN for a NaN lat), producing a degenerate polygon. Not reachable for Gothenburg venues; add `Math.max(cosLat, 1e-6)` if the exported helper is reused. [`nextjs-app/lib/services/sun-engine.ts`]
- **`applyRealSunEngine` catch-all returns seed values even when only weather failed** *(Target: None — conditional, only triggers if preserving partial geometry results on weather failure becomes desired)* — a venue whose shadow geometry computed successfully but whose weather fetch threw is reported with the 8.2 **seed** sun status, not its geometric status + `geometry-only` freshness. Mostly unreachable (`getForecast` returns `[]` rather than throwing, so the normal weather-unavailable path already preserves the geometric result); refine the catch to fall back to the geometric result if it's worth distinguishing total failure from weather-only failure. [`nextjs-app/lib/services/sun-engine.ts`]
- **Whitespace / line-ending churn in the engine extraction** *(Target: None — conditional, only triggers when `shadow-calculation-service.ts` / `lib/solar/index.ts` are next edited — normalize then)* — the Story 8.3 diff re-adds hundreds of unchanged lines in `shadow-calculation-service.ts` and the entire `lib/solar/index.ts` export block with only line-ending/indentation differences, obscuring the behaviour-preserving `computeShadowInfo` extraction and inflating the review surface. tsc/eslint pass (no runtime impact); normalize to the repo line-ending convention to keep the extraction auditable. [`nextjs-app/lib/solar/shadow-calculation-service.ts`, `nextjs-app/lib/solar/index.ts`]
