# Story 15.1 Round 2 fix audit (v4)

- R2-01 implemented and tested: successful zero-attempt completion, exact expected lane cells, required output presence and matching hashes are prerequisites to every report write. Main checkpoints without completion cannot be reported. Edge guards/counts run before final files; final completion is published atomically. Tests exercise interrupted/tainted lanes and verify zero derived writes.
- R2-02 implemented and measured: original excluded-caster control preserved; two added elevated fixtures have effective heights 10/12 m, each with an actual sun/shade transition on all seven required dates (14 cells). Full minute-oracle and candidate records retained, plus five warmups/30 coarse-grid CPU observations per cell. All candidates pass this declared reference; worst matched error 439.5 ms. This does not fix the separate missed sub-minute windows or prove field accuracy.
- Verification: pre-edit and final typecheck/lint passed; targeted 4 files / 16 tests passed; full suite 240 files / 2,246 tests passed. Main/edge/edit runners and final standalone strategy runner passed. Report and three derived ledger/hash files regenerate byte-identically.
- Provenance: current run-04, retained sql-03. SQL original dataset hash and unchanged edit payload/input manifest are checked before reporting; supplemental elevated cells do not enter old SQL totals. pre-r2 preserves the prior report/audit/manifest. README and logs disclose the failed combined invocation and superseded timing attempts.
- Preservation: application runtime/schema/migration, package and Compose unchanged. Protected config/test hashes match initial values. Party-mode untouched. No commit/push/merge/deploy/reset/clean or production activity. No new database resources launched.
- Status: in-progress; no new automatic review round or review gate. R2 technical checkboxes mean implemented/tested, not independent re-review. Owner decisions, representative inputs, adaptive operational/read-load measurements and production budgets remain open. Story 15.2 remains blocked.

Prior context and execution audits retain their historical meaning; none grants G1 acceptance.