# Captured-cohort verification record

- Story 15.1 only; baseline typecheck/lint passed before changes. Final typecheck (`--incremental false`), whole-app quiet lint, and full Vitest passed: 243 files / 2,261 tests. New helper tests: two files / five tests passed. Full log: `../vitest-captured-full.log`. No runtime UI change; E2E/visual checks not applicable.
- Offline captured runner passed in 506.95s overall, including 505.74s test execution. It publishes only after all cells and zero external attempts assertions pass. A first invocation was interrupted before evidence publication to correct a TypeScript annotation; run-07 is the completed corrected invocation. No partial timing output was retained or reported.
- Report regenerated byte-for-byte after validating capture/source hashes, completed replay and SQL artifacts, and original dataset identity. `report-generator.json` binds the report tool. New SQL wrapper binds the unchanged experiment source and its captured-input lane. Raw captures and older 180 manifest entries were verified before archiving the previous manifest/README in pre-captured.
- Read-only capture was owner-authorized; no production write experiments. Capture occurred September 10, replay September 11. Current inputs are not historical geometry. No hidden venues in this snapshot; generalization and physical correctness remain unproven.
- Reused guard-owned repository Postgres, verified ownership, created only new database `e15_story15_1_capture01`. Stop request for resource `21295fc5-ad93-43a4-b515-4ece1c7e89ce` accepted with native exit 0, ok true, stop_requested, verified false. No shutdown polling, deletion, volume removal or application migration.
- Main branch and HEAD `ed13d76a6e214f5e9f66ef5b73f8202f4406033d` retained. Protected config/test hashes match starting values; party-mode untouched. No commit/push/merge/deploy or fourth automatic review. Story/sprint status unchanged.
- Method acceptance remains blocked by unseen shade gaps. The three clipped reference windows are diagnostic, not proof of qualifying full-day windows. Full-season adaptive costs, derived-window persistence, concurrent/cold reads, retained seasonal growth and disk/plan headroom still need measurement. No numerical production budgets approved.
