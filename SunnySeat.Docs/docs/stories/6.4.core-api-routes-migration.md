# Story 6.4: Core API Routes Migration

## Status

✅ **ready-for-dev**

## Story

**As a** development team,  
**I want** essential API endpoints migrated to Next.js API routes,  
**so that** core functionality is available in the new platform while maintaining existing API contracts.

## Acceptance Criteria

1. Authentication endpoints work (login, register, if applicable) ?
2. Core API endpoints respond correctly ?
3. Request validation is in place ?
4. Error responses follow consistent format ?
5. API routes are organized in `/app/api` directory ?
6. Supabase client is used for database operations ?
7. API responses match current API contract ?
8. Integration tests pass for migrated endpoints ?

## Tasks / Subtasks

- [ ] **Task 1: Authentication Endpoints Migration** (AC: 1, 6)

  - [ ] Migrate authentication endpoints to Next.js API routes
  - [ ] Integrate Supabase Auth (if replacing custom auth)
  - [ ] Implement login endpoint
  - [ ] Implement register endpoint (if applicable)
  - [ ] Verify authentication endpoints work correctly
  - [ ] Use Supabase client for auth operations

- [ ] **Task 2: Core Business Logic Endpoints** (AC: 2, 6, 7)

  - [ ] Identify core business logic endpoints to migrate
  - [ ] Convert Entity Framework queries to Supabase client calls
  - [ ] Migrate endpoints to Next.js API routes
  - [ ] Verify core API endpoints respond correctly
  - [ ] Verify API responses match current API contract
  - [ ] Use Supabase client for database operations

- [ ] **Task 3: Request/Response Handling** (AC: 3, 4, 7)

  - [ ] Implement request validation
  - [ ] Implement consistent error response format
  - [ ] Implement request/response handling
  - [ ] Verify error responses follow consistent format
  - [ ] Verify request validation is in place

- [ ] **Task 4: API Route Structure** (AC: 5)

  - [ ] Organize API routes in `/app/api` directory
  - [ ] Follow Next.js Route Handler conventions
  - [ ] Structure routes logically
  - [ ] Verify API routes are organized correctly

- [ ] **Task 5: Error Handling** (AC: 4)

  - [ ] Implement error handling middleware
  - [ ] Implement consistent error response format
  - [ ] Handle database errors gracefully
  - [ ] Handle validation errors
  - [ ] Verify error handling works correctly

- [ ] **Task 6: Integration Testing** (AC: 8)
  - [ ] Create integration tests for migrated endpoints
  - [ ] Test authentication endpoints
  - [ ] Test core business logic endpoints
  - [ ] Verify integration tests pass for migrated endpoints

## Dev Notes

### Architecture Alignment

This story migrates essential API endpoints from .NET 8 Minimal APIs to Next.js API routes (Route Handlers). This maintains API contracts while adapting to the Next.js platform.

### Previous Story Dependencies

**From Story 6.3:**

- Data migrated to Supabase
- Database schema validated
- Supabase client connection established

### Technical Implementation

**Next.js API Routes:**

- Use Next.js Route Handlers for API endpoints
- Organize routes in `/app/api` directory
- Use Server Actions for form submissions (if applicable)
- Maintain existing API response formats

**Supabase Integration:**

- Convert Entity Framework queries to Supabase client calls
- Use Supabase client for database operations
- Use Supabase Auth for authentication (if replacing custom auth)

**API Route Structure:**

```
/app/api
  /auth
    /login/route.ts
    /register/route.ts
  /patios
    /route.ts
    /[id]/route.ts
  /venues
    /route.ts
    /[id]/route.ts
```

**Request/Response Handling:**

- Implement request validation using Zod or similar
- Implement consistent error response format
- Handle async operations properly

**Error Handling:**

- Consistent error response format
- Proper HTTP status codes
- Error logging and monitoring

### Core API Endpoints to Migrate

**Authentication Endpoints:**

- `/api/auth/login` - User login
- `/api/auth/register` - User registration (if applicable)

**Core Business Logic Endpoints (from `src/backend/SunnySeat.Api/Endpoints/`):**

- `/api/patios` - Patio search by location (GET, query params: latitude, longitude, radiusKm)
- `/api/sun-exposure/patio/{id}` - Sun exposure calculation (GET, query param: timestamp)
- `/api/feedback` - User feedback submission (POST)
- `/api/admin/venues` - Admin venue management (requires auth)
- `/api/admin/buildings` - Building data management (requires auth)
- `/health`, `/health/ready`, `/health/live`, `/health/database` - Health check endpoints

### API Contract Maintenance

**Response Format:**

- Maintain existing JSON response formats
- Maintain existing error response formats
- Maintain existing status codes

**Request Format:**

- Maintain existing request formats
- Maintain existing query parameters
- Maintain existing request body structures

### Migration Approach

**Entity Framework to Supabase:**

- Convert LINQ queries to Supabase client calls
- Convert Entity Framework models to Supabase queries
- Handle async operations properly

**Example Conversion (from `PatioEndpoints.cs`):**

```typescript
// Entity Framework (old) - from IPatioRepository
var patios = await patioRepository.GetPatiosNearPointAsync(
  searchPoint,
  radius,
  cancellationToken
);

// Supabase (new) - using RPC for spatial queries
const { data: patios } = await supabase.rpc("get_patios_near_point", {
  search_lat: latitude,
  search_lng: longitude,
  radius_meters: radius * 1000, // Convert km to meters
});

// Or direct query for simple filters
const { data: patios } = await supabase
  .from("patios")
  .select("*")
  .eq("venue_id", venueId);
```

**Note:** Complex spatial queries should use Postgres RPC functions (created in Story 6.5) rather than direct Supabase queries.

### Next Steps

After completing this story:

- Story 6.5: Spatial/Geographic API Migration

## Testing

### Testing Framework

- Integration testing for API endpoints
- Request/response validation testing
- Error handling testing
- API contract validation

### Testing Requirements

1. **Authentication Endpoint Testing:**

   - Test login endpoint
   - Test register endpoint (if applicable)
   - Verify authentication works correctly
   - Verify error handling for invalid credentials

2. **Core API Endpoint Testing:**

   - Test core business logic endpoints
   - Verify responses match current API contract
   - Verify request validation works
   - Verify error handling works

3. **Request Validation Testing:**

   - Test valid requests
   - Test invalid requests
   - Test missing required fields
   - Verify validation errors are returned correctly

4. **Error Handling Testing:**

   - Test error response format
   - Test error status codes
   - Test database error handling
   - Test validation error handling

5. **API Contract Testing:**

   - Compare responses with current API
   - Verify response formats match
   - Verify status codes match
   - Verify error formats match

6. **Integration Testing:**
   - Create integration tests for all migrated endpoints
   - Verify integration tests pass
   - Test end-to-end flows

## Change Log

- **Initial Creation**: Story created from Epic 6 epic document
