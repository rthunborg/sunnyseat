import fs from 'node:fs';
import path from 'node:path';
import {artifactHashes,validateLaneCompletion,jsonHash} from './evidence.mjs';
const [captureDir,runDir,sqlDir]=process.argv.slice(2);
if(![captureDir,runDir,sqlDir].every(p=>p&&path.isAbsolute(p)))throw Error('Absolute capture, run and SQL directories required');
const read=(dir,name)=>JSON.parse(fs.readFileSync(path.join(dir,name),'utf8'));
const capture=read(captureDir,'inputs.json'),census=read(captureDir,'census.json'),data=read(runDir,'captured-cost.json'),environment=read(runDir,'captured-environment.json');
if(JSON.stringify(environment.captureHashes)!==JSON.stringify(artifactHashes(captureDir,['inputs.json','inputs.sql','census.json','census.sql'])))throw Error('Capture identity mismatch');
const current=artifactHashes(process.cwd(),Object.keys(environment.sourceHashes));
if(JSON.stringify(current)!==JSON.stringify(environment.sourceHashes))throw Error('Replay source identity mismatch');
const expected=[...capture.venues.map(f=>f.id+'/2026-09-22'),...data.selection.selected.flatMap(id=>[60000,10000,5000,1000].map(step=>`${id}/${step}`))];
if(data.selection.selected.length!==3||data.costs.length!==capture.counts.venues||data.comparisons.length!==12)throw Error('Incomplete matrix');
validateLaneCompletion(read(runDir,'captured-completion.json'),artifactHashes(runDir,['fixtures.json','M06-identical-dataset.json','M06-edited-dataset.json','M06-edited-fixtures.json','captured-cost.json','captured-environment.json']),expected);
const sql=read(sqlDir,'summary.json'),rows=read(runDir,'M06-identical-dataset.json');
const sqlCompletion=read(sqlDir,'captured-sql-completion.json');
validateLaneCompletion(sqlCompletion,artifactHashes(sqlDir,fs.readdirSync(sqlDir).filter(n=>n!=='captured-sql-completion.json').sort()),['local-sql']);
const sqlSource=read(sqlDir,'capture-sql-source.json');
if(JSON.stringify(sqlSource.sourceHashes)!==JSON.stringify(artifactHashes(process.cwd(),Object.keys(sqlSource.sourceHashes)))||JSON.stringify(sqlSource.inputArtifacts)!==JSON.stringify(read(runDir,'captured-completion.json').artifacts))throw Error('SQL source/input identity mismatch');
if(sql.complete!==true||sql.datasetSha256!==jsonHash(rows)||sql.inputFixtures!==capture.counts.venues||sql.rowsPerEncoding!==rows.length*5)throw Error('SQL identity/completeness mismatch');
const clean=read(sqlDir,'clean.json'),edited=read(sqlDir,'edited.json'),writes=read(sqlDir,'write-phases.json');
const sum=(c,kind)=>c.relations.filter(r=>r.schema==='bench_'+kind).reduce((n,r)=>n+r.total_bytes,0);
const table=data.comparisons.map(c=>`| ${c.id} | ${c.stepMs/1000} | ${c.run.samples} | ${c.wallMs.p50.toFixed(1)} | ${c.raw.topologyMatches} | ${c.raw.maxErrorMs.toFixed(1)} | ${c.expected.complete&&c.candidate.complete} |`).join('\n');
console.log(`# Captured-input measurement supplement — Story 15.1\n\nProduction capture: ${capture.capturedAt}; census: ${census.captured_at}. These are separate read-only snapshots. Replay started ${environment.measuredAt}.\n\n${capture.counts.venues} venues, ${capture.counts.uniqueCasters} distinct resolved casters, ${capture.counts.casterReferences} memberships. Existing production database: ${census.database_bytes} B. Existing geometry-series relation: ${census.relations.find(r=>r.name==='venue_sun_geometry_series').total_bytes} B, including TOAST/index overhead. This is the actual current baseline, not the proposed schema.\n\n## Bounded computation and detection\n\nAll venues were recomputed for September 22 at a 15-minute grid with exact 5-degree support edges, five observations plus one warmup. Sum of per-venue median daily wall times: ${data.costs.reduce((n,c)=>n+c.wallMs.p50,0).toFixed(1)} ms. This sum is a derived summary, not a timed batch or season projection. Timings are on a shared Windows host; validation activity overlapped part of the run.\n\nThree transition-bearing venues selected by low/median/high caster count, each with a clipped 30-minute window. Reference: independent uniform 250ms samples (detected-boundary uncertainty 125ms). Candidate: grid plus detected-crossing bisection to 100ms brackets. Five observations; no p95 estimate.\n\n| Venue | Cadence s | Samples | Median wall ms | Raw topology matches | Max matched error ms | Duration resolved |\n|---|---:|---:|---:|---|---:|---|\n${table}\n\nNeither reference nor candidate proves absence of sub-grid shade gaps. Clipped endpoints are window boundaries, not proven full sun-window starts/ends; duration filtering is diagnostic only. The analytic unit tests retain a missed-gap counterexample at every cadence, including 1s. No candidate is accepted for production.\n\n## Local retained-storage replay\n\nSame 42 measured day rows per encoding, copied into five simulated retention roles, then recomputed +2m seating edits retained as a sixth generation. Previous-season copies were not recalculated for another year.\n\n| Encoding | Clean total relations B | After edits total relations B |\n|---|---:|---:|\n${['jsonb','arrays','bytes'].map(k=>`| ${k} | ${sum(clean,k)} | ${sum(edited,k)} |`).join('\n')}\n\nWhole isolated database after edits: ${edited.database_bytes} B; includes all three encodings and its own catalog baseline. Do not add it to production or treat it as the size of one design.\n\nWrite phases (including WAL):\n\n\`\`\`json\n${JSON.stringify(writes,null,2)}\n\`\`\`\n\nControlled SQL selection and warm read observations are retained in sql-04. Host latency includes Docker/psql startup and text transport. Local PostgreSQL 15 differs from production PostgreSQL 17/aarch64. Retained copies and edit frequency are experimental assumptions. The legacy SQL summary's synthetic/no-production-baseline labels describe that isolated lane; the separately captured census supplies an actual baseline here.\n\n## Recommendation and remaining acceptance\n\nKeep the accepted 5-degree convention, 300-second continuous eligibility and full actual-year March–October completeness. Arrays remain provisional. Use 1s sampling only as an offline diagnostic reference candidate, not a correctness guarantee or approved production cadence. A bounded method must certify no intervening shade (or conservatively mark intervals unresolved); point sampling alone cannot satisfy the no-gap requirement.\n\nNumerical production CPU/storage/read budgets remain unapproved. This one-date replay cannot establish full-season adaptive costs, concurrency, retained seasonal growth, cold-cache behavior or available disk/plan headroom. Next evidence must measure those against a selected detection method. No production writes or application changes. Story 15.1 remains in-progress; 15.2 remains blocked.\n`);
