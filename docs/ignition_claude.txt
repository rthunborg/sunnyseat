Act as the Lead Agent. We are initiating a CONTINUOUS EXECUTION LOOP.
Review _bmad-output/implementation-artifacts/sprint-status.yaml to identify the next highest-priority pending story.

Execute our standard workflow using the BMAD Runner Tool defined in CLAUDE.md.
Keep token usage strictly minimal by enforcing the Context Diet.
IMPORTANT: Each agent MUST get a unique task file name and a descriptive agent name.
Use the pattern:
  Task file: _bmad-output/task-<role>-<storyId>.txt
  Agent name: "<Role>-<StoryId>" (e.g., "PM-7.0", "Dev-7.2", "QA-7.2")

CRITICAL EXECUTION RULE: 
You MUST run the bmad-agent-runner.sh script SYNCHRONOUSLY and SEQUENTIALLY. 
DO NOT spawn multiple agents in parallel. 
DO NOT execute multiple Bash tools in the same turn.
DO NOT append `&` to the command. 
DO NOT run it in the background. 
DO NOT invent your own polling loops (no sleep, no grep). 
The script has its own internal lock-file polling. Just execute ONE command and wait patiently for the Bash tool to return before taking any other action.

1. Orchestrate a PM Agent to draft the story markdown file. 
(CRITICAL: First, use the Read tool on `_bmad/bmm/agents/pm.md` and paste its exact text at the top of the task file. Then instruct the PM to ONLY read the PRD, Architecture, and Epic files initially, only to read other documents if needed, to draft the story. Write the brief to a unique task file, then execute: `bash scripts/bmad-agent-runner.sh _bmad-output/task-pm-E1S3.txt "PM-E1S3"`).

2. Once drafted, orchestrate a Dev Agent (specify Frontend, Backend or Fullstack) or a QA agent, depending on story context, to implement the story exactly to spec using the same runner script method. 
(CRITICAL: First, use the Read tool on `_bmad/bmm/agents/dev.md` or `qa.md` and paste its exact text at the top of the task file. Then instruct the Agent to ONLY read the specific story file it needs to build. Tell it to reference architecture.md or ai-engine-specification.md only if it encounters a structural unknown. Remind them of the "Titanium Wall" constraints: PII scrubbing, zero-generation routing, and 48dp touch targets). Use: `bash scripts/bmad-agent-runner.sh _bmad-output/task-dev-E1S3.txt "Dev-E1S3"`

3. Once implemented, orchestrate an Agent to conduct a code review against our DoD (WCAG 2.1 AA, strict Next.js/FastAPI separation, and exact JSON schema adherence). 
(CRITICAL: First, use the Read tool on `_bmad/bmm/agents/qa.md` and paste its exact text at the top of the task file. Then instruct the Agent to ONLY read the implemented story file and the newly written code). Example use: `bash scripts/bmad-agent-runner.sh _bmad-output/task-qa-E1S3.txt "QA-E1S3"`

4. If issues are found, orchestrate the Dev/QA Agent to adjust the code. 
(Limit this to a maximum of 2 review cycles. If minor issues persist, output a warning for me and mark as 'review-pending').

5. Update sprint-status.yaml to mark it done.

6. LOOP: Immediately identify the next pending story in sprint-status.yaml and restart at step 1. DO NOT ask for permission to continue.

Keep your internal monologue brief between loops to save tokens. Let's build!
